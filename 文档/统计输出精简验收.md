# 统计输出精简验收

前半部分保留首次输出重构的验收记录；交付手册命令迁移和英文标签的后续验收见文末，
不能把前次“详细查验命令改为 diagnostic”的做法理解为当前普通使用流程。

## 本轮范围

按用户确认的“模型、必要参数、性能结果”三部分实施，不修改调度、时序判定、
请求数据路径或热模型计算公式。保留工作区前一轮配置重构及用户已有修改。

- summary/full 合并为精简人读报告，HBM4 示例约 28 行。
- JSON schema 2 分为 model、parameters、metrics、validation、changes、stacks。
- 该 HBM4 示例公共 metrics 为 21 项；修改前 schema 1 的平铺集合为 442 项，
  后者混有配置、路径和诊断，不能宣称删除了 421 个独立性能计数器。
- 公共结果集合真正缩减；只有显式 diagnostic 才导出内部审计字段。
- 取消七个重复派生项及其 C++ 辅助函数：三项 bytes/cycle、四项全局队列均值。
  保留有验证用途的命令、维护、PHY、后端原始计数，不以输出精简为由破坏测试。
- 删除性能曲线、敏感性及架构扫描中不再使用的三个文本解析器。
- 性能曲线用一个 row_hit_pct 替代三项行访问计数；曲线产物版本为 2。
- 敏感性分析关闭功耗/热时不再生成伪零测量，产物版本为 2。
- C++ 类型化结果直接序列化，64 位计数不经过 double，JSON 不继承终端舍入。
- 参数变化对比当前程序内置同标准基准；直接修改标准段也会识别。
  Timing 来源标签不冒充数值变化；power.source 切换计算公式则必须记为变化。

接口和字段完整定义见 [结果输出与实验有效性](结果输出与实验有效性.md)。
主手册、使用手册、项目指南、构建流程和相关目录 README 已同步。
详细查验命令改为 diagnostic；普通可视化/Demo 改读 JSON。历史验收记录加版本提示，
不把前一轮测试结果追写成这一轮结果。

## 验收证据

本轮运行产物位于 `outputs/stats_refactor_Ti4oH8/`；它是生成目录，不是新配置入口。

| 验收项 | 结果 / 证据 |
| --- | --- |
| Clang Debug 全套 CTest | 18/18，clang_tests.log |
| G++ Debug 全套 CTest | 18/18，gcc_tests.log；额外编译在临时目录，不增加仓库根目录 build |
| C++ 类型测试 | 大于 2^53 的计数、double 精度、转义、null、非有限值、迟到失败状态、参数来源语义 |
| Python 结果契约 | 12 项，result_contract.log；含四标准、三后端/两 PHY、配置复制与 replay |
| 修改前后四标准对比 | before_after.log；每标准命令 CSV 逐字节一致，340/341 项整数/布尔计数及配置精确一致 |
| 浮点结果对比 | 延迟、带宽、能量、温度在旧输出舍入精度内一致；新 JSON 保留原值精度 |
| Ramulator 四标准差分 | 225/225，ramulator.log/json；参考工作区 dirty 状态明确记录 |
| DRAMsim3 辅助对照 | 7/7，dramsim3.log/json；本轮未重跑外部 thermal runtime |
| 可视化与性能曲线示例 | example_run.log；14 个曲线点、6/6 检查，example/dashboard.html |
| 用户精简示例与表格 | summary.txt、result.json、resolved.cfg、table.txt |

结果契约还覆盖：纯写时读延迟 null、关闭模块不生成测量值、行命中率分母、
多实例统计汇总、直接改 HBM3 columns 后容量及 density 联动、诊断显式选择、
历史文本/schema 1 读取、错误路径和输入保护、截断及数据/Golden 不匹配。

回归中发现并修复的适配问题没有通过放宽数值门限解决：

1. 可视化和 Demo 原先解析人读 stdout，改为读取 JSON。
2. 专项 smoke/外部对照依赖内部键，改为显式 diagnostic。
3. 原敏感性工具把关闭模块的诊断零值作为测量，改为缺省，不补零。
4. 原 full 文本的舍入会掩盖 double 原始精度，比较时区分整数精确性和旧浮点显示精度。

## 使用与边界

```bash
./build-clang-debug/hbm_sim --config configs/hbm.cfg --standard hbm4 \
  --requests 128 --stats-json outputs/compact_run/result.json
python3 tools/view_stats.py outputs/compact_run/result.json \
  --keys standard,simulation_time_ns,achieved_bw_GBps,avg_read_latency_ns
```

命令行可不指定 stats view；旧 full 参数仍接受，但不再输出旧的数百行键。
需要 PHY/后端/响应队列等内部诊断时使用 `--stats-view diagnostic`。
未知或未收集指标不能视为零；无独立数据检查不能称为数据验证通过。

这些结果证明本轮输出重构的回归情况，不证明任意配置绝无错误，不构成厂商器件校准。
可视化本轮验证了数据读取与 HTML 生成，未将其宣称为所有浏览器的交互验收。

## 后续修订：交付手册配套迁移与英文输出（2026-09-09）

本次发现主模板虽已使用 summary，但手册普通命令又显式指定 diagnostic，覆盖了配置。
修订范围如下：

- 普通运行、四标准交付、后端、自定义配置和可视化步骤统一精简报告与公共 JSON。
- 人读 `.txt` 不再作为工具输入；配套迁移统计表、可视化、多 Stack 求和脚本。
- 普通指标表最多选 8 项，内部计数留在明确标注的可选诊断入口，完整诊断只写独立文件。
- 终端标签、说明和警告改为英文，三段标题为 MODEL / PARAMETERS / RESULTS；
  不增加指标、不改变 JSON 键名、时序/调度/数据路径或统计计算。
- 读取工具仍明确拒绝旧中文与新英文的人读报告，防止误当机器结果。
- 附录 B.9 的历史数字与当前运行分开说明；四标准和 D.4 保存实际生效参数快照。

验证在隔离临时输出目录进行，没有覆盖用户现有实验产物：

| 检查 | 本次结果 |
| --- | --- |
| 手册静态检查 | 108 个 Bash 代码块通过语法检查；相对文件链接和显式锚点有效 |
| 输出契约 | 手工命令仅保留 1 处可选 diagnostic；普通统计/可视化读取 JSON，不读精简文本 |
| 代表性命令复跑 | 四标准、异步响应、三后端及重开、多 Stack、自定义、可视化通过 |
| 生成结果 | 32 份结果文件（31 份公共结果、1 份专项诊断），7 份 HTML；公共报告无 diagnostics 分区 |
| 报告规模 | 单 Stack HBM4 128 请求示例仍为 28 行；多 Stack、Golden 等按实际启用状态附加少量行 |
| 英文前后对照 | 17 组配对运行的 model、parameters、metrics、validation、stacks 完全一致 |
| Clang Debug CTest | 18/18 通过；结果契约新增四标准默认/summary/full 英文标签、多 Stack 和人读输入拒绝检查 |

四标准交付运行使用原示例的 1024 请求；D.4 本次只覆盖 128 请求的产物 smoke，
没有把它宣称为原 20000 请求性能实验完成。也未在本次重跑整份手册的全部构建、
外部 Ramulator/DRAMsim3 校准或浏览器交互；这些结论不由输出迁移自动获得。
