
## 1. 配置体系

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| 配置入口 | 将主要配置收敛为 `configs/hbm.cfg` 和 `configs/lpddr.cfg` 两大入口 | 减少旧版本多入口配置带来的混乱 | `configs/hbm.cfg`、`configs/lpddr.cfg` |
| 标准选择 | 在大配置中按 HBM3/HBM4、LPDDR5/LPDDR6 分标准段组织参数 | 复制一个 HBM 或 LPDDR 配置即可形成自定义模型 | `configs/hbm.cfg`、`configs/lpddr.cfg` |
| 参数联动 | 集中处理 `data_rate_mbps`、`speed_bin_mbps`、`tCK_ps`、`density_gb`、`data_bus_bits`、`sids` 等关系 | 避免改行列数、速率或 stack height 后忘记同步相关字段 | `src/config/model.cpp`、`include/hbm_sim/config/model.hpp` |
| 字段映射 | 将配置 key 到模型字段的映射从 CLI 中拆出 | CLI 更短，字段来源更容易审计 | `include/hbm_sim/config/fields.hpp`、`src/cli/main.cpp` |
| 配置解析 | 增加 schema、section、override、preset、extends 等解析逻辑 | 配置层级更清楚，适合前端读取后覆盖默认参数 | `src/config/document.cpp`、`include/hbm_sim/config/parse.hpp` |
| 配置注释 | 补充 `inject_interval=0` 和 `stack_dispatch_width` 的真实含义 | 避免把饱和注入、入口分发瓶颈误解成 DRAM 本体性能 | `configs/hbm.cfg`、`configs/lpddr.cfg`、`文档/cfg指南.md` |

## 2. 模型架构

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| MemorySystem | 强化主动多 Stack、多 Channel 系统模型 | 支持每个 Stack 独立入口队列、分发宽度、后端和统计 | `src/core/system.cpp`、`include/hbm_sim/core/system.hpp` |
| Stack 路由 | 增加 stack mapping、stack interleave、stack ingress buffer、dispatch width 等系统层参数 | 能模拟多 Stack 地址分布和入口反压 | `src/core/system.cpp`、`configs/hbm.cfg`、`configs/lpddr.cfg` |
| Controller 队列 | 拆出请求队列相关结构 | 控制器代码更清楚，读写队列和维护队列更容易维护 | `include/hbm_sim/controller/request_queues.hpp`、`src/controller/controller.cpp` |
| Timing 逻辑 | 增强 timing 约束和边界检查 | 更容易验证 `t-1` 拒绝、`t` 允许这类时序边界 | `src/controller/timing.cpp`、`include/hbm_sim/controller/timing.hpp` |
| Mem PHY | 明确 direct / behavioral PHY，并保留协议适配、队列、流水线、训练、DFI 事件 | 能表达控制器到存储层之间的行为级 PHY | `src/dram/mem_phy.cpp`、`include/hbm_sim/dram/mem_phy.hpp` |
| 存储语义 | 强化 MemoryImage 的数据、掩码、初始化、row buffer、ECC、物理坐标和热事件 | 不只是统计请求，还能查真实数据写入和读回是否一致 | `src/core/data.cpp`、`include/hbm_sim/core/data.hpp` |
| 后端存储 | 支持 sparse、mmap_sparse、chunk_file 后端 | 大容量模型不用完整预分配内存，也能做文件持久化查验 | `src/core/memory_backend.cpp`、`include/hbm_sim/core/memory_backend.hpp` |

## 3. HBM / LPDDR 建模

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| HBM 组织 | 明确 Channel、Pseudo-channel、SID、BG、Bank、Row、Column 等逻辑层级 | 更接近 Ramulator 式层级几何容量计算 | `configs/hbm.cfg`、`src/dram/profiles.cpp`、`include/hbm_sim/dram/spec.hpp` |
| HBM 行为 | 加强 HBM 行列总线、边沿配对、same/different BG/SID 等约束 | HBM3/HBM4 的 timing 差异更容易表达和验证 | `src/controller/timing.cpp`、`src/dram/profiles.cpp` |
| LPDDR 组织 | 明确 LPDDR5/LPDDR6 的 channel/subchannel/rank/bank 组织 | HBM 和 LPDDR 不再混用同一套解释 | `configs/lpddr.cfg`、`src/dram/profiles.cpp` |
| LPDDR 行为 | 增加 split ACT、WCK、CAS、DVFS、bank-pair refresh 等入口 | 能覆盖 LPDDR6 里和 HBM 不同的关键行为 | `src/controller/timing.cpp`、`src/dram/spec.cpp`、`src/dram/profiles.cpp` |
| 容量计算 | 容量由逻辑几何计算，`density_gb` 通常自动推导 | 修改 bank、row、column 后容量会随之变化，显式 density 会参与一致性检查 | `src/config/model.cpp`、`include/hbm_sim/dram/spec.hpp` |
| 标准边界 | 明确当前模型是行为级研究模型，不是完整 JEDEC 合规认证器 | 防止把研究默认参数误认为厂商校准模型 | `文档/审计.md`、`堆叠存储模型交付手册.md` |

## 4. 控制器和前端

| 更新处 | 更新了什么 | 优化了什么 | 涉及文件 |
|---|---|---|---|
| 前端请求 | 支持合成流量、trace 流量和异步请求响应接口 | 后续可以接外部 CPU/UCIe/SystemC/RTL 驱动 | `src/frontend/traffic.cpp`、`include/hbm_sim/core/request.hpp`、`include/hbm_sim/core/response.hpp` |
| 突发拆分 | HostRequest 可拆成多个 DRAM transaction | 支持 host line 与 DRAM transaction payload 不同粒度 | `src/frontend/traffic.cpp`、`src/core/system.cpp` |
| 调度策略 | 保留 FCFS、FR-FCFS、行策略和地址映射策略 | 可以做控制器策略对比实验 | `src/controller/scheduler.cpp`、`src/controller/row_policy.cpp`、`src/core/addr_map.cpp` |
| 响应链路 | 增加 HostResponse / TransactionResponse 输出和队列容量 | 可以查每个请求或每个子事务的完成时间和数据返回 | `src/core/system.cpp`、`include/hbm_sim/core/system.hpp` |

## 5. 输出和统计

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| 结果输出 | 新增 Result 层，统一组织终端摘要、JSON 和指标选择 | 结果格式不再完全散落在 CLI 和 Stats 中 | `src/stats/result.cpp`、`include/hbm_sim/stats/result.hpp` |
| 统计字段 | 增加 payload、interface、带宽、延迟、后端、热、ECC、响应等字段 | 能从更多角度解释仿真结果 | `src/stats/stats.cpp`、`include/hbm_sim/stats/stats.hpp` |
| JSON 工具 | 增加 Python 结果读取工具 | 工具不需要解析人读终端输出 | `tools/result_io.py` |
| 输出指南 | 单独整理输出字段、产物、性能指标和常见误读 | 用户更容易理解 stats、CSV、热图和 dashboard | `文档/输出指南.md` |

## 6. 验证体系

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| Timing 边界测试 | 增加/强化 `t-1` 拒绝、`t` 允许的边界验证 | 直接验证核心时序约束是否按边界生效 | `tests/timing_boundary_tests.cpp`、`tools/timing_boundary_validation.py` |
| 配置测试 | 增加配置解析、自动推导、冲突检测测试 | 防止配置重构后 silent wrong | `tests/model_config_tests.cpp`、`tests/config_tests.cpp` |
| 调度契约测试 | 增加调度策略行为测试 | 防止 FCFS/FR-FCFS 等策略后续被改坏 | `tests/scheduler_contract_tests.cpp` |
| 结果测试 | 增加结果格式和值检查 | 保证 JSON/summary 等输出能被工具稳定消费 | `tests/result_tests.py`、`tests/result_value_tests.cpp` |
| 序列测试 | 强化异步请求响应、数据路径和维护命令检查 | 验证完整请求链路，而不只是最终统计 | `tests/sequence_tests.cpp` |
| PHY 测试 | 强化 direct/behavioral PHY 行为检查 | 验证 PHY 队列、流水线和协议事件 | `tests/phy_tests.cpp`、`tests/phy_smoke.sh` |
| 外部对照 | 强化 Ramulator2 差分和 DRAMsim3 辅助验证入口 | 增加与开源仿真器对照的可信度证据 | `tools/ramulator2_differential.py`、`tools/dramsim3_aux_validation.py` |
| Smoke 测试 | 更新 smoke、可视化、多 Stack、后端等快速检查脚本 | 更适合交付前快速查验项目是否能跑通 | `tests/smoke.sh`、`tests/visualization_smoke.sh`、`tests/multistack_backend_smoke.sh` |

## 7. 可视化和实验工具

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| 可视化 dashboard | 从命令、统计、DFI、热图等产物生成离线 HTML | 不需要服务器即可查看仿真过程 | `tools/visualize.py`、`tools/visualize_example.sh` |
| 性能曲线 | 扫描注入率，输出延迟、吞吐、利用率曲线数据 | 能观察注入压力变化下的性能趋势 | `tools/performance_curve.py` |
| 架构扫描 | 批量跑 HBM/LPDDR、stack 数、映射、链路保护等组合 | 便于做架构参数对比 | `experiments/architecture_sweep/run.py` |
| 后端审计 | 增加后端查验脚本 | 便于比较 sparse、mmap_sparse、chunk_file 的数据一致性 | `tools/audit_case_backends.sh` |
| 指标查看 | 增加 stats 提取和对比工具 | 减少手工从大段输出中找字段 | `tools/view_stats.py`、`tools/compare_stats.py` |

## 8. 文档结构

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| 交付手册 | 保留总体交付路线、模型说明、使用流程和验证依据 | 作为项目总入口 | `堆叠存储模型交付手册.md` |
| 项目指南 | 单独说明架构、构建、运行、数据路径、后端、热模型和测试 | 读者能先理解项目怎么运行 | `文档/项目指南.md` |
| cfg 指南 | 单独说明每个配置项的含义、可改范围和联动项 | 用户自定义配置时不用在源码里找字段 | `文档/cfg指南.md` |
| 输出指南 | 单独说明 stats、CSV、JSON、热图和 dashboard | 输出结果更容易解释 | `文档/输出指南.md` |
| 审计说明 | 单独说明 JEDEC、Ramulator2、DRAMsim3、研究默认值和厂商校准边界 | 明确模型可信度和限制 | `文档/审计.md` |

## 9. 构建和工程组织

| 更新处 | 更新 | 优化 | 涉及文件 |
|---|---|---|---|
| CMake | 更新 CMake 构建目标和测试入口 | 更适合 Clang/Ninja/CTest 工作流 | `CMakeLists.txt` |
| Makefile | 保留 Make 兼容入口，增加测试和工具目标 | 方便不使用 CMake 的环境快速运行 | `Makefile` |
| 源码目录说明 | 各目录 README 重新说明模块职责 | 新读者更容易定位代码 | `src/README.md`、`include/README.md`、各子目录 `README.md` |
| 测试目录说明 | 说明各测试文件用途 | 方便用户知道每个 test 在验证什么 | `tests/README.md` |

## 10. 主要修改点概括

1. 修改配置体系：从多类分散配置，改为 HBM / LPDDR 主配置加标准段和覆盖项。
2. 修改容量/密度处理：容量按逻辑几何计算，`density_gb` 推荐自动推导或一致性校验。
3. 修改 CLI 结构：把大量配置字段处理拆到 config 模块。
4. 修改统计输出：新增 Result 层和 JSON 读取工具。
5. 修改 MemorySystem：强化多 Stack、多 Channel、入口队列和分发宽度。
6. 修改 Mem PHY：明确 direct / behavioral 模式和 DFI-oriented 行为事件。
7. 修改数据后端：强化真实 payload、初始化、掩码、golden、mismatch 和文件后端。
8. 修改验证脚本：补充 timing、配置、调度、结果、PHY、可视化、外部对照等测试。
9. 修改文档结构：将配置、输出、审计、项目架构从交付手册中拆成专题指南。
10. 修改注释说明：补充 `inject_interval=0`、`stack_dispatch_width`、HBM rank 占位、density 自动推导等容易误读的地方。

## 11. 主要优化效果

| 优化方向 | 效果 |
|---|---|
| 配置简化 | 用户主要面对 HBM / LPDDR 两个主配置，而不是大量分散入口 |
| 参数一致性 | 几何、容量、密度、速率、tCK、总线宽度等关系更不容易错 |
| 可扩展性 | 多 Stack、后端、PHY、响应、热模型都有更明确的接口位置 |
| 可验证性 | 不只看最终 stats，还能导出 command、DFI、response、memory、thermal 等产物交叉检查 |
| 可维护性 | CLI、config、stats/result、controller、tools、docs 职责更分明 |
| 可信度说明 | 明确区分 JEDEC 依据、开源仿真器对照、研究默认值和厂商校准缺口 |

## 12. 仍需注意

1. `stack_dispatch_width=4` 对 HBM3/HBM4 宽栈高压实验可能先形成入口分发瓶颈；做饱和带宽实验时应显式调大。
2. `inject_interval=0` 是不按时间节流的饱和注入，不是普通注入间隔。
3. HBM 中的 `rank=1` 是统一模型占位维度，不是 JEDEC HBM 物理 rank。
4. DFI 和 PHY 是行为级模型，不是完整 pin-level 电气模型。
5. Ramulator2/DRAMsim3 对照能提高可信度，但不能替代厂商模型或硬件校准。
