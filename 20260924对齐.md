## 1、写事务的平均延迟模型

### 1.1 平均写延迟的定义和计算公式

当前 `avg_write_latency` 是 **DRAM transaction 从 Controller 成功接收到完成的平均周期数**：

```text
单事务写延迟 = completion_cycle - arrival_cycle

avg_write_latency_ticks
  = total_write_latency / completed_writes

avg_write_latency_ns
  = avg_write_latency_ticks × tick_duration_ps / 1000
```

它不是单纯的 `nCWL`，也不是 Host 侧完整请求的端到端延迟。它包含 Controller 排队、行操作、Timing 等待、刷新干扰、总线竞争以及 Behavioral PHY 写数据延迟。

### 1.2 起点在哪里

请求被 Controller 成功接收时执行：

```cpp
req.arrival = clk_;
```

对应 [`src/controller/controller.cpp:158`](src/controller/controller.cpp:158)。

这里有两个重要口径：

1. Frontend 因队列满而反复重试的时间发生在成功进入 Controller 之前，通常不包含在该 Controller transaction latency 中；它另由 injection stall 等指标反映。
2. 64 B Host request 若拆成两个 32 B transaction，会形成两个独立 arrival 和两个延迟样本。因此 transaction 平均写延迟不等于 HostResponse 延迟。

### 1.3 什么时候记录 issued cycle

ACT、PRE 等前置命令不会结束写请求。请求一直留在队列或 active buffer，直到真正发出终端数据命令 WR/WRA：

```cpp
done_req.issued_cycle = clk_;
```

对应 [`src/controller/controller.cpp:1663`](src/controller/controller.cpp:1663)。

所以：

```text
issued_cycle - arrival_cycle
```

主要反映 Controller 排队、PRE/ACT、Timing、刷新和总线竞争；

```text
completion_cycle - issued_cycle
```

主要反映写数据路径和项目定义的完成点。

### 1.4 Direct PHY 下如何确定完成时间

普通 WR 在 Direct 路径中默认于发出后一拍完成：

```cpp
done_req.completion = clk_ + 1;
```

见 [`src/controller/controller.cpp:1684`](src/controller/controller.cpp:1684)。这是一种行为级 acknowledgement，不表示真实 DRAM 单元在一拍内完成全部内部恢复。

WRA 带自动预充。它的完成点还必须等待：

```text
nCWL + nBL + nWR
```

代码取原完成点与该 gate 的最大值，见 [`src/controller/controller.cpp:1692`](src/controller/controller.cpp:1692)。这避免 WRA 在写数据、burst 和 write recovery 完成前就关闭物理 row buffer。

### 1.5 Behavioral PHY 下如何确定完成时间

Behavioral PHY 接收 WR/WRA 后，写数据槽的 ready cycle 为：

```text
issued_cycle
+ timing_delay(dfi_write_latency_nck 或 nCWL
               + write_data_pipeline_cycles)
```

见 [`src/dram/mem_phy.cpp:195`](src/dram/mem_phy.cpp:195)。ready 后才真正写入 `MemoryImage` 并生成 PHY completion，见 [`src/dram/mem_phy.cpp:247`](src/dram/mem_phy.cpp:247)。

Controller 的 `complete_pending()` 会等待这一 completion，并以 PHY 返回的 completion cycle 为最终事实，见 [`src/controller/controller.cpp:649`](src/controller/controller.cpp:649)。

### 1.6 平均值在哪里累计

写事务真正完成时：

```cpp
stats_.completed_writes++;
stats_.total_write_latency += it->completion - it->arrival;
```

见 [`src/controller/controller.cpp:687`](src/controller/controller.cpp:687)。平均值定义见 [`include/hbm_sim/stats/stats.hpp:264`](include/hbm_sim/stats/stats.hpp:264)，tick 转 ns 见 [`src/stats/result.cpp:118`](src/stats/result.cpp:118)。

### 1.7 特殊路径

- 同地址写合并可能 `bypass_dram`，完成时间设为下一拍，见 [`src/controller/controller.cpp:213`](src/controller/controller.cpp:213)。它仍计为一个完成 transaction，但不再次占用 DRAM 总线。
- 输出 response queue 满时，已经到 completion 的事务仍会滞留，见 [`src/controller/controller.cpp:642`](src/controller/controller.cpp:642)。因此有限响应队列会影响观测完成点。
- `phy_total_write_service_cycles` 只统计 issued 到 PHY completion 的服务时间，不等于包含 Controller 排队的 `avg_write_latency`，见 [`src/dram/mem_phy.cpp:283`](src/dram/mem_phy.cpp:283)。

**交付手册对应说明**

- [交付手册 2.4.1“队列”](堆叠存储模型交付手册.md#241-队列)：解释 write buffer、active、pending 以及写高/低水位怎样形成排队时间。
- [交付手册 2.7.1“请求层级和拆分”](堆叠存储模型交付手册.md#271-请求层级和拆分)：解释 Host request 与 DRAM transaction 的区别，说明为什么平均写延迟按 transaction 统计。
- [交付手册附录 B.2“性能、延迟和队列”](堆叠存储模型交付手册.md#b2-性能延迟和队列)：明确写延迟的起点、终点、Direct/Behavioral 完成口径和无写样本时的 N/A 规则。

交付手册给出对外交付口径；本回答进一步把该口径落实到 `arrival`、`issued_cycle`、`completion` 和累计公式的具体代码位置。

## 2、热模型与功耗模型的架构讲解

### 2.1 整体数据流

```text
Controller 发出命令
→ apply_storage_command_event()
→ MemoryImage::record_command_event()
→ command_energy_pj() 计算事件能量
→ 分类累计 ACT/PRE/RD/WR/REF/RFM/control 能量
→ physical_address() 得到 Stack/Layer/Tile/Thermal cell
→ apply_thermal_event()
→ 冷却、升温、横向/纵向耦合
→ 输出功耗统计和 thermal map
```

入口见 [`src/controller/controller.cpp:975`](src/controller/controller.cpp:975)，功耗/热共同入口见 [`src/core/data.cpp:1402`](src/core/data.cpp:1402)。

### 2.2 功耗模型

功耗采用“每条命令事件能量”抽象。`command_energy_pj()` 根据命令选择能量：

- ACT/ACT1/ACT2 → activate energy；
- PRE/PREpb/PREab → precharge energy；
- RD/RDA/CAS_RD → read energy；
- WR/WRA/CAS_WR → write energy；
- REFab/REFpb/REFdb → refresh energy；
- RFM → RFM energy；
- 其他命令 → control energy。

实现见 [`src/core/data.cpp:295`](src/core/data.cpp:295)。

项目支持两种参数来源：

1. `configured_pj`：配置直接给每命令 pJ；
2. `dramsim3_idd`：借鉴 DRAMsim3/IDD 电流差分形式，用 VDD、IDD、命令持续时间和器件数推导事件能量，见 [`src/core/data.cpp:370`](src/core/data.cpp:370)。

最终能量乘 `power_scale`，并累计到总能量和分类能量，见 [`src/core/data.cpp:1412`](src/core/data.cpp:1412)。这不是直接调用 DRAMsim3，也不是厂家校准后的绝对功耗模型。

当前主要缺口是 background/residency 功耗不完整。例如已有 `idd6x_ma` 参数，但 Self-Refresh 驻留尚未按 `IDD6 × VDD × 时间` 计能；当前 SREFEN/SREFEX 主要落入 control command energy。

### 2.3 热模型

命令能量通过 `apply_thermal_event()` 注入对应 thermal cell：

1. 根据当前周期与 `last_cycle` 的差计算冷却；
2. 温度向 ambient 按 `cooling_per_cycle` 衰减；
3. 增加 `energy_pj × rise_c_per_pj`；
4. 向同层四个相邻 cell 做 lateral coupling；
5. 向上下层做 vertical/TSV coupling；
6. 更新峰值和热点坐标。

冷却见 [`src/core/data.cpp:1239`](src/core/data.cpp:1239)，邻居耦合见 [`src/core/data.cpp:1276`](src/core/data.cpp:1276)，热事件见 [`src/core/data.cpp:1366`](src/core/data.cpp:1366)。

纵向耦合会根据 thermal cell 面积、TSV 半径/数量、硅和铜导热系数调整，见 [`src/core/data.cpp:1250`](src/core/data.cpp:1250)。但它仍是稀疏行为级一阶模型，不是完整 3D RC 网络、有限元求解器或器件级热仿真。

### 2.4 输出应该怎样解释

- `power_energy_pJ`：所有已记录命令事件能量总和；
- 分类能量：按命令类别拆分；
- `thermal_peak_temp_C`：运行中已实例化 thermal cells 的最高温度；
- `thermal_avg_temp_C`：查询时刻已实例化稀疏节点的空间平均，不是整个未触达 die 的平均，也不是时间平均；
- thermal map：每个 cell 的最终温度、累计能量和事件数。

因此这些结果适合相同参数体系下的相对趋势，不应直接写成某厂家 HBM/LPDDR 的绝对功耗或温度。

**交付手册对应说明**

- [交付手册 2.6.3“物理、功耗、热和 ECC”](堆叠存储模型交付手册.md#263-物理功耗热和-ecc)：给出命令 pJ、DRAMsim3-oriented IDD 公式和稀疏三维热耦合的总体边界。
- [交付手册附录 B.6“功耗、热、ECC、RAS 和模式”](堆叠存储模型交付手册.md#b6-功耗热eccras-和模式)：解释 Energy、Mean/Peak Temperature 的输出语义。
- [交付手册 B.9.5～B.9.6](堆叠存储模型交付手册.md#b95-从能量得到平均功率)：说明能量转平均功率、冷却/升温公式和绝对温度不可直接校准的原因。

手册是架构与输出说明；本回答补充了命令能量分类、热更新调用链以及 Self-Refresh 驻留能量尚未计入这一实现边界。

## 3、cmd和dfi的validation逻辑

### 3.1 两者检查对象不同

```text
Command Validation：IssuedCommand 是否满足 DRAM 状态和 Timing
DFI Validation：IssuedCommand 是否被正确转换为 DFI command/data events
```

它们都不是重新运行一遍 Controller 调度，而是对仿真产生的结构化轨迹进行重放检查。CLI 调用位置分别是 [`src/cli/main.cpp:2221`](src/cli/main.cpp:2221) 和 [`src/cli/main.cpp:2234`](src/cli/main.cpp:2234)。

### 3.2 Command Validation

Validator 按 Stack 分组，避免不同 Stack 的同 ID/同周期事件混淆，入口见 [`src/validation/validator.cpp:820`](src/validation/validator.cpp:820)。每个 Stack 内按 trace 顺序维护独立的 Bank/Channel 状态和 Timing 历史。

主要检查：

- Bank 是否 Closed/Activating/Opened；
- RD/WR 是否命中已打开 Row；
- PRE、ACT、refresh、RFM 的状态是否合法；
- same/different Bank、BG、PC、Rank、Channel 的 Timing；
- tFAW 滑动窗口；
- HBM Row/Column bus 和边沿配对；
- LPDDR ACT1/ACT2 配对；
- WCK readiness；
- PDE/PDX、SREFEN/SREFEX 状态。

状态检查核心在 [`src/validation/validator.cpp:355`](src/validation/validator.cpp:355)，Timing 重放在 [`src/validation/validator.cpp:553`](src/validation/validator.cpp:553)。约束不是一个单独 cfg 文件，而来自：

```text
DramSpec Timing 数值
+ src/dram/spec.cpp 构建的 TimingConstraint 表
+ validator.cpp 的协议状态规则
```

### 3.3 DFI Validation

DFI 生成器先把每条 issued command 转成一条 `COMMAND` event；RD/RDA 再生成 `READ_DATA` beats，WR/WRA 生成 `WRITE_DATA` beats。入口见 [`src/validation/dfi.cpp:215`](src/validation/dfi.cpp:215)。

验证器检查：

1. 每条 issued command 是否恰有一条 COMMAND event；
2. DFI 中是否有无来源的孤立事件；
3. cycle、issued cycle、bus、地址和 decoded location 是否一致；
4. `ceil(payload_bytes/beat_bytes)` 是否等于实际 beat 数；
5. beat index、周期、地址递增是否正确；
6. 读写信号字段是否互斥且完整；
7. payload、初始化 mask、write mask 是否一致；
8. 有 expected payload 时，读数据是否匹配。

入口见 [`src/validation/dfi.cpp:307`](src/validation/dfi.cpp:307)，command 一一对应检查见 [`src/validation/dfi.cpp:428`](src/validation/dfi.cpp:428)，data beat 检查见 [`src/validation/dfi.cpp:455`](src/validation/dfi.cpp:455)。

### 3.4 PASS 能证明什么

PASS 能证明轨迹满足当前检查器中已实现的规则，不能单独证明：

- 覆盖全部 JEDEC 条款；
- DFI 完整信号级合规；
- 与真实厂家器件一致；
- 检查器与生成器没有共享相同理解错误。

因此还需要 JEDEC `t-1` 禁止/`t` 允许边界测试，以及 Ramulator2/DRAMsim3 的独立共同面对照。

**交付手册对应说明**

- [交付手册 2.8“统计、输出与验证”](堆叠存储模型交付手册.md#28-统计输出与验证)：区分在线检查、command/DFI validator、CTest、外部差分和硬件校准。
- [交付手册 8.2“可信度证据链”](堆叠存储模型交付手册.md#82-可信度证据链)：说明每一层验证能证明什么、不能证明什么。
- [交付手册 8.4“项目内验证”](堆叠存储模型交付手册.md#84-项目内验证)：给出 `model_validation.py` 和 timing boundary validation 的运行入口。
- [交付手册附录 B.5“PHY 与 DFI”](堆叠存储模型交付手册.md#b5-phy-与-dfi)：强调 DFI PASS 是行为事件一致性，不是电气或训练认证。

本回答相对手册增加了两个 Validator 的逐项判断顺序和准确代码入口。

## 4、DRAM本身的刷新机制（我已经初步确认）

### 4.1 为什么需要刷新

真实 DRAM 用电容保存数据，电荷会泄漏。周期级模拟器通常不求解每个电容电压，而把物理保持要求抽象为刷新 deadline、刷新命令、资源占用、Timing 阻塞和能耗。

### 4.2 正常工作状态的周期刷新

当前项目有 `RefreshManager`。它为每 Rank 保存 `next_refresh_cycle` 和 credit。每跨过一个 `nREFI/nREFIpb` deadline，credit 增加；Controller 再把刷新义务变成命令。

- all-bank → 为每个 PC/SID scope 生成 REFab；
- HBM per-bank → 按 SID/BG/Bank 轮转生成 REFpb；
- LPDDR6 非 all-bank → 按双 Bank 配对生成 REFdb。

deadline/credit 逻辑见 [`src/controller/refresh.cpp:28`](src/controller/refresh.cpp:28)，命令批次见 [`src/controller/refresh.cpp:139`](src/controller/refresh.cpp:139)，Controller 调度入口见 [`src/controller/controller.cpp:583`](src/controller/controller.cpp:583)。

`REFpb` 是指定 Bank；`REFdb` 是 LPDDR6 双 Bank；`REFab` 是目标作用域全部 Bank。LPDDR6 配置枚举即使叫 `per_bank`，dual-bank 路径实际发的是 REFdb。

### 4.3 Self-Refresh

正常刷新由 Controller 发 REF；Self-Refresh 是 Controller 发 SREFEN 后由器件内部维持刷新，退出发 SREFEX。

当前项目已有 SREF 命令、入口状态、Behavioral PHY LowPower 阻塞、退出命令和 nSREFEX，见：

- [`src/dram/state.cpp:80`](src/dram/state.cpp:80)；
- [`src/dram/mem_phy.cpp:143`](src/dram/mem_phy.cpp:143)；
- [`src/controller/controller.cpp:1611`](src/controller/controller.cpp:1611)；
- [`src/controller/executor.cpp:264`](src/controller/executor.cpp:264)。

但自动低功耗路径、RefreshManager deadline 暂停/恢复、IDD6 驻留能量和逐 Row 内部刷新仍不完整。数据后端也不会因长期不刷新自动丢数据。

### 4.4 是否需要物理漏电模型

若研究性能、刷新干扰和低功耗状态，不需要立即实现逐 Row 漏电；优先补齐 Self-Refresh 与 RefreshManager 耦合及 IDD6 驻留能量。

若研究 retention，则必须一起实现 Row 保持时间、温度/工艺影响、内部刷新游标、漏刷后的数据错误和 ECC 交互。单独增加“内部刷新次数”不能验证数据保持。

**交付手册对应说明**

- [交付手册 2.4.5“Refresh、RFM/PRAC 与低功耗”](堆叠存储模型交付手册.md#245-refreshrfmprac-与低功耗)：解释 per/all-bank refresh、postpone/pull-in、显式 SREF 和自动 residency 的区别。
- [交付手册附录 A.5“Refresh、RFM、低功耗和协议模式”](堆叠存储模型交付手册.md#a5-refreshrfm低功耗和协议模式)：列出相关配置字段。
- [交付手册 8.1“当前模型完成度”](堆叠存储模型交付手册.md#81-当前模型完成度)：将刷新归入可验证命令级模型，同时保留器件内部机制的校准边界。

手册已经说明外部刷新和低功耗状态；本回答进一步明确了“周期刷新已实现”和“逐电容漏电/数据失效未实现”不是同一层问题。

## 5、row_hit的判断逻辑及其对应的代码位置

### 5.1 判定条件

针对一个普通读写 transaction 的目标 Bank：

```text
bank.open_row == request.row  → row hit
bank.open_row >= 0 且不等于 request.row → row conflict
bank.open_row < 0             → row miss
```

实现位于 [`src/controller/controller.cpp:1716`](src/controller/controller.cpp:1716)。

### 5.2 为什么要在调度时判断

统计发生在命令真正 issue 的路径中，调用顺序是：

```text
append_issued()
→ PHY accept
→ classify_row_status(req)
→ 执行命令并更新 BankState
```

见 [`src/controller/controller.cpp:1560`](src/controller/controller.cpp:1560)。它在本条命令改变 Bank 状态之前观察当前 open row，才能反映请求刚开始服务时看到的行状态。

### 5.3 为什么一个请求只统计一次

一个 miss 请求可能依次发 ACT、RD，conflict 可能发 PRE、ACT、RD。如果每条命令都分类，会把同一请求重复计数。

因此 `Request` 带 `row_status_counted`，首次真正参与 issue 后置 true；之后 promote 到 active buffer 或继续发 CAS/RD/WR 都不再统计。见 [`src/controller/controller.cpp:1717`](src/controller/controller.cpp:1717) 和 [`include/hbm_sim/core/request.hpp:58`](include/hbm_sim/core/request.hpp:58)。维护请求不进入 row hit/miss/conflict。

### 5.4 Row hit 与命令合法性

在线状态检查也构造：

```cpp
snapshot.row_hit = bank.open_row == req.decoded.row;
```

见 [`src/controller/controller.cpp:2040`](src/controller/controller.cpp:2040)。RD/WR 只有 Bank 为 Opened 且 row_hit 时才合法，规则见 [`src/dram/state.cpp:32`](src/dram/state.cpp:32)。

### 5.5 两组相近统计不要混淆

- `row_hits/row_misses/row_conflicts`：Controller 按请求首次服务状态统计；
- `rowbuf_hits/rowbuf_misses`：MemoryImage 数据 row-buffer 的存储实现统计。

前者适合分析调度和 DRAM 命令行为；后者适合分析数据后端 row-buffer materialization。它们口径不同，不要求数值相等。字段注释见 [`include/hbm_sim/stats/stats.hpp:24`](include/hbm_sim/stats/stats.hpp:24) 和 [`include/hbm_sim/stats/stats.hpp:129`](include/hbm_sim/stats/stats.hpp:129)。

**交付手册对应说明**

- [交付手册 2.4.2“调度器与行策略”](堆叠存储模型交付手册.md#242-调度器与行策略)：说明 FR-FCFS、open/closed page 与 row hit 的关系。
- [交付手册附录 B.3“行行为和命令”](堆叠存储模型交付手册.md#b3-行行为和命令)：给出 hit/miss/conflict 三类定义、`row_hit_pct` 分母以及 transaction 统计口径。

本回答补充了 `classify_row_status()` 的精确执行时机、`row_status_counted` 防重复机制，以及 Controller row status 与 MemoryImage rowbuf 统计的区别。

## 6、写延迟的相关介绍

### 6.1 写请求可能经历的阶段

```text
Frontend 注入
→ 写队列等待
→ 可能进入 write-drain 模式
→ row conflict 时 PRE
→ row closed 时 ACT（LPDDR6 可能 ACT1/ACT2）
→ 等待 nRCDWR/命令总线
→ WR/WRA
→ Behavioral PHY 等待 nCWL/DFI write latency/pipeline
→ 写入 MemoryImage
→ TransactionResponse
→ 多 transaction 合并成 HostResponse
```

因此写延迟不能用一个 `nCWL` 表示。

### 6.2 影响写延迟的主要参数

- 队列：`write_buffer_size`、到达速率、已有 backlog；
- 调度：FCFS/FR-FCFS、row hit 优先级；
- 写排空：`write_low_watermark`、`write_high_watermark`；
- 行状态：hit 不需 ACT，conflict 需 PRE+ACT；
- Timing：nRCDWR、nCWL、nBL、nWR、nRP；
- 总线换向：读转写、写转读限制；
- 维护：refresh/RFM；
- PHY：write FIFO、write_data_pipeline_cycles、反压；
- 输出：response queue backpressure。

写模式统计和切换位于 [`src/controller/controller.cpp:560`](src/controller/controller.cpp:560) 及写队列调度路径；Timing constraint 在 [`src/dram/spec.cpp`](src/dram/spec.cpp)。

### 6.3 WR 与 WRA

WR 写数据后保持 Row 打开，便于后续 row hit。WRA 带 auto-precharge，会在满足写数据、burst 和 write recovery 后关闭 Row。WRA 的完成 gate 见 [`src/controller/controller.cpp:1692`](src/controller/controller.cpp:1692)。

### 6.4 写完成语义

Direct 模式普通 WR 默认发出后一拍完成，是模型的 acknowledgement 抽象。Behavioral 模式在 PHY 写数据 ready 时才 commit 到 MemoryImage 并完成。外部系统若需要“进入写队列即返回”“进入 PHY 即返回”或“持久写入后返回”等其他语义，应另设 acknowledgement point，不能混用当前 `avg_write_latency`。

### 6.5 怎样分析写延迟

不能只看平均值。至少同时查看：

- `avg_write_latency` / ns；
- write queue average/peak；
- write-mode cycles；
- row hit/conflict；
- WR/WRA/PRE/ACT 数量；
- read↔write turnaround stalls；
- refresh/RFM；
- `phy_data_backpressure` 和 write FIFO peak；
- write ratio 和 injection rate。

性能曲线工具会输出 `avg_write_latency_ticks`，见 [`tools/performance_curve.py:82`](tools/performance_curve.py:82)，Dashboard 可切换 read/write latency 曲线，见 [`tools/visualize.py:259`](tools/visualize.py:259)。

**交付手册对应说明**

- [交付手册 2.4.1～2.4.4](堆叠存储模型交付手册.md#24-memory-controller)：从队列、调度、Bank 状态和 TimingEngine 解释写请求等待来源。
- [交付手册附录 B.2](堆叠存储模型交付手册.md#b2-性能延迟和队列)：给出对外的写事务延迟定义和 Direct/Behavioral 模式差异。
- [交付手册 B.9.4“延迟应与吞吐一起看”](堆叠存储模型交付手册.md#b94-延迟应与吞吐一起看)：说明为什么不能脱离注入率和吞吐只比较平均延迟。

问题 1 重点回答“平均值怎么算”，本问题则按一次写访问的完整路径解释“延迟由哪些阶段构成、怎样分析”。

## 7、热模型坐标的相关说明（计算方式、可视化图中热力图的坐标计算、比如热力图是4x16的矩阵，为什么这样分类）

### 7.1 地址到 Layer

先得到 decoded address：Channel、PC、SID、Rank、BG、Bank、Row、Column。Bank 平铺编号为：

```text
bank_index = bank_group × banks_per_group + bank
```

每个 SID 分配若干 layer，最终 layer 由 SID 和 bank_index 映射并限制在 stack height 内，见 [`src/core/data.cpp:831`](src/core/data.cpp:831)。这是项目的确定性研究映射，不是 JEDEC 规定的厂家 die 版图。

### 7.2 地址到 Floorplan tile

每个地址域的 Bank 数：

```text
banks_per_layer = bank_groups × banks_per_group
floorplan_cols = ceil(sqrt(banks_per_layer))
floorplan_rows_per_slice = ceil(banks_per_layer / floorplan_cols)
```

PC/SID/Rank 组合形成纵向 slice：

```text
slice = ((rank × sid_count + sid) × pseudo_channels) + pseudo_channel
tile_x = bank_index % floorplan_cols
tile_y = slice × floorplan_rows_per_slice
         + bank_index / floorplan_cols
```

实现见 [`src/core/data.cpp:852`](src/core/data.cpp:852)。

### 7.3 Tile 内的 local grid

配置：

```text
thermal_grid_cols_per_tile = Gx
thermal_grid_rows_per_tile = Gy
```

Row 被缩放到 `0..Gx-1`，Column 被缩放到 `0..Gy-1`：

```text
local_grid_x = floor(row × Gx / rows)
local_grid_y = floor(column × Gy / columns)

thermal_x = tile_x × Gx + local_grid_x
thermal_y = tile_y × Gy + local_grid_y
```

见 [`src/core/data.cpp:886`](src/core/data.cpp:886)。因此 tooltip 中：

- `thermal(x,y)`：整层热网格坐标；
- `tile(x,y)`：Bank/floorplan 粗格；
- `local grid(x,y)`：该 tile 内细分位置。

### 7.4 为什么会是 4×16

Dashboard 不固定写死 4×16。它使用 thermal map 中每个 cell 的 `cols/rows/x/y`，取整层最大维度后建立 CSS grid，见 [`tools/visualize.py:278`](tools/visualize.py:278)。

若某配置得到 4×16，通常是：

```text
thermal_cols = floorplan_cols × Gx = 4
thermal_rows = floorplan_rows × Gy = 16
```

例如 `bank_groups × banks_per_group = 16` 时，`ceil(sqrt(16))=4`，每个 Bank slice 是 4×4；若 PC/SID/Rank 组合形成 4 个纵向 slice、且每 tile 为 1×1 thermal grid，整层就是 4×16。

所以 4×16 来自当前组织参数和映射规则，不是所有 HBM/LPDDR 固定形状，也不是物理 die 已经被证实按 4×16 摆放。

### 7.5 为什么坐标会是 -1

热耦合可能创建一个没有任何命令直接命中的邻居 cell。它只有传入热量，没有唯一 DRAM 地址。代码保留 thermal 坐标，但把 Channel/PC/SID/Rank/BG/Bank/Row/Column 设为 -1，见 [`src/core/data.cpp:1310`](src/core/data.cpp:1310)。

这表示 `coupling-only / address unknown`，不是负地址，也不是解析错误。

**交付手册对应说明**

- [交付手册 2.6.3](堆叠存储模型交付手册.md#263-物理功耗热和-ecc)：说明物理坐标和稀疏 thermal-grid 的模块定位。
- [交付手册 7.3.5“页面操作方法”](堆叠存储模型交付手册.md#735-页面操作方法)：明确 Dashboard 的 thermal x/y 是热网格坐标，不是 tile 坐标。
- [交付手册 B.9.6“热结果和功耗的联合解读”](堆叠存储模型交付手册.md#b96-热结果和功耗的联合解读)：给出 `thermal_x/y` 公式、Channel 汇聚边界、coupling-only 节点和 -1 地址含义。

本回答进一步用当前组织参数推导了 4×16 的来源，说明它是 `floorplan × tile 内 grid` 的结果而非固定版图。

## 8、LPDDR5 6的command命令解释（RD、WR、ACT1、ACT2、PREpb、PEFpb、CAS_RD、CAS_WR）

### 8.1 先纠正命令名

问题文件中的 `PEFpb` 应当是 `REFpb`。项目中没有 `PEFpb` 命令。另需注意：LPDDR6 标准的非 all-bank 刷新是 `REFdb`（dual-bank），不是单 Bank REFpb；项目公共枚举保留 REFpb 主要用于其他标准/共同抽象。

命令枚举见 [`include/hbm_sim/core/common.hpp:53`](include/hbm_sim/core/common.hpp:53)，语义表见 [`src/dram/semantics.cpp:11`](src/dram/semantics.cpp:11)。

### 8.2 RD

读命令。要求目标 Bank 已打开且 open row 等于请求 Row；随后按读延迟返回 payload。在 LPDDR split-CAS 路径中，CAS_RD 可以是前置命令，最终 RD 才是终端数据命令。

状态条件见 [`src/dram/state.cpp:39`](src/dram/state.cpp:39)，完成时间在 [`src/controller/controller.cpp:1684`](src/controller/controller.cpp:1684)。

### 8.3 WR

写命令。同样要求 Bank 已打开且 row hit；数据经 Direct 或 Behavioral PHY 写入 MemoryImage。WR 后 Row 保持打开，便于后续命中。

状态条件见 [`src/dram/state.cpp:39`](src/dram/state.cpp:39)，PHY 写入见 [`src/dram/mem_phy.cpp:247`](src/dram/mem_phy.cpp:247)。

### 8.4 ACT1 和 ACT2

LPDDR6 可将激活地址分两个命令传输：

- ACT1：发送第一部分地址，Bank 进入 Activating/等待配对状态；
- ACT2：发送剩余地址，完成 Row 打开。

ACT1 单独完成不代表 Row 已打开；ACT2 必须与拥有该激活过程的请求匹配，并满足间隔/deadline。状态规则见 [`src/dram/state.cpp:50`](src/dram/state.cpp:50)，执行更新见 [`src/controller/executor.cpp:106`](src/controller/executor.cpp:106)。

### 8.5 PREpb

关闭指定 Bank 当前打开的 Row，使 Bank 回到 Closed/预充状态。它用于 row conflict、行策略或刷新前清理。`pb` 表示 per-bank scope，不是写回缓存。

执行逻辑见 [`src/controller/executor.cpp:151`](src/controller/executor.cpp:151)，存储 row buffer 关闭见 [`src/controller/controller.cpp:991`](src/controller/controller.cpp:991)。

### 8.6 REFpb / REFdb / REFab

- REFpb：单 Bank 刷新抽象；
- REFdb：LPDDR6 一次刷新配对的两个 Bank；
- REFab：目标 Rank/PC/SID scope 的 all-bank refresh。

刷新前目标必须满足关闭/空闲要求，刷新后相关 Bank 在 nRFCpb/nRFC 期间不可重新 ACT。LPDDR6 REFdb 配对和 Timing 见 [`src/controller/timing.cpp:86`](src/controller/timing.cpp:86) 与 [`src/dram/spec.cpp:339`](src/dram/spec.cpp:339)。

### 8.7 CAS_RD 和 CAS_WR

它们是 LPDDR 命令序列中列访问/CAS 阶段的读写方向命令，用于建立随后数据命令所需的状态。项目中它们不是最终完成请求的数据命令：发出后请求仍留在 active buffer，等待后续 RD/WR。

非终端命令不 retire 请求的逻辑见 [`src/controller/controller.cpp:1655`](src/controller/controller.cpp:1655)，CAS 状态更新见 [`src/controller/executor.cpp:225`](src/controller/executor.cpp:225)。

### 8.8 LPDDR5 与 LPDDR6 不应混为一张命令表

两者可共用 RD、WR、PRE、refresh 等概念，但 LPDDR6 的 split activate、dual-bank refresh、WCK/CA 行为和具体 Timing 不应直接套用到 LPDDR5。项目依靠 `DramSpec` 的 `lpddr_family`、`split_activate`、`lpddr_dual_bank_refresh` 等能力开关选择路径，而不是看到 LPDDR 名称就启用全部命令。

**交付手册对应说明**

- [交付手册 2.4.3“Bank 状态与命令语义”](堆叠存储模型交付手册.md#243-bank-状态与命令语义)：说明公共命令与 HBM/LPDDR 专用语义的分工。
- [交付手册第 4 章“四种标准模型”](堆叠存储模型交付手册.md#4-四种标准模型)：区分 LPDDR5 与 LPDDR6 能力范围。
- [交付手册 C.3“LPDDR5”](堆叠存储模型交付手册.md#c3-lpddr5) 与 [C.4“LPDDR6”](堆叠存储模型交付手册.md#c4-lpddr6)：提供两种标准的实际运行和命令观察入口。

本回答对应问题中的每个命令逐一解释，并明确把 `PEFpb` 更正为 `REFpb`；对于 LPDDR6，标准非 all-bank 刷新应进一步区分为 `REFdb`。

## 9、DRAMsim3验证方式（对话记录中的问题）

### 9.1 这不是完整 HBM3/HBM4 差分

当前工具 [`tools/dramsim3_aux_validation.py`](tools/dramsim3_aux_validation.py) 的定位是 **DRAMsim3 辅助对照**。它使用 DRAMsim3 的较老 HBM2/HBM 配置，对比双方共同面：组织换算、简单命令序列、部分 Timing、读延迟、IDD 命令能量和可选热运行。

它不能证明 HBM3/HBM4/LPDDR5/LPDDR6 全部协议正确，也不能作为厂家校准。

### 9.2 实际运行了哪些程序

脚本先解析：

- 本项目 binary；
- `--dramsim3-root` 指定的外部 DRAMsim3 仓库；
- `build-validation/dramsim3main`；
- DRAMsim3 `HBM2_4Gb_x128.ini`；
- 本项目共同面 validation config。

检查与路径处理见 [`tools/dramsim3_aux_validation.py:55`](tools/dramsim3_aux_validation.py:55)。因此只运行 hbm_sim、换一个叫 DRAMsim3 的配置，不算此验证。

### 9.3 为什么需要 CMD_TRACE

命令序列比较需要 DRAMsim3 生成 `dramsim3ch_0cmd.trace`。该文件只有构建时启用 `CMD_TRACE=ON` 才生成。脚本在文件缺失时会给出重建命令，见 [`tools/dramsim3_aux_validation.py:128`](tools/dramsim3_aux_validation.py:128)。

该 trace 默认位于脚本临时目录中的 `dramsim_command/`。未提供 `--artifact-dir` 时，临时目录在结束后删除；要保留证据必须指定空的 artifact 目录，参数定义见 [`tools/dramsim3_aux_validation.py:31`](tools/dramsim3_aux_validation.py:31)。

### 9.4 具体检查项

1. **组织换算**：将 DRAMsim3 columns、BL、bus width 换算成本项目 transaction slots/bytes/capacity，而不是直接比较原始数字，见 [`tools/dramsim3_aux_validation.py:169`](tools/dramsim3_aux_validation.py:169)。
2. **命令序列**：单读场景比较本项目 `ACT,RD` 与 DRAMsim3 `activate,read`。
3. **tRCDRD 间隔**：比较 ACT→RD gap，见 [`tools/dramsim3_aux_validation.py:194`](tools/dramsim3_aux_validation.py:194)。
4. **读延迟**：双方平均读延迟允许 1 cycle 容差。
5. **功耗公式**：双方 ACT/RD/WR/REF 每命令能量与同一个 IDD 公式比较，公式构造见 [`tools/dramsim3_aux_validation.py:88`](tools/dramsim3_aux_validation.py:88)。
6. **写命令**：DRAMsim3 单写可能只在 write buffer 内确认，所以工具注入足够多写请求越过高水位，确保物理 WRITE 可见。
7. **刷新**：让 DRAMsim3 跨过 tREFI，比较 all-bank refresh 每命令能量。
8. **热**：未指定 `--skip-thermal-runtime` 时运行单独 thermal build，进行辅助趋势/结果检查。

### 9.5 推荐命令

```bash
python3 tools/dramsim3_aux_validation.py \
  --binary ./build-clang-debug/hbm_sim \
  --dramsim3-root /home/guxing/learn/DRAMsim3 \
  --artifact-dir outputs/validation/dramsim3_aux_full/artifacts \
  --json-out outputs/validation/dramsim3_aux_full/report.json
```

`artifact-dir` 必须为空。若暂不运行 DRAMsim3 热求解，可加 `--skip-thermal-runtime`，但报告只能声明命令/Timing/IDD 共同面，不能声明完成 thermal runtime 对照。

### 9.6 怎样判断通过

不能只看脚本退出码。还应打开 JSON 报告，确认：

- `passed=true`；
- 每个 check 都为 pass；
- 使用的外部 binary/config/commit 正确；
- artifact 中同时有双方输入、原始 stats 和 command traces；
- 没有把临时文件路径当作长期证据。

**交付手册对应说明**

- [交付手册 8.5.1“DRAMsim3 辅助对照”](堆叠存储模型交付手册.md#851-dramsim3-辅助对照)：给出构建目录、CMake policy、`CMD_TRACE=ON`、快速模式、热模式及结论边界。
- [交付手册 8.2“可信度证据链”](堆叠存储模型交付手册.md#82-可信度证据链)：将 DRAMsim3 定位为较老共同协议和 IDD/热趋势的第二来源，而非 HBM4/LPDDR6 全协议验证。

本回答根据实际脚本进一步拆解了每个 check、临时 trace 的来源、`--artifact-dir` 的保留语义和通过判据。

## 10、性能曲线指标的解释（横纵坐标、各个指标的含义解释）

### 10.1 横轴：offered requests / tick

工具对每个 `inject_interval` 运行一次：

```text
offered_requests_per_tick = 1 / inject_interval_ticks
```

见 [`tools/performance_curve.py:76`](tools/performance_curve.py:76)。例如 interval=32、4、1 分别是 0.03125、0.25、1 request/tick。

这里的 request 是 Frontend/Host request，不是 DRAM command，也不一定等于 DRAM transaction。若 Host line 64 B、transaction 32 B，一次 Host request 可产生两个 transaction。

它表示 offered load，不是实际接受率或完成率。队列满时会出现 injection stall，因此高 offered load 不保证系统每 tick 真接收同样数量。

### 10.2 左纵轴：payload throughput GB/s

实线使用左轴，来自 `achieved_bw_GBps`：

```text
achieved_bw_GBps
  = (read_bytes + write_bytes)
    / simulation_seconds / 1e9
```

系统用 `system_cycles` 而不是所有 Controller 周期之和计算时间，见 [`src/core/system.cpp:1114`](src/core/system.cpp:1114)。它统计完成 payload，不是命令带宽，也不默认把 ECC/metadata 都算作 payload。

### 10.3 右纵轴：read/write transaction latency ticks

虚线使用右轴。Dashboard 可选择 read 或 write：

- `avg_read_latency_ticks`；
- `avg_write_latency_ticks`。

它们是 Controller transaction 的 arrival→completion 平均值，不是 Host 端到端延迟，也不是 ns。换算 ns 要乘 `tick_duration_ps/1000`。

双 Y 轴不能混读：虚线高度要看右轴，实线要看左轴。绘图实现见 [`tools/visualize.py:259`](tools/visualize.py:259)。

### 10.4 每个点还包含哪些指标

脚本记录：

- `standard`：协议标准；
- `pattern`：random/stream；
- `read_ratio_pct`：Host request 读比例；
- `inject_interval_ticks`：注入间隔；
- `requests`：请求总数；
- `system_cycles`：本点排空用时；
- `completed_reads/writes`：完成 transaction 数；
- `avg_read/write_latency_ticks`；
- `achieved_bw_GBps`；
- `peak_bandwidth_GBps`；
- `bandwidth_util_pct`；
- `row_hit_pct`。

字段生成见 [`tools/performance_curve.py:72`](tools/performance_curve.py:72)。

### 10.5 曲线怎样分析

1. **低负载区**：吞吐应随 offered load 近似增加，延迟接近基础服务时间。
2. **拐点**：队列开始持续积压，延迟明显上升，吞吐增幅下降。
3. **饱和区**：吞吐接近平台；继续提高 offered load 主要增加延迟。
4. **下降或异常区**：若吞吐随负载下降，要检查 refresh、写排空、热点、反压、cycle limit 和统计完成性。

性能好不能只看最高 GB/s。实际工作点若在拐点前，更应比较延迟；若追求饱和吞吐，则要同时确认利用率、Channel 分布和 row behavior。

### 10.6 自动检查的含义

脚本当前做三类趋势检查：

- 高负载吞吐不低于低负载；
- achieved bandwidth 不超过 peak 的 1.001 倍；
- 有读请求时，高负载读延迟不低于低负载。

见 [`tools/performance_curve.py:91`](tools/performance_curve.py:91)。这些检查只能验证曲线方向和内部峰值边界，不能证明厂家真实性；脚本的 `claim_boundary` 也明确说明不是 vendor/hardware calibration。

### 10.7 比较曲线时必须固定什么

至少固定：标准和组织、数据率、Stack/Channel 数、transaction bytes、请求数、pattern、seed、地址空间、read ratio、Controller/PHY 模式、refresh/power/thermal 开关。

若这些条件不同，曲线变化可能来自 workload 或模型规模，而不是被研究的那个参数。对 random 流量尤其要固定 seed，并保留每个点的 resolved config 和 stats。

**交付手册对应说明**

- [交付手册 7.3.3“生成性能曲线”](堆叠存储模型交付手册.md#733-可选生成性能曲线)：给出 sweep 命令和标准基线限制。
- [交付手册 7.3.5“页面操作方法”](堆叠存储模型交付手册.md#735-页面操作方法)：明确实线/虚线使用不同纵轴。
- [交付手册 8.6“性能曲线和不确定性”](堆叠存储模型交付手册.md#86-性能曲线和不确定性)：给出性能与敏感性工具入口。
- [交付手册 B.9.3～B.9.4](堆叠存储模型交付手册.md#b93-区分速率理论带宽和实际带宽)：解释峰值、实际带宽、利用率以及吞吐—延迟联合分析。

本回答补充了 `performance_curve.py` 每个输出字段的代码来源、自动趋势检查以及 offered load 不等于实际接收率的原因。
