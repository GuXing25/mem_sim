# 堆叠存储模型交付手册

> 项目：`hbm_sim`  
> 支持标准：HBM3、HBM4、LPDDR5、LPDDR6  
> 实现语言：C++20  
> 模型粒度：周期级、命令级、事务数据级、行为级 PHY  
> 文档定位：本文件是交付总入口；代码、配置和测试结果是最终事实来源。

## 0. 文档用途与交付边界

本项目由“仿真平台”和“堆叠存储模型”两部分组成。平台负责流量、配置、调度、
统计、验证和输出；堆叠存储模型由可选的行为级 Mem PHY 与 Mem Stack 组成，负责
协议适配、命令/数据流水、真实 payload、物理位置、功耗、热和 ECC 等语义。

本交付包含：

- HBM3、HBM4、LPDDR5、LPDDR6 的统一配置和运行入口；
- 多 stack、多 channel、pseudo-channel、SID、rank、bank group、bank 的组织和路由；
- Controller、TimingEngine、刷新/RFM/PRAC、低功耗和标准专用协议行为；
- Direct/Behavioral 两种 Mem PHY 模式及 HBM/LPDDR 协议适配器；
- `sparse`、`mmap_sparse`、`chunk_file` 三种存储后端；
- 实际数据读写、掩码、期望值检查、golden image 和 SECDED shadow；
- command、response、DFI、memory image、mismatch、thermal 等可重放产物；
- 原生回归、Timing 边界、性能曲线、敏感性分析和外部参考差分工具。

本项目不是晶体管级、信号完整性级或量产器件模型。Behavioral PHY 不建模阻抗、
电压摆幅、抖动、眼图、训练算法的模拟电气收敛，不宣称通过 JEDEC/DFI 合规认证。
HBM3 与 LPDDR5 的部分默认参数来自 Ramulator2.1 可复核基线；HBM4 与 LPDDR6 采用
JEDEC-oriented 组织/协议和公开研究默认值。厂商 RL/WL、速度档、行时序、RFM/RAS、
PHY/DFI、功耗、热阻热容等参数仍须用厂家模型或硬件结果校准。

### 0.1 本手册的自包含范围与阅读导航

本手册可独立代替项目指南、仿真使用说明、内存模型与平台说明、多 Stack/存储后端、
架构构建、验证审计等专题说明。

| 问题 | 手册入口 |
|---|---|
| 项目组成、平台和模型划分 | [总体设计](#1-总体设计)、[平台与模型划分](#3-平台与模型划分) |
| Controller、PHY、Mem Stack 协作 | [模块详细设计](#2-模块详细设计) |
| HBM3/HBM4/LPDDR5/LPDDR6 区别 | [四种标准模型](#4-四种标准模型) |
| 安装、用 Clang/G++ 构建和测试 | [构建与部署](#5-构建与部署) |
| 运行、换 PHY、换后端、做多 Stack | [仿真模型使用说明](#6-仿真模型使用说明) |
| 保存和解释输出 | [仿真输出与查看方法](#7-仿真输出与查看方法)、[输出指标附录](#附录-b输出全指标说明) |
| 生成、操作和分析 dashboard | [任意仿真的离线可视化](#73-任意仿真的离线可视化) |
| 当前交付程度、可信度 | [平台与模型验证](#8-平台与模型验证)、[审计与完成情况](#9-审计与完成情况) |
| 参数配置 | [配置参数附录](#附录-a配置参数说明) |
| 四标准完整案例 | [四标准交付用例](#附录-c四标准交付用例) |
| 假定一款新 HBM4 并完成实验 | [新 HBM4 自定义案例](#附录-d新-hbm4-自定义用例) |

若本文与实现不一致，以 `src/`、`include/`、`configs/`、`CMakeLists.txt` 和当前测试结果
为最终事实来源；这条优先级用于防止文档滞后，不表示使用者还需要依赖其他说明文件。

---

## 1. 总体设计

### 1.1 设计目标

1. 在同一套平台内比较 HBM3/HBM4/LPDDR5/LPDDR6 的组织、时序与协议特性。
2. 支持从 host 请求到 DRAM transaction、命令、PHY 事件和真实数据落盘的完整路径。
3. 支持一颗或多颗独立 stack，统计系统级与 per-stack 性能、功耗和温度。
4. 允许替换调度器、地址映射、PHY 模式、存储后端和 timing/profile，而不重写主流程。
5. 结果可以由 CSV、memory image、验证报告和固定随机种子复核。
6. 在缺少厂家手册时，明确区分标准约束、外部参考、派生值和研究默认值。

### 1.2 设计原则

- **模块化**：Frontend、MemorySystem、Controller、PHY、MemoryImage、Stats 分层。
- **配置与模型分离**：标准 traits 定义能力，profile 给出参数，配置选择实验。
- **统一多标准接口**：公共请求和统计保持一致，标准差异通过 traits/semantics/adapter 表达。
- **多 stack 独立性**：每个 `[stack][channel]` 拥有独立 Controller 和 PHY 端点；每个
  stack 拥有独立 MemoryImage，数据、队列、温度和统计不串扰。
- **时序与数据并重**：Controller/TimingEngine 管命令合法性，MemoryImage 管真实 payload。
- **PHY 可选**：Direct 用于兼容和对照；Behavioral 用于在线 FIFO、流水和协议行为。
- **可扩展后端**：存储介质实现不改变 DRAM 时序和数据正确性语义。
- **可追溯**：命令、DFI、响应、参数来源和校验结果可以导出。
- **研究建模**：研究默认值而非 JEDEC 精确值，行为级事件而非真实引脚波形。

### 1.3 总体分层

```text
配置/Profile/CLI
       │
       ▼
Frontend / TrafficStream / Trace parser
       │ HostRequest（可拆为多个 DRAM transaction）
       ▼
MemorySystem
  ├─ stack 选择、system→local address、ingress、QoS
  └─ [stack][channel] Controller
         ├─ read/write/priority/active/pending queues
         ├─ scheduler + row policy
         ├─ bank state + command semantics + TimingEngine
         ├─ refresh/RFM/low-power
         └─ MemPhy
              ├─ Direct：兼容旁路
              └─ Behavioral：状态机、FIFO、pipeline、HBM/LPDDR adapter
                     │
                     ▼
              per-stack MemoryImage / Mem Stack
                     ├─ MemoryBackend
                     ├─ row buffer / payload / init mask
                     ├─ floorplan / topology
                     ├─ power / thermal
                     └─ ECC shadow
       │
       ▼
HostResponse + Stats + command/DFI/response/memory/thermal artifacts
```

控制器按 channel 建立，PHY 也按 channel 接口切片建立。因此“一个 stack 一个 PHY”在
本项目中应理解为“一颗 stack 拥有一组 channel-local PHY 端点”，而不是多个 stack
共享一个全局 FIFO/状态机。HBM4 六 stack、每 stack 32 channel 时会建立 192 个
Controller 和 192 个 PHY 端点，但只有 6 个独立的 per-stack MemoryImage。

### 1.4 主动模型与被动模型

| 模式 | 入口 | 包含调度/时序 | 包含数据/物理/热 | 适用场景 |
|---|---|---:|---:|---|
| 主动完整路径 | `MemorySystem` | 是 | 是 | CLI、性能、协议和完整系统实验 |
| 单控制器兼容 | `--single-controller` | 是 | 是 | 旧结果复现、局部控制器实验 |
| 被动单 stack | `StackModel` | 否 | 是 | 外部 MC 已产生合法 DRAM 操作 |
| 被动多 stack | `MultiStackMemoryModel` | 否 | 是 | SystemC/RTL/UCIe 外部驱动存储后端 |

若后期只需要“Mem PHY + Mem Stack”，可从 `MemPhy`、`MemoryImage`、后端和相关 traits
中提取；外部系统必须补上请求仲裁、命令合法性与时序责任，不能简单删除 Controller。

---

## 2. 模块详细设计

### 2.1 配置模块

#### 2.1.1 标准定义与能力声明

`StandardTraits`/标准负责标准身份、默认 profile、组织和协议能力，例如 HBM 的
SID/伪通道/行列双总线，LPDDR 的 WCK、分裂 ACT、DVFS 和链路保护入口。标准名字是
选择模型族的入口，不等于已经选择某家器件。

#### 2.1.2 Profile

Profile 把速度档、密度、堆叠高度、组织和 timing 绑定成可复现实验基线：

- `configs/profiles/`：可复用 timing/mode 片段；
- `configs/calib/`：标准依据、派生值、研究默认值和厂商校准入口；
- `configs/run/`：可直接运行的 workload；
- `configs/validation/`：验证场景和证据清单。

最终覆盖顺序为：

```text
标准内置默认
  < 内置 profile
  < timing_profile_file
  < key=value 配置文件
  < 命令行参数
```

同一字段后层覆盖前层。`timing_source_*`、`timing_override_source`、
`vendor_profile` 用于标注数据来源；`--strict-timing-table` 可在仍存在必须校准的默认值时
拒绝运行。

#### 2.1.3 DramSpec 与 finalize

配置首先形成可覆盖的 draft，`finalize_spec()` 再完成派生和一致性检查，包括容量、
transaction 大小、tCK/ns→nCK 换算、约束表、刷新分支、DFI 派生值和协议开关。
代码不得依赖“配置文件中字段出现的先后顺序”推导模型语义。

### 2.2 Frontend模块

Frontend 支持：

- `stream`、`random` 合成流量；
- 普通 trace：`R/W ADDRESS`；
- 带 payload 的 `data=HEX`、`expect=HEX`、`mask=HEX`；
- burst：`BW/BR ADDRESS len=... pattern/check=...`；
- 维护请求：`M REFpb|REFab|RFMpb|RFMab|REFdb ADDRESS`；
- `stack=N` 显式指定 stack-local 地址，`qos=N` 指定优先级；
- 初始化/训练/低功耗控制序列。

一个 host line 可按 `dram_transaction_bytes` 拆成多个 DRAM transaction。因此：

- `host_requests` 是上层请求数；
- `dram_transactions` 是控制器实际接收的拆分事务数；
- `reads/writes` 与完成量应按 transaction 口径解释；
- burst 的多个子事务由 `host_request_id` 聚合为一个 HostResponse。

大量请求通过 `TrafficStream` 按周期生成，不预先构造完整请求数组，避免输入规模直接
转化为宿主机内存峰值。

### 2.3 MemorySystem 与多 Stack 路由

MemorySystem 负责：

1. 通过 `interleaved` 或 `blocked` 选择目标 stack；
2. 将 `system_address` 转换为 stack-local `address`；
3. 请求进入每 stack ingress；
4. 按 `fcfs` 或 `strict_priority` QoS、`stack_dispatch_width` 分发；
5. 再按 channel mapper 选择 `[stack][channel]` Controller；
6. 完成子事务后重组 HostResponse。

`interleaved` 以 `stack_interleave_bytes` 为条带，适合均衡带宽；`blocked` 将连续容量
分给不同 stack，适合显式数据放置。trace 中 `stack=N` 表示输入地址已经是该 stack
的局部地址，绕过系统地址的 stack 选择，但仍执行该 stack 内的 channel 解码。

多 stack 不是一个 Controller 复制统计标签：每个 stack 的请求入口、MemoryImage、
温度、功耗和完成统计均独立；系统总带宽与容量再做聚合。

### 2.4 Memory Controller

#### 2.4.1 队列

- read buffer：普通读；
- write buffer：普通写；
- priority buffer：刷新、RFM、初始化和控制命令；
- active：已进入当前服务过程的事务；
- pending：已发出、等待数据/PHY/存储完成的事务。

写高/低水位控制读写模式切换；buffer 深度影响 frontend 反压和排队延迟。

#### 2.4.2 调度器与行策略

- `fcfs`：按到达先后选择可发命令；
- `frfcfs`：优先 ready/row-hit，再考虑年龄；
- `open_page`：保留已打开行；
- `closed_page`：列访问后自动关闭；
- `closed_cap`：同一行达到 `row_policy_cap` 后关闭。

调度器只能在 TimingEngine、bank 状态和 PHY 接收条件均满足时发命令。统计中的
row hit/miss/conflict 描述调度时的行缓冲关系，不等于应用级 cache 命中率。

#### 2.4.3 Bank 状态与命令语义

公共命令包括 ACT、PRE、RD、WR、RDA、WRA、REF、RFM 和模式/低功耗命令。
HBM/LPDDR 专用语义由 `dram/semantics` 与 adapter 处理。Bank 状态至少区分关闭、
激活行和刷新/维护占用；非法的列命令、重复激活或过早预充不能绕过状态检查。

#### 2.4.4 TimingEngine

TimingEngine 使用表驱动约束，按 channel、rank、bank group、bank 等作用域记录最近命令，
表达 `tRCD`、`tRP`、`tRAS`、`tRC`、`tCCD`、`tRRD`、`tFAW`、`tWTR`、`tRTW`、
刷新窗口及标准专用间隔。核心 timing 应采用边界测试验证：第 `t-1` 拍禁止，第 `t` 拍
允许，而不是只看一条长 trace 是否完成。

#### 2.4.5 Refresh、RFM/PRAC 与低功耗

刷新可选择 per-bank 或 all-bank，并提供 postpone、pull-in、credit 限制。RFM 可按
ACT 阈值触发 per-bank/all-bank 维护。它们是控制器策略模型，不应当被解读为特定厂商
芯片内部 TRR 的完全复现。

低功耗支持 power-down/self-refresh 的入口、退出和延迟。显式发送 PDE/SREFEN 时，
Behavioral PHY 需要对应 PDX/SREFEX 才能返回 Ready；自定义控制序列必须保持成对，
否则后续普通命令会被 PHY 合法反压。

### 2.5 Mem PHY

#### 2.5.1 功能边界

PHY 位于 Controller 与 Mem Stack 之间，将控制器级命令和数据事务转换为带时序、
通道和协议语义的行为事件：

- 接收命令、写数据和读返回；
- 建模 command/write/read FIFO 容量与流水延迟；
- 建模 Reset→Init→Training→Ready→LowPower 生命周期；
- 生成 DFI-oriented command/data beat 和 signal-like CSV；
- 调用 HBM/LPDDR adapter 完成标准专用的命令展开和校验；
- 在完成时访问 per-stack MemoryImage，并把读 payload 返回 Controller。

它不产生真实的 CK/WCK/DQ/DQS 电压波形，也不计算 setup/hold、串扰、抖动、BER、
眼图或封装信号完整性。DFI 版本字段是目标口径标签，事物级建模不是完整 DFI 端口合规实现。

#### 2.5.2 Direct 与 Behavioral

| 模式 | 含义 | 适用 |
|---|---|---|
| `direct` | 兼容旁路；维持旧路径的直接完成时序 | 回归对照、隔离 PHY 影响 |
| `behavioral` | 生命周期、FIFO、pipeline、adapter、DFI event | 正式 full-stack 仿真 |

Direct 不等于零延迟真实 PHY，Behavioral 也不等于 pin-level PHY。比较两模式时要同时
检查命令发射、自动预充完成点和 PHY pipeline 的统计口径。

#### 2.5.3 HBM 与 LPDDR 适配

HBM adapter 处理 pseudo-channel/SID、行列命令总线、HBM 特定 metadata/CRC/retry
入口；LPDDR adapter 处理 ACT1/ACT2、WCK sync/train、DVFS、DBI/link ECC/CA parity
入口。公共 Controller 不通过大量标准名分支硬编码这些差异。

PHY 反压指标按“本拍目标总线因 PHY 无法接收而未发出”计周期，一个 tick、一条总线
至多增加一次；它不是候选调度项被拒绝的累计次数。

### 2.6 Mem Stack 与存储后端

#### 2.6.1 MemoryImage

MemoryImage 是每颗 stack 的真实数据状态，管理：

- payload 与逐字节初始化 mask；
- 写掩码、版本号、最后写者；
- 行缓冲打开/关闭和当前行数据；
- system/local 地址对应的 DRAM 组织和物理坐标；
- ECC shadow、命令能量和稀疏热节点；
- memory dump、golden compare 和 mismatch report。

未初始化读取应通过统计/报告暴露；写后读和 `expect=` 是验证数据链闭环的主要方法。

#### 2.6.2 三种后端

| 后端 | 数据保存 | 优点 | 主要限制 |
|---|---|---|---|
| `sparse` | 宿主内存稀疏表 | 快、无文件管理 | 大覆盖集占 RAM，进程退出后消失 |
| `mmap_sparse` | 稀疏映射文件及 sidecar | 可持久化、随机访问 | 需要显式容量和兼容文件头 |
| `chunk_file` | 文件 + 有界 chunk cache | 满容量长跑、受控 RAM | cache miss 和写回有宿主 I/O 成本 |

更换后端只改变宿主机保存方式，不改变 Controller/TimingEngine/PHY、DRAM 地址映射、
row buffer、ECC、功耗和热的模型语义。公平比较必须固定配置、trace、初始 image、随机
种子，并为文件后端使用全新的文件集合。

#### 2.6.3 物理、功耗、热和 ECC

模型从 DRAM 地址派生 stack/die/layer/tile/grid/subarray/mat/cell/microbump 坐标，
只为已访问区域分配稀疏状态，不预分配完整芯片阵列。功耗支持配置的命令 pJ 或
DRAMsim3-oriented IDD/VDD 公式；热模型是稀疏 3D RC 趋势模型，可配置横向、纵向和
TSV 耦合。二者适合相对趋势和热点定位，绝对瓦数/温度需要器件、封装和散热参数校准。

ECC shadow 提供 payload line 的 SECDED 行为、单比特纠正和周期性注错入口，但不替代
真实链路 ECC、on-die ECC 和系统级 RAS 的全部编码、时延和覆盖范围。

### 2.7 异步请求—响应接口

异步接口是上层 CPU/NoC/UCIe/SystemC/RTL 仿真器与 `MemorySystem` 之间的协议中立边界。
它把“请求何时被接收”“系统推进一拍”“子事务何时完成”“host line 何时重组完成”分开，
从而表达真实反压，而不是一次函数调用同步返回数据。它不定义 UCIe flit、AXI channel、
NoC credit 或 CDC；适配器需要把这些协议映射到本接口的 ready/valid 语义。

#### 2.7.1 请求层级和拆分

一个上层 `HostRequest` 可以拆成多个 `Request`/DRAM transaction。例如 64B host line 在
32B `dram_transaction_bytes` 下拆成两个子事务：

```text
host_request_id = 100, transaction_count = 2
  transaction 0: id=1000, transaction_index=0, address=0x1000, transfer_bytes=32
  transaction 1: id=1001, transaction_index=1, address=0x1020, transfer_bytes=32
```

关键字段：

| Request 字段 | 含义和约束 |
|---|---|
| `id` | 子事务追踪 ID，不参与优先级 |
| `host_request_id` | host 请求聚合标签；在响应真正 pop 前不能复用 |
| `transaction_index/count` | 子事务位置和总数；首个提交必须为 index 0，index 必须小于 count |
| `address/system_address` | 路由前全局地址和路由后 stack-local 地址 |
| `inject_cycle/arrival` | 上层希望发送的拍与系统实际接收拍，用于反压统计 |
| `target_stack/qos_class` | 显式 stack 和 stack ingress QoS；高数字优先级更高 |
| `payload/byte_mask` | 写数据和逐 byte 写掩码 |
| `expected_payload` | 读数据期望值，用于独立正确性检查 |

同一 host 的所有 transaction 必须保持 type/count 一致，每个 index 只能成功提交一次。违反
这些约束会抛出 `invalid_argument`，不会静默覆盖旧请求。

#### 2.7.2 请求 ready/valid 与入口反压

```cpp
bool accepted = memory_system.try_submit(request);
```

- 返回 `true`：等价于本拍 `valid && ready`，Request 已复制进目标 stack ingress，调用方
  可以前进到下一个 transaction；
- 返回 `false`：目标 stack ingress 已满，调用方必须保留完全相同的 Request，下一拍重试；
- 每个 system cycle 对同一阻塞只累计一次 frontend stall，不按重复候选调用次数累加；
- `stack_ingress_buffer_size` 决定入口容量，`stack_dispatch_width` 决定每拍向 channel
  Controller 分发的最大数量；目标 channel 反压时该 stack 保留请求，其他 stack 仍可推进。

Maintenance 不能调用 `try_submit()`，必须调用：

```cpp
bool accepted = memory_system.try_submit_maintenance(maintenance_request);
```

它同样受到 ingress 反压，但刷新、RFM、初始化等维护请求按定义不产生 HostResponse。

#### 2.7.3 TransactionResponse 与 HostResponse

Controller 每完成一个 DRAM transaction 产生 `TransactionResponse`，其中包含 request/host
ID、index/count、system/local address、stack/channel、arrival/issued/completion cycle、
读 payload、初始化 mask、ECC、forward/coalesce 和 status。

MemorySystem 按 `host_request_id + transaction_index` 重组 `HostResponse`：

- `transactions` 始终按 index 排序；
- 读数据和初始化 mask 按 index 拼接；
- arrival/first-issued 取最早值，completion 取最晚值；
- 子事务跨 stack/channel 时，host 级 stack/channel 记为 `-1`；
- 状态按严重度合并：`ok < ecc_corrected < uninitialized_data < data_mismatch < retry < timeout
  < ecc_uncorrectable < invalid_transaction`；
- 写响应只表示模型定义的写完成点，不返回写 payload。

#### 2.7.4 响应保留模式

`ResponseDeliveryMode` 决定对外保留哪种视图：

| 模式 | HostResponse | TransactionResponse | 用途 |
|---|---:|---:|---|
| `Disabled` | 否 | 否 | 默认批处理性能实验；完成对象及时丢弃，只留统计/trace |
| `HostOnly` | 是 | 否 | CPU/NoC/UCIe frontend 的默认异步模式 |
| `TransactionOnly` | 否 | 是 | 上层自行重组或调试乱序 transaction |
| `Both` | 是 | 是 | 同时审计子事务和 host 重组，内存开销最高 |

第一次成功 `try_submit()` 会把 Disabled 自动切到 HostOnly。也可在系统尚未运行且处于
`quiescent()` 时显式设置：

```cpp
MemorySystemOptions options;
options.response_delivery_mode = ResponseDeliveryMode::Both;
options.transaction_response_queue_capacity = 64;
options.host_response_queue_capacity = 16;
MemorySystem memory(spec, options);
```

容量 0 表示无限制。有限响应队列满时，MemorySystem 不从 Controller pop completion，反压会
真实传播到 Controller/pending；不会丢响应。若 host 长期不消费，吞吐下降或仿真停在响应
反压状态是预期行为。

callback 是非消费型旁路通知：注册 callback 后响应仍留在队列，最终仍必须 `pop_*()`。
`front_*()` 返回的引用只在下一次 pop/队列修改前有效，需要长期保存时应复制。

#### 2.7.5 完整外部事件循环

HostOnly 的最小循环是：

```cpp
MemorySystemOptions options;
options.response_delivery_mode = ResponseDeliveryMode::HostOnly;
options.host_response_queue_capacity = 32;  // 0 可表示无限制
MemorySystem memory_system(spec, options);

std::optional<Request> pending;
bool source_done = false;

while (!source_done || pending.has_value() || !memory_system.quiescent()) {
  if (!pending && !source_done) {
    Request next;
    if (source.next(next)) pending = std::move(next);
    else source_done = true;
  }

  if (pending && pending->inject_cycle <= memory_system.clock()) {
    const bool accepted = pending->type == RequestType::Maintenance
        ? memory_system.try_submit_maintenance(*pending)
        : memory_system.try_submit(*pending);
    if (accepted) pending.reset();
  }

  memory_system.step();

  while (memory_system.has_response()) {
    HostResponse response = memory_system.pop_response();
    consume(response);
  }
}

memory_system.finish(/* remaining_frontend_requests = */ 0);
```

若选择 TransactionOnly/Both，还要独立 drain：

```cpp
while (memory_system.has_transaction_response()) {
  TransactionResponse response = memory_system.pop_transaction_response();
  inspect_transaction(response);
}
```

`finish()` 生成与批处理 `run()` 相同的聚合统计和 command trace；若因外部 cycle limit 停止，
应把尚未送入的 frontend 数量传给 `finish(remaining)`，否则 `remaining_requests` 会偏小。

#### 2.7.6 idle、responses_drained 与 quiescent

| 查询 | 为 true 的条件 | 适合用途 |
|---|---|---|
| `idle()` | ingress、Controller、PHY、pending 等执行路径已空 | 判断内存执行是否完成 |
| `responses_drained()` | transaction/host 队列、重组表、tag 和 Controller response 均空 | 判断响应是否全部消费 |
| `quiescent()` | `idle() && responses_drained()` | 安全退出、切换 response mode、复用系统状态 |

因此 `idle()==true` 但 `quiescent()==false` 通常表示 HostResponse 已 valid、等待上层 pop。
事件循环退出条件必须使用 `quiescent()` 或同时检查执行与响应，不能只检查 `idle()`。

#### 2.7.7 CLI 中的真实异步路径

普通 CLI 不要求响应时走低开销 `run()` 批处理。增加 `--response-trace` 后，CLI 会真实执行
`try_submit/try_submit_maintenance → step → pop_response` 循环，并在运行中写 CSV：

```bash
mkdir -p outputs/async
./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg --requests 128 \
  --response-trace outputs/async/responses.csv \
  | tee outputs/async/stats.txt

column -s, -t < outputs/async/responses.csv | less -S
rg '^(host_requests|dram_transactions|host_responses_exported|injection_stall_cycles|remaining_requests|hit_cycle_limit)\s*:' \
  outputs/async/stats.txt
```

这里的“真实异步”指 cycle-stepped valid/ready 风格的提交、反压和响应队列，不是启动
一个操作系统异步线程。在当前 HBM4 示例中，128 个 64 B host request 会拆成 256 个
32 B DRAM transaction，但上层最终仍收到 128 个重组后的 `HostResponse`。因此示例输出可
这样解读：

| 字段 | 示例值 | 含义 |
|---|---:|---|
| `host_responses_exported` | 128 | 已从 host 响应队列 pop 并写入 CSV 的上层请求数；不是拆分后 transaction 数 |
| `injection_stall_cycles` | 0 | frontend 提交时没有因 stack ingress/控制器无法接收而重试；不代表延迟为 0，也不代表 PHY 反压为 0 |
| `remaining_requests` | 0 | `finish()` 时 frontend 未提交量、ingress、Controller 队列和 pending 路径已清空 |
| `hit_cycle_limit` | false | 不是因 `max_cycles` 截断；应与 `remaining_requests : 0` 联合判定完成性 |

`responses.csv` 每行是一个 host response；`transaction_count`是它包含的 DRAM transaction 数，
`arrival/first_issued/completion_cycle` 可用于分解等待和服务时间，`status` 区分正常、未初始化数据等
响应，`data_hex` 和 `initialized_mask_hex` 用于上层 payload 审计。长行使用 `less -S`
左右滚动，不要让终端自动折行破坏列对齐。

运行时 `stats.txt` 仍然可能在结束前为空，因为聚合统计在末尾统一输出；CSV 则是边 pop
边写。另一终端可用下式观察响应文件增长：

```bash
watch -n 1 'wc -l outputs/async/responses.csv outputs/async/stats.txt'
```

`--response-trace` 只能用于默认 MemorySystem，不能与 `--single-controller` 同用，因为单
Controller 兼容路径没有 host transaction 聚合层。

#### 2.7.8 对接外部协议时的映射建议

```text
外部 request.valid/ready  → try_submit() 返回值
外部 memory clock/tick    → step()
外部 response.valid       → has_response()
外部 response payload     → front_response()
外部 response.ready       → ready 时调用 pop_response()
外部 tag                  → host_request_id（完成握手前唯一）
外部 burst beat           → transaction_index/count
外部 QoS                  → qos_class（仅 stack ingress）
```

RTL/NoC 适配器还需自行处理跨时钟、credit、flit 编码、取消/复位和超时。本接口保证的是内存
语义、反压、完成重组和状态返回，不自动提供上述传输协议。

### 2.8 统计、输出与验证

所有外部时间应优先使用 `system_cycles`。多 controller 环境中的
`aggregate_ctrl_cycles` 是控制器周期求和，不能拿它直接计算系统壁钟带宽。
`achieved_bw_GBps` 使用 payload 字节；`achieved_if_bw_GBps` 包含配置的 metadata/ECC。

验证分为：在线状态/数据检查、仿真后 command/DFI validator、CTest 原生回归、
外部参考差分和最终厂家/硬件校准。通过 Ramulator/DRAMsim 差分只能说明共同抽象在给定
配置下相符，不能证明某个 HBM4/LPDDR6 器件等价。

---

## 3. 平台与模型划分

| 层 | 主要对象 | 责任 | 可替换项 |
|---|---|---|---|
| 配置层 | traits/profile/config/CLI | 标准、参数、来源、派生和合法性 | profile、override |
| 平台层 | Frontend/MemorySystem/Controller/Stats | 输入、路由、调度、时序、响应、统计 | scheduler、mapping、QoS |
| PHY 层 | MemPhy + adapter | 生命周期、FIFO、流水、协议事件 | direct/behavioral、adapter |
| Stack 层 | MemoryImage/Backend | payload、row buffer、物理、ECC、功耗热 | backend、floorplan、模型参数 |
| 工具层 | Python validators/visualizer | 回归、差分、曲线、离线展示 | 外部参考、输出格式 |

“模型”并非仅指 MemoryImage；在完整交付口径下，堆叠存储模型是 Behavioral Mem PHY +
Mem Stack，而 Controller/Frontend/MemorySystem 是使它可独立运行的仿真平台。

---

## 4. 四种标准模型

| 标准 | 主要组织/协议特性 | 当前配置定位 | 校准重点 |
|---|---|---|---|
| HBM3 | stack、channel、PC、SID、BG/bank | Ramulator2.1-derived 研究基线 | 目标 speed-bin、RL/WL、功耗热 |
| HBM4 | full-stack、32 channel、行列总线、metadata/RAS 入口 | JESD270-4A-oriented + 研究默认 | 厂商 timing、RFM、PHY、封装热 |
| LPDDR5 | channel/subchannel、WCK、低功耗 | Ramulator2.1-derived 对照基线 | 器件组织、WCK/训练、IDD |
| LPDDR6 | x12/BL24、ACT1/2、WCK、DVFS、链路保护入口 | JESD209-6-oriented + 研究默认 | RL/WL、训练、link ECC、功耗 |

四种标准共享请求、调度、状态、数据和输出框架，但不应通过简单改名互相替代。
每次论文/交付结果应同时记录 standard、profile、speed bin、density、stack height、
timing source、commit 和完整命令行。

---

## 5. 构建与部署

### 5.1 依赖

- Linux 或具备 POSIX 兼容层的环境；
- 支持 C++20 的 Clang 或 G++；
- CMake 3.16+ 与 Ninja（推荐），或 GNU Make；
- Python 3（验证和可视化工具）；
- Bash（CTest smoke、示例和 Make 工具目标会调用 shell 脚本）；
- 可选 ccache、clangd、LLDB、`rg`。

核心 C++ 不依赖第三方库。Ramulator2/DRAMsim3 只用于可选外部验证，不参与默认构建。

#### 5.1.1 新环境的基础配置

Ubuntu/Debian 示例：

```bash
sudo apt update
sudo apt install build-essential cmake ninja-build python3 bash

# 选择 Clang 时再安装；版本不必固定为 18，只要完整支持 C++20
sudo apt install clang clangd lldb ccache

cmake --version
ninja --version
python3 --version
clang++ --version   # 或 g++ --version
```

进入仓库根目录后先做一个全新的 out-of-source 构建：

```bash
cmake -S . -B build-port -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=clang++ \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-port --parallel
ctest --test-dir build-port --output-on-failure
./build-port/hbm_sim --config configs/run/hbm4.cfg --requests 128
```

不需要设置项目专用环境变量。配置和 profile 中使用相对路径时，应从仓库根目录运行；若从
其他工作目录启动，应把 `--config`、trace、profile 和输出路径改成绝对路径或正确相对路径。

#### 5.1.2 移植声明

项目的算法核心、Controller、TimingEngine、PHY、统计和 `sparse` 后端只依赖 C++20 标准库，
适合移植到常见 64-bit Linux/Unix 环境。文件后端在 `src/core/memory_backend.cpp` 中直接使用
`mmap`、`open`、`ftruncate`、`pread/pwrite`、`fsync` 和 `unistd/fcntl`，因此：

- Linux 原生环境无需修改；
- WSL 建议把仓库和后端文件放在 WSL Linux 文件系统，而不是低性能的跨系统挂载目录；
- macOS/其他 POSIX 系统应验证 `off_t`、稀疏文件和 `mmap` 行为；
- Windows 原生 MSVC 不能直接编译当前文件后端，需要用 WSL，或为这些 POSIX API 实现
  Win32/file-mapping 适配层；只移植 `sparse` 仍需在编译期隔离 POSIX 实现文件。

文件后端 `.meta` 使用项目版本化的本机二进制结构，虽然检查 magic、版本、record size、
line size 和容量，但不承诺跨大小端、跨 ABI 或跨版本直接搬运。跨架构迁移应先用
`--dump-memory-image` 导出文本/项目二进制 checkpoint，在目标环境重新创建空后端并加载，
不要只复制 data/`.init`/`.meta`/`.present` 后假定兼容。

#### 5.1.3 移植时通常需要修改的位置

| 环境差异 | 需要检查或修改的位置 | 一般不需要改的位置 |
|---|---|---|
| 编译器路径/版本 | CMake 的 `CMAKE_CXX_COMPILER`；`.vscode/settings.json`、`tasks.json` 中 `/usr/bin/clang++-18`/`clangd-18` | `src/` 模型逻辑 |
| 构建目录 | `.vscode/settings.json` 的 `build-clang-debug`、launch/task 中程序路径 | CMake source list |
| 非 POSIX 文件系统 | `src/core/memory_backend.cpp` 的 mmap/file descriptor 实现 | `MemoryBackend` 抽象接口、`sparse` 语义 |
| 没有 Bash | `tests/*.sh`、`examples/*.sh`、CTest shell 项、Make 工具目标 | C++ 单元测试可执行文件 |
| Python 命令不同 | CMake/Make/Python 工具中的 `python3` 调用 | C++ CLI |
| 项目路径改变 | VS Code 配置、外部 Ramulator 根目录、用户配置中的绝对路径 | `${workspaceFolder}` 和仓库内相对路径 |
| 新标准/新器件 | `configs/profiles/`、`configs/calib/`、必要时 traits/profile/finalize | 构建系统通常无需修改 |

不要把 `/usr/bin/clang++-18`、个人目录或 Ramulator 路径写进 C++ 源码；它们应停留在本地
IDE、构建命令或实验配置中。

#### 5.1.4 最小部署集合

本项目没有 `cmake --install` 目标。只运行仿真时至少需要：

```text
hbm_sim                         编译后的可执行文件
configs/                        使用到的 run/profile 配置
examples/ 或自定义 trace/image  输入数据
outputs/                        可写输出位置
```

需要离线验证/可视化时还要复制 `tools/` 并准备 Python 3；需要 CTest 时还要保留 `tests/`、
源码和构建树。部署后先运行 `--help`、128 请求 smoke、一个数据 trace 和 dashboard 生成，
再开展长时间实验。

#### 5.1.5 移植验收清单

- 编译器实际启用 C++20，且 `sizeof(std::uint64_t)==8`；
- Debug/Release 都能构建，`ctest` 全部通过；
- 四标准各完成一个小规模运行；
- `sparse` 数据写后读通过；
- 若启用文件后端，创建、关闭、重开和 sidecar 头校验通过；
- command/DFI CSV 能生成，Python 工具能读取；
- 多 Stack `{stack}` 文件展开正确；
- 新环境与原环境使用同一配置/seed 时，完成量和命令级不变量一致；
- 浮点功耗/热允许记录编译器差异，但必须在交付误差范围内。

### 5.2 Clang 推荐构建

```bash
cmake -S . -B build-clang-debug -G Ninja \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18 \
  -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-clang-debug --parallel
ctest --test-dir build-clang-debug --output-on-failure
```

### 5.3 G++ 构建

```bash
cmake -S . -B build-gcc -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=g++
cmake --build build-gcc --parallel
ctest --test-dir build-gcc --output-on-failure
```

### 5.4 Make 兼容入口

Makefile 使用 `build/`，默认 `CXX ?= g++`、`-std=c++20 -O2 -Wall -Wextra -pedantic`。
它是轻量兼容入口；完整、可枚举的 10 项回归仍以 CMake/CTest 为主。

#### 5.4.1 构建、增量构建和运行

```bash
# 默认 G++ Release-like 构建，生成 build/hbm_sim
make -j"$(nproc)"

# 指定 Clang；切换编译器前必须清理旧 object
make clean
make CXX=/usr/bin/clang++-18 -j"$(nproc)"

# 默认 HBM4 stream/10000 requests
make run

# 只构建目标，不运行
make build/hbm_sim

# Debug/ASan 示例；CXXFLAGS 必须保留 -std=c++20
make clean
make CXX=clang++ \
  CXXFLAGS='-std=c++20 -O0 -g -Wall -Wextra -pedantic -fsanitize=address,undefined' \
  -j"$(nproc)"
```

Make 会用 `-MMD -MP` 生成 `.d` 依赖文件，头文件变化可触发相关 object 增量重编译。不要把
G++ 与 Clang object 混在同一 `build/`；需要并存时优先使用不同 CMake 构建目录。

#### 5.4.2 示例目标

| 命令 | 实际行为/预计产物 |
|---|---|
| `make examples` | 依次运行 HBM4 stream、HBM3 random、LPDDR6 trace、LPDDR5 trace |
| `make examples_hbm4` | 运行 `examples/run_hbm4_stream.sh` |
| `make examples_hbm3` | 运行 `examples/run_hbm3_random.sh` |
| `make examples_lpddr6` | 运行 `examples/run_lpddr6_trace.sh` |
| `make examples_lpddr5` | 运行 `examples/run_lpddr5_trace.sh` |

#### 5.4.3 测试和验证目标

| 命令 | 覆盖内容 |
|---|---|
| `make smoke` | CLI、四标准和主要输出的 shell smoke |
| `make sequence-test` | 构建并运行 C++ `sequence_tests` |
| `make phy-test` | 构建并运行 C++ `phy_tests` |
| `make phy-smoke` | Behavioral PHY CLI 路径 |
| `make visualization-smoke` | 从实际 CLI 产物生成 dashboard |
| `make timing-boundary-validation` | 构建 timing probe 并审计 `t-1/t` 矩阵 |
| `make model-validation` | 数据、后端、DFI、物理、功耗热和身份验收 |
| `make performance-validation` | 生成/检查 CI 规模性能曲线 |
| `make sensitivity-validation` | Timing 敏感性与不确定性检查 |
| `make test` | smoke、sequence、PHY、可视化、Timing 边界、model validation 的组合；不含性能/敏感性/外部参考 |

完整 Make 验收建议：

```bash
mkdir -p outputs/acceptance
set -o pipefail

make CXX=/usr/bin/clang++-18 test 2>&1 \
  | tee outputs/acceptance/make_test.log
test_status=${PIPESTATUS[0]}

make CXX=/usr/bin/clang++-18 performance-validation 2>&1 \
  | tee outputs/acceptance/performance_validation.log
performance_status=${PIPESTATUS[0]}

make CXX=/usr/bin/clang++-18 sensitivity-validation 2>&1 \
  | tee outputs/acceptance/sensitivity_validation.log
sensitivity_status=${PIPESTATUS[0]}

printf 'test=%s performance=%s sensitivity=%s\n' \
  "$test_status" "$performance_status" "$sensitivity_status"

rg '(^PASS |^FAIL |tests passed|validation:|performance curves:|sensitivity/uncertainty:)' \
  outputs/acceptance/*.log | less -S
```

`CXX=/usr/bin/clang++-18` 同时用于主程序和 C++ test binary；Make 会先增量构建
`build/hbm_sim`，某一步返回非 0 时立即停止。`set -o pipefail` 和 `PIPESTATUS[0]` 用于
保留 `tee` 前面 Make 的真实退出码；三个 status 均为 0 才是完整通过。

`make test` 的实际执行顺序是：

1. `smoke`：运行四标准、配置、链路保护、数据和比较工具等 CLI 主路径。
2. `sequence-test`：直接调用 C++ 模型，断言 ACT/RD/WR/PRE、refresh/RFM、低功耗等命令顺序。
3. `phy-test` 和 `phy-smoke`：分别检查 PHY 单元状态机与 Behavioral PHY 的 CLI 整体路径。
4. `visualization-smoke`：从真实 command/DFI/stats 产物生成 HTML，并检查必要数据已嵌入。
5. `timing-boundary-validation`：构建 timing probe，将命令约束转成 `t-1` 必须拒绝、`t` 必须允许的矩阵。
6. `model-validation`：用 Python 反复调用 CLI，校验公式、timing 来源、DFI payload、存储后端、物理坐标、功耗热及项目身份。

例如：

```text
timing boundary validation: 8/8 checks passed
```

不表示只测了 8 条 timing。当前 probe 先生成 315 行边界案例，8 项是对“矩阵非空、
全部边界通过、edge/scope 合法、四标准核心覆盖、summary 可解析”的验证器级汇总。
`skipped` 是明确不适用/无法构造的组合，不是失败；真正的边界错误会使 `failed` 非 0
并返回非 0。

Make 中的 `model-validation` 实际就是：

```bash
python3 ./tools/model_validation.py --binary ./build/hbm_sim
```

用户示例中的主要 PASS 项可按下表解释：

| PASS 项 | 它在证明什么 | 不能扩大成什么结论 |
|---|---|---|
| `project_validation_config_identity` | 验证专用 profile/mode/channel 身份正确，防止跑错配置 | 不是厂商器件身份认证 |
| `peak_bandwidth_formula` | 按 data rate × bus width/8 得到的理论峰值与 CLI 一致 | 不代表 workload 可达到峰值 |
| `interface_rate_formula` | Mbps/pin 到 Gbps/pin 的换算一致 | 不是信号完整性或 BER 验证 |
| `timing_source_audit` | 46 个 timing 项均带有合法来源标签，`bad_sources=[]` | 不代表 research default 已获厂商确认 |
| `research_defaults_are_visible` | 临时/研究默认值没有被伪装成 JEDEC/vendor 值 | 不代表这些值已完成器件校准 |
| `act_to_read_timing` | ACT 到 RD 的实际命令间隔等于预期 60 ticks | 只覆盖该构造场景 |
| `closed_row_read_completion` | closed-row read 从提交到完成的时间链为 125 ticks | 不是所有读延迟的固定值 |
| `dfi_read_latency` | DFI 读返回事件与命令之间为配置的 121 ticks | 不包含模拟电气信号延迟 |
| `online_validators` | command validator 和 DFI validator 都实际运行且 PASS | `off` 不等价于 PASS |
| `dfi_real_payload_roundtrip` | 32 B 写 payload 通过存储/DFI 路径后读回 32 B | 不是长时间随机数据压测 |
| `project_storage_payload_semantics` | write commit、checked read、mismatch 和 sparse backend 的项目语义闭环 | 不说明其他 backend 的宿主性能 |
| `dfi_mask_and_source` | byte mask 合法，payload 来源包含 memory image/无 payload/request payload | 不是 PHY 引脚波形校验 |
| `dfi_expected_payload_guard` | expected 读数据独立参与比较，不是用 actual 自证 | 不会替代更大 trace 的覆盖 |
| `physical_stack_coordinates` | 访问产生 layer/bank/subarray/cell 坐标 | 是行为级映射，不是版图 sign-off |
| `power_thermal_event_path` | 3 个 power event 导致 3 次 thermal update，事件链闭合 | 不是厂商功耗/温度校准 |

脚本还会验证同地址顺序、错误退出码、文件后端持久化、四标准范围、burst frontend、
带宽阈值、NCL 敏感性及 manifest 绑定。当前完整成功摘要是
`model validation: 28/28 passed`；必须同时核对进程退出码，不能只搜索某几个 `PASS`。

`performance-validation` 默认对 HBM3/HBM4/LPDDR5/LPDDR6，分别使用 100/50/0% 读比例
和 64/32/16/8/4/2/1 tick 注入间隔，共生成 84 个确定性曲线点。它检查高负载吞吐
不低于低负载、吞吐不超过物理峰值，以及有读流量时高负载延迟不低于低负载。
当前成功摘要为 `performance curves: 32/32 checks passed; rows=84`。这是内部曲线形状和
峰值边界验证，不是厂商带宽校准。

`sensitivity-validation` 默认对四标准的 `nCL/nRCDRD/nRP/nCCDS/nRRDS/nFAW`
分别做 -10%/标称/+10% 单因素扰动，得到 72 行 OAT 结果；再用固定 seed 组合采样生成
p05/p50/p95 顺序区间，并检查 power scale 与能量/温度的单调性。当前成功摘要为
`sensitivity/uncertainty: 34/34 checks passed; oat_rows=72`。`finite_response` 只证明结果
有限且非负；只有名为 `*_monotonic` 的项才断言单调。p05/p50/p95 是确定性参数不确定区间，
不是硬件统计置信区间。

Make 入口默认只打印性能/敏感性结果，不保存 CSV/JSON。需要表格与可视化输入时，直接
调用工具的 `--csv-out/--json-out`，见第 7.3 节。

#### 5.4.4 可视化和外部参考

```bash
# 生成 outputs/visualization/example/dashboard.html
make visualize-example

# 使用本地 Ramulator2；ramulator-validation 是同义兼容目标
make reference-validation \
  RAMULATOR2_ROOT=/home/guxing/learn/memorystack/ramulator2
make ramulator-validation \
  RAMULATOR2_ROOT=/home/guxing/learn/memorystack/ramulator2
```

`make visualize-example` 会先确保 `build/hbm_sim` 已构建，再运行一个小规模真实仿真以生成
command/DFI/stats/thermal 产物，还会生成 HBM4 下 2 种读比例 × 7 个注入间隔的 14 个性能曲线点，
最后由 `tools/visualize.py` 把数据嵌入自包含 HTML。生成完后：

```bash
test -s outputs/visualization/example/dashboard.html
ls -lh outputs/visualization/example
xdg-open outputs/visualization/example/dashboard.html
```

`test -s` 只检查 HTML 存在且非空；浏览器中还应先看 Run summary 身份与 validation，再看
Trace explorer、Command mix 和 DFI activity。该 HTML 不需要服务器。如果远程主机无图形桌面，
可在项目根目录运行：

```bash
python3 -m http.server 8000 --directory outputs/visualization/example
```

然后从可访问该主机的浏览器打开 `http://HOST:8000/dashboard.html`；涉及跨主机时应遵守
本机网络/防火墙规则，或直接下载 HTML 本地打开。任意自己的仿真如何生成 dashboard 见第 7.3 节。

`reference-validation` 不生成 dashboard。它会定位并调用给定目录中的 Ramulator2，将双方
可共同观测的命令/延迟面做差分，结果打印到终端；可用 `2>&1 | tee outputs/acceptance/ramulator.log`
保存。要做图形对比，应先分别导出可视化支持的 CSV/JSON，不要把差分 PASS 误读为图形产物。

外部参考不属于默认 `make test`，结果只证明共同命令面的差分关系。

#### 5.4.5 清理目标

```bash
# 删除 Make 构建目录 build/；不影响 build-clang-debug/build-gcc
make clean

# 删除 outputs/ 下除 outputs/README.md 外的全部仿真产物
make clean-outputs

# 与 clean-outputs 完全相同的旧兼容名称
make delete
```

不同编译器必须使用不同构建目录。PHY 是 `hbm_sim_core` 的组成部分，不单独建立
`build-phy*`。VS Code/clangd 使用 `build-clang-debug/compile_commands.json`。

---

## 6. 仿真模型使用说明

### 6.1 标准流程

```text
选择 standard/profile
  → 选择 workload/trace
  → 选择 stack/PHY/backend
  → 配置输出与验证
  → 运行仿真
  → 检查退出码和 validation 字段
  → 检查数据、性能、功耗热和 trace
  → 保存完整配置、命令、commit 与产物
```

### 6.2 四标准快速运行

下表给出快速入口。需要查看完整的产物导出、指标筛选、预计效果和可视化命令时，直接跳到
[附录 C：四标准交付用例](#附录-c四标准交付用例)。

| 标准 | 快速配置 | 完整交付案例 |
|---|---|---|
| HBM3 | `configs/run/hbm3.cfg` | [C.1 HBM3](#c1-hbm3) |
| HBM4 | `configs/run/hbm4.cfg` | [C.2 HBM4](#c2-hbm4) |
| LPDDR5 | `configs/run/lpddr5.cfg` | [C.3 LPDDR5](#c3-lpddr5) |
| LPDDR6 | `configs/run/lpddr6.cfg` | [C.4 LPDDR6](#c4-lpddr6) |

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm3.cfg
./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg
./build-clang-debug/hbm_sim --config configs/run/lpddr5.cfg
./build-clang-debug/hbm_sim --config configs/run/lpddr6.cfg
```

最小命令也可直接选择标准：

```bash
./build-clang-debug/hbm_sim --standard hbm4 --requests 1024
```

### 6.3 合成流量

```bash
./build-clang-debug/hbm_sim --standard hbm4 \
  --pattern random --requests 10000 --read-ratio 70 \
  --addr-stride 64 --inject-interval 2 --seed 42
```

合成流量不需要先制作 trace，frontend 会按参数逐条生成 host request：

| 参数 | 含义 |
|---|---|
| `--pattern stream` | 地址按 `request_id × addr_stride` 增长，适合连续/跨行步长实验 |
| `--pattern random` | 在聚合地址空间内随机选择 cache line；此时 `addr_stride` 不参与地址生成 |
| `--requests 10000` | 生成 10000 个拆分前 host request；一个 host request 可拆成多个 DRAM transaction |
| `--read-ratio 70` | 每条 host request 有 70% 概率为读，写比例为 30% |
| `--inject-interval 2` | 第 N 条请求的最早注入时间为 `N×2` controller ticks；0 表示全部从 cycle 0 开始可注入 |
| `--seed 42` | 固定地址与读写类型的随机序列，便于重放 |

建议先用 128/512 请求检查身份和完成性，再扩大到 10000 或更多。上例只把统计打到
stdout；如果需要查看具体生成的地址/命令，同时导出 command 和 response trace：

```bash
mkdir -p outputs/synthetic
./build-clang-debug/hbm_sim --standard hbm4 \
  --pattern random --requests 512 --read-ratio 70 \
  --inject-interval 2 --seed 42 \
  --cmd-trace outputs/synthetic/commands.csv \
  --response-trace outputs/synthetic/responses.csv \
  | tee outputs/synthetic/stats.txt

rg '^(inject_interval|host_requests|dram_transactions|completed_reads|completed_writes|injection_stall_cycles|remaining_requests|hit_cycle_limit|avg_read_latency|achieved_bw_GBps)\s*:' \
  outputs/synthetic/stats.txt
column -s, -t < outputs/synthetic/responses.csv | less -S
```

stdout 当前没有单独的 `pattern` 和 `seed` 字段，所以实验必须保留完整命令/配置作为身份证据；
`inject_interval` 可从统计复核。需要曲线时，应用 `tools/performance_curve.py`
输出 CSV/JSON 再加入 dashboard，而不是手工把多次 stdout 拼在一起。改变 injection interval 时，
标准、profile、pattern、requests、read ratio、seed 和其他配置必须固定，才能形成可比较的
注入率—延迟—吞吐曲线。

### 6.4 多 Stack

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_6stack.cfg
```

或覆盖：

```bash
./build-clang-debug/hbm_sim --standard hbm4 \
  --stack-count 6 --stack-mapping interleaved \
  --stack-interleave-bytes 256 \
  --stack-ingress-buffer-size 256 --stack-dispatch-width 4 \
  --stack-qos-policy strict_priority --requests 10000
```

多 Stack 不是把一颗 stack 的统计乘以 N。`MemorySystem` 对每个 `[stack][channel]` 创建独立
Controller，每个 controller 有自己的 PHY，每个 stack 有独立 ingress、MemoryImage、功耗和热状态。
`interleaved + 256 B` 将系统地址按 256 B stripe 在 stack 间轮转，进入 stack 后再转成局部地址并解码
channel/bank/row。Trace 中显式 `stack=N` 会覆盖自动映射。

建议先小规模运行并查看当前真实动态字段：

```bash
mkdir -p outputs/multistack
./build-clang-debug/hbm_sim --config configs/run/hbm4_6stack.cfg \
  --requests 1024 | tee outputs/multistack/stats.txt

rg '^(stack_count|active_stacks|controller_count|active_controllers|aggregate_capacity_bytes|system_cycles|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|stack_[0-9]+_(active_ctrls|reads|writes|bw_GBps|avg_read_latency|ingress_stalls|qos_dispatches|power_pJ|peak_temp_C))\s*:' \
  outputs/multistack/stats.txt | less -S
```

`stack_N_reads/writes` 是第 N 颗 stack 完成的 DRAM transaction，各 stack 求和应等于全局
`completed_reads/writes`；`stack_N_bw_GBps` 是该 stack payload 带宽；`avg_read_latency`、
`ingress_stalls`、`power_pJ`、`peak_temp_C` 分别用于判断排队、入口反压、能量和热点。
`active_stacks=stack_count` 只表示所有 stack 都被触达，不表示负载完全均衡。应对比每颗 stack
的 reads+writes、带宽、延迟和温度；只看聚合带宽可能掩盖偏斜与热点。需要时间线时再加
`--cmd-trace` 生成 dashboard，按 Stack 过滤器切换查看。

### 6.5 存储后端选择

三种后端共用同一个 `MemoryImage` 语义层，只改变 payload、初始化 mask 和
metadata 在宿主机上的保存方式。切换后端不应改变命令序列、完成数或读写数据。

| 后端 | 宿主保存方式 | 适用场景 | 人工查验重点 |
|---|---|---|---|
| `sparse` | 进程内只保存已触达 line | 低密度、快速功能/数据验证 | stdout + final txt/CSV；进程退出后无原始 backend |
| `mmap_sparse` | data/init/meta/present 四文件，由 OS 稀疏映射 | 大逻辑容量、持久化、稀疏实占 | 四件齐全、header、逻辑/实占大小、重开及 golden |
| `chunk_file` | 相同四文件 + 有界 chunk cache | 需明确限制主数据 cache 的文件后端 | 除上述项外，还要核对 chunk size/cache entries |

这三种是“存储媒介选择”，不是三套 DRAM 协议模型。功能对比应使用相同 profile、
trace、初始 image、容量和 line size；不应用原始 `.bin` 字节布局代替统一 final image 的语义比较。

#### 6.5.1 选择和最小运行方法

```bash
# 内存稀疏后端
./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --memory-backend sparse

# mmap sparse 文件后端
./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --memory-backend mmap_sparse --memory-capacity-bytes 1073741824 \
  --memory-data-file outputs/hbm4_mmap.bin

# chunk file 后端
./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --memory-backend chunk_file --memory-capacity-bytes 1073741824 \
  --memory-data-file outputs/hbm4_chunks.bin \
  --memory-chunk-size 2097152 --memory-chunk-cache-entries 16
```

文件后端可能产生 `.init`、`.meta`、`.present` sidecar。更改 block size/容量/格式后应
使用新路径，避免把旧文件误当作兼容 checkpoint。多 stack 路径可用 `{stack}` 占位符；
未使用占位符时程序自动插入 `.stackN`。

#### 6.5.2 共同的可重放人工查验准备

下面使用 1 MiB 逻辑容量做人工查验，以减少全量导出和 metadata 扫描成本。三次
运行必须使用同一 `examples/memory_image.txt`、`examples/data_check.trace`、profile 和容量。
每次重做实验应换一个新目录，不要直接复用旧的文件后端。

```bash
mkdir -p outputs/backend_audit/sparse \
  outputs/backend_audit/mmap outputs/backend_audit/chunk

./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --memory-backend sparse --memory-capacity-bytes 1048576 \
  --dump-memory-image outputs/backend_audit/sparse/final.txt \
  --dump-memory-csv outputs/backend_audit/sparse/final.csv \
  --mismatch-report outputs/backend_audit/sparse/mismatch.txt \
  | tee outputs/backend_audit/sparse/stats.txt
```

首先查运行完整性，再查数据：

```bash
rg '^(memory_backend|memory_capacity_bytes|memory_backend_line_bytes|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|data_mismatches|golden_mismatches|storage_lines_allocated|unique_written_lines|storage_read_line_accesses|storage_write_line_accesses|rowbuf_.*)\s*:' \
  outputs/backend_audit/sparse/stats.txt
sed -n '1,20p' outputs/backend_audit/sparse/final.txt
sed -n '1,10p' outputs/backend_audit/sparse/final.csv
sed -n '1,40p' outputs/backend_audit/sparse/mismatch.txt
```

通过标准是 `remaining_requests : 0`、`hit_cycle_limit : false`、`data_mismatches : 0`；CSV 中的
address、data、initialized mask、version、writer 和物理坐标应与文本导出一致。`sparse` 是
进程内容器，退出后没有原始 backend 文件；`final.txt/final.csv` 就是它的人工审计产物。

上述统计分三层看：`completed_* + remaining_requests + hit_cycle_limit` 证明运行完整；
`data_mismatches/golden_mismatches` 证明数据比较结果（sparse 基线未使用 `--verify-golden`
时不会出现 `golden_mismatches`）；`storage_*` 和 `rowbuf_*` 说明实际分配/读写的 line 以及
ACT/RD/WR/PRE 驱动的 row-buffer 行为。原正则中的 `rowbuf_` 只会匹配名称恰好等于
`rowbuf_` 的字段，必须写为 `rowbuf_.*` 才能选中 `rowbuf_hits` 等真实字段。

| 统计族 | 人工解读 |
|---|---|
| `memory_backend/capacity/line_bytes` | 本次真正使用的后端、逻辑容量和 backend line 粒度 |
| `completed_reads/writes` | 已完成的 DRAM transaction；需与 host request/拆分关系一起解释 |
| `storage_lines_allocated` | 最终 backend 中存在的 line 数，可包含由初始 image 带入的 line |
| `unique_written_lines` | 本次仿真中至少被写一次的不同 line 数 |
| `storage_read/write_line_accesses` | 存储语义层累计的 line 读/写访问次数，不是唯一 line 数 |
| `rowbuf_activations/precharges` | 由 ACT/PRE/自动预充驱动的 row-buffer 打开/关闭数 |
| `rowbuf_hits/misses` | 数据操作在当前 row-buffer 上的命中/未命中 |
| `rowbuf_dirty_writebacks` | 关闭 dirty row 时写回 MemoryImage 的 line 数 |

`final.txt` 首行是列说明，后续每行是一条已分配 line；`data=` 是 payload，`init=` 是逐 byte
初始化 mask，`version/last_writer/last_write_cycle` 用于重放溯源，后面是物理/热坐标。
`final.csv` 是同一语义的表格版。`mismatch.txt` 只有 `# cycle ... expected actual` 表头表示没有
错误记录，不是“没有生成”。对齐查看可用：

```bash
less -S outputs/backend_audit/sparse/stats.txt
less -S outputs/backend_audit/sparse/final.txt
column -s, -t < outputs/backend_audit/sparse/final.csv | less -S
cut -d, -f1,2,3,5,6,7,13,18,19,20 \
  outputs/backend_audit/sparse/final.csv | column -s, -t | less -S
less -S outputs/backend_audit/sparse/mismatch.txt
```

`cut` 一行只选 address/data/init/version/writer/cycle/channel/bank/row/column，适合终端宽度；需要全列时
用第三条并在 `less` 中左右滚动，或用 `libreoffice --calc final.csv`/Excel/WPS 打开。

#### 6.5.3 `mmap_sparse` 的人工查验

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
  --memory-data-file outputs/backend_audit/mmap/data.bin \
  --dump-memory-image outputs/backend_audit/mmap/final.txt \
  --dump-memory-csv outputs/backend_audit/mmap/final.csv \
  --mismatch-report outputs/backend_audit/mmap/mismatch.txt \
  --verify-golden outputs/backend_audit/sparse/final.txt \
  | tee outputs/backend_audit/mmap/stats.txt

ls -lh outputs/backend_audit/mmap/data.bin*
stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
  outputs/backend_audit/mmap/data.bin*
od -An -tc -N8 outputs/backend_audit/mmap/data.bin.meta
od -An -tx1 -N64 outputs/backend_audit/mmap/data.bin.meta
rg '^(memory_backend|golden_verified|golden_mismatches|data_mismatches|storage_lines_allocated|unique_written_lines)\s*:' \
  outputs/backend_audit/mmap/stats.txt
diff -u outputs/backend_audit/sparse/final.txt \
  outputs/backend_audit/mmap/final.txt
```

`od -tc` 应显示 `H B M B A C K \0`；64-byte header 后跟每 line metadata。`stat` 中 `%s`
是逻辑文件大小，`%b×%B` 才是近似实际磁盘占用，稀疏文件两者可差很多。
`data.bin` 不能单独解读，还需 `.init`、`.meta`、`.present`；四件中任一缺失都不是
完整 checkpoint。`golden_mismatches : 0` 且 `diff` 无输出才证明其与 sparse 语义一致。
`golden_verified` 是实际与 golden 比较的 line 数，不是布尔 PASS；它应与本用例有效 final line
数一致，而是否通过由 `golden_mismatches : 0` 和进程退出码联合判定。

`data.bin` 保存 payload，`.init` 保存初始化状态，`.meta` 保存格式头和每 line metadata，
`.present` 标识哪些 line 实际存在。二进制文件不要用 `cat`；人工只看头部和宿主文件特性：

```bash
file outputs/backend_audit/mmap/data.bin*
du -h outputs/backend_audit/mmap/data.bin*
xxd -g 1 -l 128 outputs/backend_audit/mmap/data.bin.meta | less -S
column -s, -t < outputs/backend_audit/mmap/final.csv | less -S
less -S outputs/backend_audit/mmap/mismatch.txt
```

`ls/stat` 中的逻辑大小说明可寻址文件范围，`du` 或 `allocated_blocks×block_bytes` 才近似说明
当前实占磁盘。`diff -u` 完全无输出就是相同；有 `+/-` 行才是差异。

另外做一次“关闭后重开”检查：用相同容量、line size 和四件路径重启程序，
但第二次不再重放写 trace，只将已持久化的状态导出到新文件：

```bash
./build-clang-debug/hbm_sim --standard hbm4 --requests 0 \
  --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
  --memory-data-file outputs/backend_audit/mmap/data.bin \
  --dump-memory-image outputs/backend_audit/mmap/reopened.txt \
  --dump-memory-csv outputs/backend_audit/mmap/reopened.csv \
  | tee outputs/backend_audit/mmap/reopen_stats.txt

diff -u outputs/backend_audit/mmap/final.txt \
  outputs/backend_audit/mmap/reopened.txt
```

程序能通过 header 检查并再次得到同样的 final image，才证明
持久化可重开。如果改了容量、transaction bytes 或当前磁盘格式，程序应拒绝不兼容
`.meta`，此时应换新路径，不应手工修改 header。

#### 6.5.4 `chunk_file` 的人工查验

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --memory-backend chunk_file --memory-capacity-bytes 1048576 \
  --memory-data-file outputs/backend_audit/chunk/data.bin \
  --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
  --dump-memory-image outputs/backend_audit/chunk/final.txt \
  --dump-memory-csv outputs/backend_audit/chunk/final.csv \
  --mismatch-report outputs/backend_audit/chunk/mismatch.txt \
  --verify-golden outputs/backend_audit/sparse/final.txt \
  | tee outputs/backend_audit/chunk/stats.txt

ls -lh outputs/backend_audit/chunk/data.bin*
stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
  outputs/backend_audit/chunk/data.bin*
od -An -tc -N8 outputs/backend_audit/chunk/data.bin.meta
rg '^(memory_backend|memory_chunk_size|memory_chunk_cache_entries|golden_verified|golden_mismatches|data_mismatches|storage_lines_allocated|unique_written_lines)\s*:' \
  outputs/backend_audit/chunk/stats.txt
diff -u outputs/backend_audit/sparse/final.txt \
  outputs/backend_audit/chunk/final.txt
```

`chunk_file` 的人工数据判定仍以 `final.txt/final.csv`、golden 和 mismatch report 为准；
原始文件用于检查四件是否存在、header 是否正确、磁盘占用是否受控。内存上限的粗略主数据量是
`memory_chunk_size × memory_chunk_cache_entries`，不包括 metadata、bitmap 和 C++ 容器开销。
对齐查看与 mmap 相同：

```bash
file outputs/backend_audit/chunk/data.bin*
stat --format='%n logical=%s allocated=%b*%B' \
  outputs/backend_audit/chunk/data.bin*
xxd -g 1 -l 128 outputs/backend_audit/chunk/data.bin.meta | less -S
column -s, -t < outputs/backend_audit/chunk/final.csv | less -S
less -S outputs/backend_audit/chunk/mismatch.txt
```

重开检查与 mmap 相同：必须原样提供四件路径和格式参数，但不重放写 trace，然后再导出文本做语义比较：

```bash
./build-clang-debug/hbm_sim --standard hbm4 --requests 0 \
  --memory-backend chunk_file --memory-capacity-bytes 1048576 \
  --memory-data-file outputs/backend_audit/chunk/data.bin \
  --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
  --dump-memory-image outputs/backend_audit/chunk/reopened.txt \
  | tee outputs/backend_audit/chunk/reopen_stats.txt

diff -u outputs/backend_audit/chunk/final.txt \
  outputs/backend_audit/chunk/reopened.txt
```

#### 6.5.5 三种后端的最终判定表

| 检查面 | `sparse` | `mmap_sparse` | `chunk_file` | 通过条件 |
|---|---|---|---|---|
| 数据语义 | 文本/CSV | 文本/CSV + golden | 文本/CSV + golden | 三份 final image 一致，mismatch 为 0 |
| 协议行为 | stats/command trace | 同左 | 同左 | 完成数、命令数和 system cycles 一致 |
| 持久化 | 不适用 | 重开后导出 | 重开后导出 | 重开不丢数据 |
| 磁盘结构 | 无 | data + 3 sidecar | data + 3 sidecar | magic/header 正确，四件齐全 |
| 宿主资源 | RSS | `stat` 逻辑/实占 | `stat` + cache 设置 | 与选择后端的目标相符 |

原始 `.bin` 不要在三种后端之间做逐字节 `cmp`；它们的物理布局本来就不同。应比较
统一导出的 text/CSV、golden 结果和协议统计。若只有数据一致而 system cycles 不同，
需要检查是否复用了旧 backend，或三次运行的 config/CLI 并非完全一致。

#### 6.5.6 多 Stack 后端查验

多 Stack 时每个 stack 有独立 `MemoryImage` 和 backend。推荐在所有后端及导出路径中
显式使用 `{stack}`：

```bash
mkdir -p outputs/backend_audit/multi
./build-clang-debug/hbm_sim --config configs/run/hbm4_6stack.cfg \
  --requests 1024 --memory-backend mmap_sparse \
  --memory-capacity-bytes 1048576 \
  --memory-data-file 'outputs/backend_audit/multi/data_{stack}.bin' \
  --dump-memory-image 'outputs/backend_audit/multi/final_{stack}.txt' \
  --dump-memory-csv 'outputs/backend_audit/multi/final_{stack}.csv' \
  | tee outputs/backend_audit/multi_stats.txt
ls -lh outputs/backend_audit/multi/data_*.bin*
rg '^(stack_count|active_stacks|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|stack_[0-9]+_(active_ctrls|reads|writes|bw_GBps|avg_read_latency|ingress_stalls|power_pJ|peak_temp_C))\s*:' \
  outputs/backend_audit/multi_stats.txt
```

人工查验时不能只打开 stack 0：要遍历所有 `final_N.*`，并对照 `stack_N_*`
确认哪些 stack 本就应该有数据。某个文件为空可能是地址映射未触达该 stack，不能单独
据此判定 backend 失效。

当前 CLI 的精确字段名是 `stack_N_reads/writes/bw_GBps/...`，不是
`stack_N_completed_reads/achieved_bw_GBps`。先检查 `remaining_requests=0` 与 `hit_cycle_limit=false`，
再验证各 stack 的 `reads+writes` 之和等于全局完成数。文件和表格可批量查看：

```bash
wc -l outputs/backend_audit/multi/final_*.txt \
      outputs/backend_audit/multi/final_*.csv

for csv_file in outputs/backend_audit/multi/final_*.csv; do
  printf '\n[%s]\n' "$csv_file"
  cut -d, -f1,5,6,7,13,18,19,20,21 "$csv_file" \
    | column -s, -t | sed -n '1,8p'
done

for meta_file in outputs/backend_audit/multi/data_*.bin.meta; do
  printf '%s: ' "$meta_file"
  od -An -tc -N8 "$meta_file"
done
```

`wc -l` 显示每颗 stack 导出的 line 数；循环只预览每个 CSV 的前 7 条数据；每个 `.meta`
都应显示 `H B M B A C K \0`。如果需要比较不同后端，对每个 stack 的 `final_N.txt` 执行
`diff -u`，不对 `data_N.bin` 执行 `cmp`。

### 6.6 Trace 与数据正确性

```text
0 W 0x1000 data=0011223344556677 mask=ffffffffffffffff
20 R 0x1000 expect=0011223344556677
100 BW 0x2000 256 pattern=inc stack=2 qos=7
300 BR 0x2000 256 check=last_write stack=2 qos=7
```

Trace 按空白分隔，空行和 `#` 注释被忽略。基本格式是 `[cycle] R|W address [key=value ...]`：

| 记号 | 含义 |
|---|---|
| `R` / `W` | 普通读/写；可选首列 cycle 是最早注入时间 |
| `data=HEX` | 写 payload；没有 payload 的合成写会由 request id/地址生成可重放数据 |
| `expect=HEX` | 读完成时独立比较 expected/actual |
| `mask=HEX` | 逐 byte 写 mask，非 0 byte 才更新相应位置 |
| `BW` / `BR` | burst write/read；地址后跟 byte 数，frontend 再拆成 DRAM transaction |
| `pattern=inc` | burst write 数据生成规则；也可使用明确 payload |
| `check=last_write` | burst read 与该地址最近一次写入比较 |
| `stack=N qos=N` | 显式目标 stack 和 QoS 类；未指定 stack 时按系统地址映射 |

`--trace` 存在时 synthetic `pattern` 被忽略。`--requests 0` 在 trace 模式下表示读取完整文件，
而不是“运行 0 条”；设为正数则限制展开后的请求数。

```bash
mkdir -p outputs
set -o pipefail
./build-clang-debug/hbm_sim --standard hbm4 \
  --trace examples/data_check.trace --requests 0 \
  --dump-memory-image outputs/final_memory.txt \
  --dump-memory-csv outputs/final_memory.csv \
  --mismatch-report outputs/mismatch.txt \
  --fail-on-data-mismatch \
  | tee outputs/data_check_stats.txt
run_status=${PIPESTATUS[0]}
printf 'hbm_sim exit=%s\n' "$run_status"
```

当前 `examples/data_check.trace` 包含 3 写、4 读，覆盖同地址写后读、偏移写、mask 写和部分读。
在未修改示例和默认 HBM4 参数的前提下，预期核心结果为：

```text
completed_reads               : 4
completed_writes              : 3
host_requests                 : 7
dram_transactions             : 7
data_checked_reads            : 4
data_mismatches               : 0
data_write_commits            : 3
remaining_requests            : 0
hit_cycle_limit               : false
```

这些 stdout 指标说明 7 条 trace 已完成、4 次带 expected 的读都通过、3 次写已 commit，且
不是周期上限截断。它们不会把最终 payload 全部打到 stdout；具体数据在三个文件中人工查看：

```bash
rg '^(completed_reads|completed_writes|host_requests|dram_transactions|data_checked_reads|data_mismatches|data_write_commits|remaining_requests|hit_cycle_limit)\s*:' \
  outputs/data_check_stats.txt

less -S outputs/final_memory.txt
column -s, -t < outputs/final_memory.csv | less -S
cut -d, -f1,2,3,5,6,7,13,18,19,20 \
  outputs/final_memory.csv | column -s, -t | less -S
less -S outputs/mismatch.txt
wc -l outputs/final_memory.txt outputs/final_memory.csv outputs/mismatch.txt
```

`final_memory.txt` 一行对应一个已分配 line，查 `address/data/init/version/last_writer`及物理坐标；
CSV 是同样内容的对齐表格。`mismatch.txt` 只有表头表示无 mismatch；如果有数据行，则每行给出
cycle/request/address/expected/actual 及物理位置。`wc -l` 用于区分“只有表头”和“有错误记录”。

如需二进制稀疏 checkpoint，将后缀换为 `.bin`：

```bash
./build-clang-debug/hbm_sim --standard hbm4 \
  --trace examples/data_check.trace --requests 0 \
  --dump-memory-image outputs/final_memory.bin

file outputs/final_memory.bin
stat --format='%n bytes=%s' outputs/final_memory.bin
xxd -g 1 -l 128 outputs/final_memory.bin | less -S
```

`.bin` 适合用 `--memory-image` 重载或 `--verify-golden` 做语义检查，不适合通过肉眼解析全部结构；
需要人工看内容时应同时导出 txt/CSV。

数据不匹配且指定 `--fail-on-data-mismatch` 时退出码为 3；故障注入只想记录时使用
`--allow-data-mismatch`。后者允许返回 0，但 `data_mismatches` 和 mismatch report 仍必须按错误结果解读。

### 6.7 自定义配置

本节给出通用的完整流程；一款假定新 HBM4 的逐字段示例见
[附录 D：新 HBM4 自定义用例](#附录-d新-hbm4-自定义用例)。

#### 6.7.1 第一步：建立完整配置文件

以已有、可审计的 profile 为起点，再写 workload、系统、PHY、后端和输出。下面的
`configs/run/my_case.cfg` 是一个完整可运行的双 Stack HBM4 研究配置：

```ini
# 身份与来源
standard = hbm4
timing_profile = hbm4_jedec_8g_32gb_8hi
timing_profile_file = configs/profiles/hbm4/jedec_8000_32gb_8hi.cfg
vendor_profile = generic
mode_profile = full_stack_crc_off
speed_bin_mbps = 8000
density_gb = 32
stack_height = 8

# workload
pattern = random
requests = 10000
read_ratio = 70
seed = 2026
addr_stride = 64
inject_interval = 2
max_cycles = 100000000

# Controller 与地址映射
read_buffer_size = 128
write_buffer_size = 128
priority_buffer_size = 4096
scheduler = frfcfs
row_policy = open_page
address_mapping = default
channel_mapper = decoded

# 两颗独立 stack
stack_count = 2
stack_mapping = interleaved
stack_interleave_bytes = 256
stack_ingress_buffer_size = 256
stack_dispatch_width = 4
stack_qos_policy = strict_priority

# 行为级 PHY
mem_phy_mode = behavioral
dfi_version = 6.0.1
phy_command_fifo_depth = 64
phy_read_fifo_depth = 64
phy_write_fifo_depth = 64
phy_command_pipeline_cycles = 1
phy_read_return_pipeline_cycles = 1
phy_write_data_pipeline_cycles = 1
phy_reset_cycles = 4
phy_initialization_cycles = 4
phy_training_cycles = 8
phy_auto_train = true

# 维护策略
supports_refresh = true
refresh_policy = per_bank
supports_rfm = true
rfm_policy = per_bank
rfm_act_threshold = 256
rfm_decrement = 256
refresh_temperature_mode = normal

# 数据、物理、功耗与热
memory_backend = sparse
floorplan = true
power_model = true
thermal_model = true
power_scale = 1.0
thermal_ambient_c = 40
thermal_cooling_per_cycle = 0.00025
thermal_rise_c_per_pj = 0.00002

# 可重放产物
cmd_trace = outputs/my_case/commands.csv
response_trace = outputs/my_case/responses.csv
dfi_trace = outputs/my_case/dfi.csv
dfi_signal_trace = outputs/my_case/dfi_signal.csv
dump_timing_table = outputs/my_case/timing.csv
dump_memory_image = outputs/my_case/final_memory_{stack}.txt
dump_memory_csv = outputs/my_case/final_memory_{stack}.csv
mismatch_report = outputs/my_case/mismatch.txt
dump_thermal_map = outputs/my_case/thermal_map_{stack}.txt
validate_cmd_trace = true
validate_dfi_trace = true
```

这里引用的 profile 路径在项目内真实存在。配置覆盖顺序仍是“标准默认 < profile < 本文件
< CLI”，因此输出中的最终配置快照和 timing table 才是本次实验真正使用的数值。

#### 6.7.2 第二步：先做小规模配置验证

```bash
mkdir -p outputs/my_case
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 128 --max-cycles 1000000 \
  | tee outputs/my_case/smoke_stats.txt
```

预期：退出码为 0，`remaining_requests : 0`、`hit_cycle_limit : false`，命令和 DFI 验证
均为 PASS；`controller_count` 应与两颗 full-stack 的 controller 数一致，
`stack_0_*`、`stack_1_*` 均应出现。若小规模不能完成，不要直接扩大 requests。

#### 6.7.3 第三步：用 CLI 自定义 workload、调度和地址模式

```bash
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 50000 --seed 7 --pattern random --read-ratio 80 \
  --inject-interval 1 --scheduler frfcfs --row-policy open_page \
  --addr-mapping RoBaRaCoCh \
  | tee outputs/my_case/high_load_stats.txt
```

预期：CLI 值覆盖配置文件；更高注入率通常提高吞吐并增加排队延迟，新的地址映射会改变
活跃 channel 和 row hit/conflict 分布。比较时只能改变研究变量，其余配置和 seed 应固定。

#### 6.7.4 第四步：选择 PHY 模式

```bash
# 正式行为级路径
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 1024 --mem-phy behavioral \
  | tee outputs/my_case/behavioral_stats.txt

# 兼容旁路对照
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 1024 --mem-phy direct \
  | tee outputs/my_case/direct_stats.txt
```

Behavioral 预期出现 PHY 生命周期、FIFO、pipeline、DFI 和可能的反压计数；Direct 用于
隔离 PHY 影响。两者不能被解释为“真实 PHY”和“无 PHY”的器件级精度对比。

#### 6.7.5 第五步：选择存储后端

```bash
# 默认稀疏内存
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 1024 --memory-backend sparse

# 持久 mmap；使用新的文件路径
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 1024 --memory-backend mmap_sparse \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file outputs/my_case/backend/mmap.bin

# 有界 chunk cache
./build-clang-debug/hbm_sim --config configs/run/my_case.cfg \
  --requests 1024 --memory-backend chunk_file \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file outputs/my_case/backend/chunks.bin \
  --memory-chunk-size 2097152 --memory-chunk-cache-entries 16
```

预期三种后端的完成数、命令序列和最终 payload 一致，差别主要是宿主内存、文件持久化和
I/O 开销。文件后端的 data/`.init`/`.meta`/`.present` 应作为一组管理。
具体的 text/CSV、golden、magic/header、磁盘实占、重开和多 Stack 人工查验方法见
[6.5.2–6.5.6](#652-共同的可重放人工查验准备)。

#### 6.7.6 第六步：查看运行过程和输出

```bash
# 关键完成/性能/验证指标
rg '^(standard|controller_count|stack_count|system_cycles|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|avg_read_latency|achieved_bw_GBps|bandwidth_util_pct|cmd_validation|dfi_validation|data_mismatches)\s*:' \
  outputs/my_case/high_load_stats.txt

# 查看 timing 来源和前 20 行最终表
rg '^timing_(profile|source|vendor_required)' outputs/my_case/smoke_stats.txt
sed -n '1,20p' outputs/my_case/timing.csv

# 查看命令/响应/DFI 文件头和少量事件
sed -n '1,8p' outputs/my_case/commands.csv
sed -n '1,8p' outputs/my_case/responses.csv
sed -n '1,8p' outputs/my_case/dfi.csv

# 数据和热输出
sed -n '1,20p' outputs/my_case/final_memory_0.txt
sed -n '1,20p' outputs/my_case/mismatch.txt
sed -n '1,20p' outputs/my_case/thermal_map_0.txt
```

预期：统计采用 `字段 : 值`；CSV 第一行为列名；没有数据错误时 mismatch 文件为空或只含
无错误摘要；thermal map 只有在启用 thermal 且发生存储事件时才有有效节点。

#### 6.7.7 第七步：生成和查看可视化

```bash
python3 tools/visualize.py \
  --command-trace outputs/my_case/commands.csv \
  --dfi-trace outputs/my_case/dfi.csv \
  --stats outputs/my_case/smoke_stats.txt \
  --thermal-map outputs/my_case/thermal_map_0.txt \
  --title 'My HBM4 two-stack experiment' \
  --max-events 50000 \
  --out outputs/my_case/dashboard.html

xdg-open outputs/my_case/dashboard.html
```

预期：Run summary 显示本次配置；Trace explorer 可按 stack/channel/command/request
过滤；Command mix 非空；DFI activity 与 Behavioral 模式一致；当前 dashboard 热图显示所选的
stack 0 文件并可切换 layer，stack 1 可用 `thermal_map_1.txt` 另生成一份 dashboard。
详细操作和分析方法见[第 7.3 节](#73-任意仿真的离线可视化)。

---

## 7. 仿真输出与查看方法

### 7.1 stdout

stdout 是左侧字段名、右侧值的 `字段 : 值` 文本统计，适合 `tee`、`rg`、脚本和版本比较：

```bash
mkdir -p outputs/run1
set -o pipefail

# 第一轮用小规模，并真正启用两个 validator
/usr/bin/time -f 'elapsed=%E exit=%x' \
  ./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg \
  --requests 256 --validate-cmd-trace --validate-dfi-trace \
  2>outputs/run1/stderr.txt \
  | tee outputs/run1/stats.txt
run_status=${PIPESTATUS[0]}
printf 'hbm_sim exit=%s\n' "$run_status"

# 第一轮先检查运行身份、完成性和验证状态
rg '^(standard|timing_profile|vendor_profile|mem_phy_mode|stack_count|system_cycles|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|cmd_validation|dfi_validation|data_mismatches)\s*:' \
  outputs/run1/stats.txt

# 第二轮再检查性能、排队、行行为和 PHY 反压
rg '^(peak_bandwidth_GBps|achieved_bw_GBps|bandwidth_util_pct|avg_read_latency|read_queue_len_avg|write_queue_len_avg|row_hits|row_misses|row_conflicts|phy_command_backpressure|phy_data_backpressure)\s*:' \
  outputs/run1/stats.txt

# 编译/运行错误与 time 总结在 stderr 文件
less -S outputs/run1/stderr.txt
```

首先检查：退出码、`completed_reads/writes`、`remaining_requests`、
`cmd_validation`、`dfi_validation`、`data_mismatches`、`hit_cycle_limit`；再解释性能。

这条命令的运行过程是：读取配置和 profile → 生成 frontend 流量 → 逐 cycle 推进多
Controller/PHY/Stack → drain 请求和响应 → 运行 command/DFI validator → 一次性打印统计。
当前 CLI 不按 cycle 打印进度，因此大规模时终端和 `stats.txt` 在结束前可以一直没有内容；
`tee` 不能显示程序尚未产生的输出，`stdbuf` 也不能改变这一点。中途 `Ctrl+C` 则统计文件可能仍为 0
byte。

这也是第一轮显式使用 `--requests 256` 的原因。完成性和 validator 均通过后，再去掉该
覆盖以使用 `hbm4.cfg` 中的 10000 requests。另一终端可用：

```bash
pgrep -af 'build-clang-debug/hbm_sim'
watch -n 1 'ps -o pid,etime,%cpu,%mem,rss,stat,cmd -C hbm_sim'
```

`PID` 是进程号，`ELAPSED` 是运行时间，`%CPU≈100` 通常表示正在占用一个核计算，`RSS`
是实驻内存，`STAT=R` 表示正在运行，`S` 表示当前等待/休眠。单次的 `R` 或 `S` 不能单独证明
是否死锁；要结合 elapsed 增长、CPU、错误输出和最终退出码。在 `watch` 窗口按 `Ctrl+C`
只停止 watch；要停止仿真，应在启动仿真的终端按 `Ctrl+C`。

`cmd_validation : off`/`dfi_validation : off` 只表示本次未运行 validator，不是验证通过。
要获得 PASS，必须像上例显式加 `--validate-cmd-trace --validate-dfi-trace`。性能值只在
`remaining_requests=0`、`hit_cycle_limit=false`、退出码 0 的完整运行上解释。

#### 7.1.1 通用对齐查看速查

`less -S` 禁止长行自动折行，可用左右方向键水平滚动，按 `/text` 搜索，按 `q` 退出。
对交付产物统一建议：

| 文件类型 | 对齐/安全查看命令 | 说明 |
|---|---|---|
| stdout stats `.txt` | `less -S stats.txt`，再用 `rg '^KEY\s*:' stats.txt` | CLI 已使用固定 key 宽度，无需重新 column |
| 空白分隔 text | `column -t file.txt \| less -S` | 适合列数稳定的 trace/简单表；`key=value` 长行直接 `less -S` 更清楚 |
| CSV | `column -s, -t < file.csv \| less -S` | 本项目当前 CSV 字段不含嵌入逗号；超宽时先 `cut -d, -f...` 选列 |
| CSV 图形表格 | `libreoffice --calc file.csv` | 也可用 Excel/WPS；导入分隔符选逗号 |
| JSON | `python3 -m json.tool file.json \| less -S` | 先进行语法检查并缩进排版 |
| 二进制 `.bin/.meta` | `file FILE`; `stat FILE`; `xxd -g 1 -l 256 FILE \| less -S` | 不要 `cat`；只人工看 magic/头部，完整语义用模拟器重载/导出 |
| HTML | `xdg-open dashboard.html` | 无桌面时下载本地打开，或用 `python3 -m http.server` |
| 大文件 | `wc -l FILE`; `sed -n '1,20p' FILE`; `tail -n 20 FILE` | 先看规模和首尾，避免整个打印到终端 |

Markdown 表中的 `\|` 只是为了在文档单元格中显示 shell 管道，复制代码时实际命令使用普通 `|`。
`column -s,` 适用于本项目当前导出；如将来 CSV 字段允许引号内嵌逗号，应使用 Excel/WPS/
LibreOffice 或 Python `csv` 解析器，不要再用简单 `column -s,`。

### 7.2 可选产物

| 参数 | 产物 | 用途 |
|---|---|---|
| `--cmd-trace` | command CSV | 命令时间线、地址、stack/channel/bank |
| `--response-trace` | HostResponse CSV | host 聚合完成、状态、payload |
| `--dfi-trace` | DFI beat CSV | 命令/数据 beat、expected/actual |
| `--dfi-signal-trace` | signal-like CSV | DFI-oriented 信号事件审计 |
| `--dump-timing-table` | timing CSV | 最终值、单位、来源 |
| `--dump-memory-image` | txt/bin | 最终稀疏数据与 metadata |
| `--dump-memory-csv` | CSV | Excel/脚本查看数据状态 |
| `--mismatch-report` | text | expected/actual 和定位信息 |
| `--dump-thermal-map` | text | 稀疏热 grid/tile |

一个同时生成所有主要产物的实际命令如下。使用 `hbm4_storage.cfg` 是因为它已有 trace、
初始 memory image、floorplan、power 和 thermal，适合展示完整数据闭环：

```bash
mkdir -p outputs/full_artifacts
./build-clang-debug/hbm_sim --config configs/run/hbm4_storage.cfg \
  --cmd-trace outputs/full_artifacts/commands.csv \
  --response-trace outputs/full_artifacts/responses.csv \
  --dfi-trace outputs/full_artifacts/dfi.csv \
  --dfi-signal-trace outputs/full_artifacts/dfi_signal.csv \
  --dump-timing-table outputs/full_artifacts/timing.csv \
  --dump-memory-image outputs/full_artifacts/final_memory.txt \
  --dump-memory-csv outputs/full_artifacts/final_memory.csv \
  --mismatch-report outputs/full_artifacts/mismatch.txt \
  --dump-thermal-map outputs/full_artifacts/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  | tee outputs/full_artifacts/stats.txt
```

运行后按产物类型查看：

```bash
# 文件是否存在、规模是否合理
ls -lh outputs/full_artifacts
wc -l outputs/full_artifacts/commands.csv \
       outputs/full_artifacts/responses.csv \
       outputs/full_artifacts/dfi.csv

# 最终 timing 及来源
sed -n '1,20p' outputs/full_artifacts/timing.csv
rg 'timing_(source|vendor_required)' outputs/full_artifacts/stats.txt

# 命令、DFI、响应的表头和前几个事件
sed -n '1,8p' outputs/full_artifacts/commands.csv
sed -n '1,8p' outputs/full_artifacts/dfi.csv
sed -n '1,8p' outputs/full_artifacts/responses.csv

# 数据正确性、最终内容和热点
rg '^(data_|golden_|ecc_|cmd_validation|dfi_validation)' outputs/full_artifacts/stats.txt
sed -n '1,20p' outputs/full_artifacts/mismatch.txt
sed -n '1,20p' outputs/full_artifacts/final_memory.txt
sed -n '1,20p' outputs/full_artifacts/thermal_map.txt
```

预计效果：命令 CSV 至少含 ACT/RD/WR/PRE，DFI CSV 含 command/data beat，response CSV
按 host 请求聚合，timing CSV 显示最终 nCK 和来源，memory dump 反映 trace 写入后的数据，
`cmd_validation` 与 `dfi_validation` 应为 PASS，非故障注入用例的 `data_mismatches` 应为 0。

完整 command/DFI trace 可能非常大，只应对有限窗口启用。性能长跑优先保留统计、性能 JSON
和抽样 trace；若必须保留全量 trace，应同时估算磁盘空间和后处理时间。

### 7.3 任意仿真的离线可视化

#### 7.3.1 先确认可视化工具本身可用

```bash
make visualize-example
xdg-open outputs/visualization/example/dashboard.html
```

该入口使用项目固定示例，只用于确认 Python 生成器、HTML 和浏览器工作正常。真正分析自己的
仿真时，不应反复查看 example，而应把本次仿真的产物显式传给 `tools/visualize.py`。

#### 7.3.2 为任意仿真准备独立产物目录

下面的模板适用于任意 `--config`；只需修改 `SIM_OUT` 和配置文件：

```bash
SIM_OUT=outputs/visualization/my_experiment
mkdir -p "$SIM_OUT"

./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg --requests 512 \
  --floorplan true --power-model true --thermal-model true \
  --cmd-trace "$SIM_OUT/commands.csv" \
  --dfi-trace "$SIM_OUT/dfi.csv" \
  --dfi-signal-trace "$SIM_OUT/dfi_signal.csv" \
  --dump-thermal-map "$SIM_OUT/thermal_map.txt" \
  --validate-cmd-trace --validate-dfi-trace \
  | tee "$SIM_OUT/stats.txt"
```

其中 command trace 是必需输入；DFI、stats、thermal map 和 performance JSON 都是可选
增强项。若采用 Direct PHY，DFI 面板可能比 Behavioral 少；若关闭 thermal，热图为空是
正确结果，不是 dashboard 故障。

#### 7.3.3 可选：生成性能曲线

性能曲线脚本执行一组确定性的标准/注入间隔 sweep，它不是单次 trace 的时间线。只有在
dashboard 需要同时展示饱和趋势时才生成：

```bash
python3 tools/performance_curve.py \
  --binary ./build-clang-debug/hbm_sim \
  --standards hbm4 \
  --intervals 1,2,4,8,16 \
  --read-ratios 50,100 \
  --requests 512 --pattern random --seed 2026 \
  --csv-out "$SIM_OUT/performance.csv" \
  --json-out "$SIM_OUT/performance.json"
```

预计得到多组 `offered_requests_per_tick`、`achieved_bw_GBps` 和
`avg_read_latency_ticks`。若只分析自定义 vendor profile，当前脚本的标准入口不自动继承
该 profile，应把性能曲线作为标准基线，或另行扩展脚本后再声称是自定义器件曲线。

#### 7.3.4 生成自包含 HTML

```bash
python3 tools/visualize.py \
  --command-trace "$SIM_OUT/commands.csv" \
  --dfi-trace "$SIM_OUT/dfi.csv" \
  --stats "$SIM_OUT/stats.txt" \
  --performance-json "$SIM_OUT/performance.json" \
  --thermal-map "$SIM_OUT/thermal_map.txt" \
  --title 'HBM4 custom experiment' \
  --max-events 50000 \
  --out "$SIM_OUT/dashboard.html"

xdg-open "$SIM_OUT/dashboard.html"
```

`dashboard.html` 已嵌入采样事件和统计，无需 HTTP 服务，可复制到另一台机器直接打开。
`--max-events` 限制嵌入 HTML 的 command event 数；页面会注明显示全量还是确定性采样。

#### 7.3.5 页面操作方法

1. 在 **Run summary** 先核对 standard、PHY 模式、完成量、系统周期和 validation；身份不对
   时不要继续分析图。
2. 在 **Trace explorer** 先点击 overview 中的密集区域缩小周期范围，再用 Start/End 精确
   设定窗口；输入框清空表示恢复数据实际范围。
3. 用 Stack、Channel、Command 过滤器隔离目标位置；多 Stack 用例先比较各 stack，再观察
   单个 channel。`Lanes` 控制同时显示的 bank/request 行数。
4. **Command / bank view** 用于看 ACT→RD/WR→PRE、refresh/RFM 插入和 bank 并行；
   **Request swimlanes** 用于看一个 host request 从首命令到完成相关命令的生命周期。
5. 鼠标点击彩色事件，底部详情会显示 cycle、request、stack、channel、PC、SID、BG、bank、
   row 和 bus；键盘 `+`/`-` 可缩放当前周期窗口。
6. **Open command CSV** 可替换为另一轮 command trace，快速比较时间线；替换后 DFI/统计仍
   属于原 HTML，因此只用它比较命令，不要混合解释其他面板。
7. 在 **Command mix** 检查 ACT/PRE 与 RD/WR 比例；ACT/PRE 过多通常表示行冲突或 closed-page，
   refresh/RFM 比例异常则检查维护策略和运行长度。
8. 在 **DFI activity** 核对命令事件、读写 beat 和 payload byte 是否与 transaction 大小匹配。
9. 在 **Injection-rate / latency / throughput** 看实线吞吐是否趋于饱和、虚线延迟是否在饱和点
   附近快速上升；延迟使用独立比例尺，不能直接和 GB/s 的纵坐标比较。
10. 在 **Thermal map** 选择 layer，寻找温度集中区域。颜色对应 thermal grid 的绝对温度；
    `x/y` 是热网格坐标，不是 floorplan tile 坐标。

#### 7.3.6 从图得到可审计结论

| 观察 | 可能含义 | 必须回查的证据 |
|---|---|---|
| 同一 bank ACT/PRE 很密集 | 行冲突或关闭页策略 | `row_hits/misses/conflicts`、地址映射 |
| RD/WR 切换后出现空窗 | 总线换向 timing | `nRTW/nWTRS/nWTRL`、command CSV |
| 某 stack 事件显著更多 | stack mapping/QoS 不均衡 | `stack_N_*`、system/local address |
| PHY 面板有反压 | FIFO/pipeline 或低功耗状态阻塞 | `phy_*fifo_peak`、backpressure、PHY 状态 |
| 吞吐饱和且延迟陡增 | 系统进入排队饱和区 | injection stall、queue average、完成率 |
| 热点与命令热点重合 | 访问局部性和命令能量集中 | thermal map、bank lane、power 分项 |

可视化是定位和解释工具，不单独构成正确性证据；结论必须回到统计字段、CSV 原始事件和验证器。

#### 7.3.7 空白或异常页面排查

```bash
# 必需 trace 是否非空、表头是否正确
test -s "$SIM_OUT/commands.csv" && sed -n '1,3p' "$SIM_OUT/commands.csv"

# 可选输入是否真实存在
ls -lh "$SIM_OUT/stats.txt" "$SIM_OUT/dfi.csv" "$SIM_OUT/thermal_map.txt"

# HTML 是否生成并嵌入数据
test -s "$SIM_OUT/dashboard.html"
rg 'const DATA=' "$SIM_OUT/dashboard.html"

# 重新生成时先看工具给出的 timeline=嵌入数/总数
python3 tools/visualize.py --command-trace "$SIM_OUT/commands.csv" \
  --stats "$SIM_OUT/stats.txt" --out "$SIM_OUT/dashboard_debug.html"
```

若 Run summary 有值但时间线空白，检查 Start/End、Stack/Channel/Command 过滤器；若只有
曲线或热图空白，检查是否分别提供了 performance JSON 或启用了 thermal；若浏览器仍报错，
打开开发者工具 Console 检查本地文件权限、损坏 CSV 或旧页面缓存。

---

## 8. 平台与模型验证

### 8.1 当前交付物完成度

当前交付已经达到“可独立构建、可运行四标准、可进行多 Stack/PHY/真实数据实验、可导出并
验证产物”的软件研究模型阶段；尚未达到“指定厂家器件的电气级、功耗温度绝对值级校准”阶段。

| 交付面 | 当前已经实现 | 自动/可重放证据 | 当前成熟度与剩余边界 |
|---|---|---|---|
| 构建与库 | C++20 core library、CLI、Clang/G++、CMake/Ninja、Make | CMake build、CTest | 软件交付完整；无第三方 C++ 运行依赖 |
| 四标准入口 | HBM3/HBM4/LPDDR5/LPDDR6 traits、profile、配置、CLI | smoke、model validation | 入口和公共行为可回归；部分参数是研究基线 |
| Frontend | synthetic、trace、burst、payload/mask/expect、流式生成 | sequence/model tests | 已实现；不直接模拟 CPU cache/coherence |
| 多 Stack 平台 | system/local 地址、interleaved/blocked、ingress、QoS、per-stack 统计 | sequence/model tests、`hbm4_6stack.cfg` | 主动多 Stack 已实现；外部 NoC/UCIe 协议需适配 |
| Controller | FCFS/FR-FCFS、行策略、状态机、表驱动 timing、刷新/RFM/低功耗 | sequence、timing boundary、validator | 命令级研究可用；器件私有策略需校准 |
| Mem PHY | Direct/Behavioral、生命周期、FIFO、pipeline、HBM/LPDDR adapter、DFI event | `phy_tests`、`phy_smoke`、DFI validator | 行为级完整；不是 pin/electrical PHY |
| Mem Stack | per-stack MemoryImage、row buffer、payload、mask/init、物理坐标 | model/sequence tests | 数据和结构语义已闭环；非晶体管阵列模型 |
| 存储后端 | sparse、mmap_sparse、chunk_file、持久 sidecar | model validation | 三后端语义已实现；宿主 I/O 不代表 DRAM 时延 |
| 数据正确性 | 写后读、expect、golden、mismatch、SECDED shadow | sequence/model tests | 研究级数据闭环；ECC/RAS 非完整硅实现 |
| 功耗与热 | configured pJ、IDD 公式、稀疏 3D RC、TSV coupling | model validation、DRAMsim3 辅助工具 | 趋势可用；绝对值须目标器件/封装校准 |
| 输出与可视化 | stats、command/response/DFI、timing、memory、thermal、dashboard | visualization smoke | 离线审计链已实现；不含实时 Web 服务 |
| 外部验证 | Ramulator2 差分、DRAMsim3 辅助、性能曲线、敏感性区间 | Python tools | 方法和工具已交付；外部结果取决于本地参考模型 |

当前 CMake 定义 10 个回归入口：

| CTest 名称 | 主要证明内容 |
|---|---|
| `sequence_tests` | 命令序列、数据、请求响应、多 Stack 等 C++ 契约 |
| `timing_boundary_tests` | 四标准核心约束的 `t-1/t` 边界 |
| `phy_tests` | PHY 生命周期、FIFO、adapter 和数据完成 |
| `phy_smoke` | CLI Behavioral PHY 路径 |
| `visualization_smoke` | 实际 CLI 产物可被 dashboard 生成器消费 |
| `smoke` | 四标准及主要 CLI 功能集合 |
| `timing_boundary_validation` | C++ probe 的边界矩阵审计 |
| `model_validation` | 项目身份、数据、后端、DFI、物理/功耗/热和门槛 |
| `performance_curve_validation` | CI 规模注入率—延迟—吞吐曲线 |
| `sensitivity_uncertainty_validation` | Timing 敏感性和联合扰动区间 |

本次文档整理时的当前工作树验证快照为：

```text
Test project: build-clang-debug
Total Tests: 10
Result: 100% tests passed, 0 tests failed out of 10
```

该快照证明当时构建产物的默认回归通过，不替代后续 commit 的重新测试。最终交付应执行
下一节命令并保存实际日志。完成度可概括为：

```text
软件工程与可运行性        已完成并有默认回归
周期/命令/事务数据模型    已完成研究级实现
多 Stack + Behavioral PHY 已完成研究级实现
可重放输出与离线可视化    已完成
开源参考差分方法与工具    已完成，结果需本地参考仓库
指定厂商器件参数校准      未完成
真实电气 PHY/封装 SI      不在当前模型范围
硬件级绝对功耗/温度校准   未完成
```

### 8.2 可信度证据链

验证采用分层证据：

1. **编译与静态质量**：Clang/G++、warning、clang-tidy；
2. **单元和序列测试**：地址、状态、命令、数据、response、PHY；
3. **Timing 边界**：每个核心约束 `t-1` 禁止、`t` 允许；
4. **内部闭环**：写后读、mask、expect、golden、ECC、命令和 DFI validator；
5. **不变量**：完成数、容量、地址范围、系统/stack 聚合关系、后端一致性；
6. **外部差分**：Ramulator2.1 的共同命令/时序面；
7. **辅助交叉验证**：DRAMsim3 的较老共同协议、IDD 功耗公式和热趋势；
8. **性能曲线**：注入率—延迟—吞吐饱和趋势；
9. **敏感性/不确定性**：Timing 单因素及联合扰动；
10. **最终校准**：厂家模型、RTL、FPGA 或硬件测量。

证据强度从“软件是否按设计运行”逐步提升到“是否能代表目标器件”。默认回归可以证明当前
实现没有破坏已覆盖行为，但只有目标厂家模型或硬件数据才能完成器件级数值校准。

### 8.3 默认回归

```bash
cmake --build build-clang-debug --parallel
ctest --test-dir build-clang-debug --output-on-failure
```

CTest 条目以当前 `CMakeLists.txt`/`ctest -N` 为准，不在文档中固定编译步骤数量。
交付时必须保存实际通过/失败结果，不能用历史截图替代当前工作树测试。

### 8.4 原生验收

```bash
python3 tools/model_validation.py \
  --binary ./build-clang-debug/hbm_sim \
  --json-out outputs/model_validation.json

python3 tools/timing_boundary_validation.py \
  --binary ./build-clang-debug/hbm_sim
```

原生验收检查四标准入口、命令序列、数据、full-stack、DFI、物理/功耗/热、来源和项目身份。

### 8.5 Ramulator2 差分

```bash
python3 tools/ramulator2_differential.py \
  --binary ./build-clang-debug/hbm_sim \
  --ramulator-root /home/guxing/learn/memorystack/ramulator2 \
  --json-out outputs/ramulator2_diff.json
```

差分是“同 workload、同组织、同 timing、同统计口径下比较共同可观察行为”的方法。
它适合发现命令间隔、地址解码、same/different BG、总线换向、tFAW、refresh/RFM、
自动预充等错误。对方不支持的 HBM4/LPDDR6 行为必须标为 unsupported/项目扩展，不能
为了数值一致而删除本项目协议特性。

### 8.6 性能曲线和不确定性

```bash
python3 tools/performance_curve.py \
  --binary ./build-clang-debug/hbm_sim \
  --standards hbm4,lpddr6 \
  --json-out outputs/performance_curve.json \
  --csv-out outputs/performance_curve.csv

python3 tools/sensitivity_uncertainty.py \
  --binary ./build-clang-debug/hbm_sim \
  --json-out outputs/sensitivity.json
```

合理曲线通常随注入率上升经历低负载、吞吐增长、排队延迟陡增和饱和。异常零曲线应先
排查请求未注入、cycle limit、完成口径或前端反压，而不是直接调整 timing。

### 8.7 可行性结论的表述

可以声称：

- 软件结构、状态机、数据闭环和回归在已覆盖场景下可复核；
- 与外部开源模型的共同抽象可做差分；
- 模型适合架构趋势、调度、带宽/延迟和相对功耗热研究。

不应声称：

- 已等价于某家 HBM4/LPDDR6 量产器件；
- Behavioral PHY 是真实 pin-level/electrical PHY；
- 仅凭 Ramulator/DRAMsim 一致就完成 JEDEC 合规认证；
- 未经校准的绝对功耗、结温或错误率具有器件级精度。

---

## 9. 审计与完成情况

### 9.1 来源分级

| 等级 | 含义 | 交付写法 |
|---|---|---|
| JEDEC/DFI | 公开标准可核对 | 写明标准号、版本和字段 |
| vendor | 厂家 datasheet/model | 写明器件、speed-bin、版本 |
| external | Ramulator2/DRAMsim3 等 | 写明仓库版本和规范化方式 |
| derived | 由容量/频率/总线计算 | 给出公式和输入 |
| research_default | 缺少器件依据的假设 | 明确标注并做敏感性区间 |

### 9.2 当前已完成

- 四标准统一 CLI、配置、traits/profile/finalize；
- 主动多 stack、per-stack ingress/QoS 和 `[stack][channel]` Controller/PHY；
- FCFS/FR-FCFS、行策略、bank 状态、TimingEngine、refresh/RFM/低功耗；
- Behavioral PHY、HBM/LPDDR adapter、DFI-oriented 输出；
- 三种真实 payload 后端、数据校验、物理坐标、功耗热和 ECC shadow；
- command/DFI/response/memory/thermal 输出和离线可视化；
- 原生验证、Timing 边界、差分、性能曲线、敏感性工具。

### 9.3 尚需器件级工作

- 目标厂商 HBM3/HBM4/LPDDR5/LPDDR6 的完整 speed-bin timing；
- PHY training、电气、封装、链路错误率和真实 DFI/RTL 端口校准；
- IDD/电压、命令能量、floorplan、TSV、热阻热容和散热边界；
- RFM/PRAC、RAS、on-die ECC/link ECC 的器件实现细节；
- 厂家模型或硬件带宽、延迟、功耗和温度数据的最终拟合与误差区间。

上述来源分级、完成矩阵、验证方法和限制共同构成本交付的审计结论；无需依赖另一份专题文档。

---

## 附录 A：配置参数说明

参数可写入 `key=value` 配置；CLI 支持的字段可在后续覆盖。布尔值使用 `true/false`。
Timing 支持 nCK 以及允许的 ns/us 别名，最终均应通过 timing table 核对。

### A.1 实验和输入

| 配置键/CLI | 含义 | 可否自定义/注意事项 |
|---|---|---|
| `standard` / `--standard` | hbm3/hbm4/lpddr5/lpddr6 | 可；会改变默认 traits/profile |
| `pattern` | stream/random | 可；trace 存在时由 trace 驱动 |
| `trace` / `trace_path` | trace 文件 | 可；相对路径以运行目录解析 |
| `requests` | host 请求上限；trace 中 0 为全部 | 可 |
| `read_ratio` | 合成读比例，百分数 | 可，0–100 |
| `seed` | 随机种子 | 可；可复现必须保存 |
| `addr_stride` | 合成地址步长，byte | 可；建议 transaction/line 对齐 |
| `inject_interval` | 前端注入间隔，controller tick | 可；性能曲线的主自变量 |
| `init_sequence` | none/auto/hbm4/lpddr6/lpddr6_full | 可；必须与标准匹配 |
| `init_sequence_interval` | 控制序列命令间隔 | 可 |
| `max_cycles` | 系统周期上限 | 可；命中上限不代表正常完成 |

### A.2 Profile、组织与时钟

| 配置键 | 含义 | 替换规则 |
|---|---|---|
| `timing_profile` | 内置 profile 名 | 选择已注册名称 |
| `timing_profile_file` | 外部 profile 片段 | 推荐自定义器件时使用 |
| `vendor_profile` | 厂家/器件身份标签 | 非 generic 才可声称 vendor 校准 |
| `mode_profile` | 标准模式片段 | 与协议能力匹配 |
| `timing_override_source` | vendor/jedec/research_default/derived | timing 覆盖前设置来源 |
| `speed_bin_mbps` | pin data rate | 可；必须重新核对 tCK 和所有派生 timing |
| `density_gb` | die density | 可；须与 rows/columns/banks/宽度一致 |
| `stack_height` | die 层数 | 可；影响容量和物理映射 |
| `strict_timing_table` | 禁止未校准的必要默认值 | 正式交付建议 true |
| `data_rate_mbps` | 数据率 | 可；通常由 profile 提供 |
| `data_bus_bits` | 数据总线位宽 | 可；必须匹配标准/full-stack 口径 |
| `prefetch_size` | 内部预取/BL 相关组织 | 谨慎；标准相关 |
| `line_size` | host line byte | 可；可拆 transaction |
| `dram_transaction_bytes` | 单 RD/WR payload byte | 可；必须满足 burst/总线组织 |
| `tck_ps` | 时钟周期 ps | 谨慎；应由速度档推导 |
| `tick_multiplier` | 模拟 tick 与 CK 比例 | 谨慎；影响所有 nCK 解释 |
| `channels` | 每 stack channel 数 | 可；需与 profile/容量一致 |
| `pseudo_channels` | 每 channel PC 数 | 标准相关 |
| `sids` | stack/SID 组织维度 | HBM 相关 |
| `ranks` | rank 数 | 可，需重算容量 |
| `bank_groups` | BG 数 | 标准相关 |
| `banks_per_group` | 每 BG bank 数 | 标准相关 |
| `rows`、`columns` | bank 行列数 | 可，需与密度/transaction 对齐 |
| `full_stack_model` | 使用完整 stack 组织口径 | 可；不等于 stack_count>1 |

### A.3 Controller、映射和多 Stack

| 配置键 | 含义 | 可否自定义/注意事项 |
|---|---|---|
| `read_buffer_size`、`write_buffer_size` | 读写队列深度 | 可；影响反压/排队 |
| `priority_buffer_size` | 维护/控制队列深度 | 可 |
| `write_high_watermark`、`write_low_watermark` | 写模式切换水位 | 可；低值≤高值 |
| `scheduler` | fcfs/frfcfs | 可 |
| `row_policy` | open_page/closed_page/closed_cap | 可 |
| `row_policy_cap` | closed_cap 列访问上限 | 可 |
| `addr_mapping` | default/RoBaRaCoCh/ChRaBaRoCo/RoCoRaBaCh | 可；改变局部性 |
| `channel_mapper` | decoded/round_robin/xor | 可；公平比较需固定 |
| `single_controller` | 旧单控制器路径 | 仅兼容/对照 |
| `stack_count` | 独立 stack 数 | 可；主动模型真正复制端点和 image |
| `stack_mapping` | interleaved/blocked | 可 |
| `stack_interleave_bytes` | stack 条带大小 | 可；必须为 line_size 整数倍 |
| `stack_ingress_buffer_size` | 每 stack 系统入口深度 | 可 |
| `stack_dispatch_width` | 每 stack 每拍分发上限 | 可 |
| `stack_qos_policy` | fcfs/strict_priority | 可；高数字为高优先级 |

### A.4 PHY/DFI

| 配置键 | 含义 | 可否自定义/注意事项 |
|---|---|---|
| `mem_phy_mode` / `phy_mode` | direct/behavioral | 可；正式 full-stack 用 behavioral |
| `dfi_version` | DFI 目标标签 | 可；不自动获得合规能力 |
| `phy_command_fifo_depth` | 命令 FIFO 深度 | 可；与 command pipeline/吞吐联合解释 |
| `phy_write_fifo_depth` | 写数据 FIFO 深度 | 可 |
| `phy_read_fifo_depth` | 读返回 FIFO 深度 | 可 |
| `command_pipeline_cycles` | 命令流水延迟 | 可 |
| `write_pipeline_cycles` | 写数据流水延迟 | 可 |
| `read_pipeline_cycles` | 读返回流水延迟 | 可 |
| `phy_reset_cycles` | reset 状态持续 | 可，需校准 |
| `phy_init_cycles` | init 状态持续 | 可，需校准 |
| `phy_training_cycles` | training 状态持续 | 可，需校准 |
| `phy_auto_train` | 自动进入训练并 Ready | 可 |
| `dfi_phase_count` | DFI beat phase 数 | 0 表示派生 |
| `dfi_data_lane_bytes` | 每 beat byte | 0 表示派生 |
| `dfi_read_latency_nck`、`dfi_write_latency_nck` | DFI 读写延迟 | 可，需 PHY/profile 依据 |

配置文件还接受以下语义更明确的长名称；它们与上表的运行时字段指向同一类参数，推荐
profile 保持现有写法，不要在同一文件混用长短名称：

| 配置键 | 含义 |
|---|---|
| `phy_command_pipeline_cycles` | command pipeline 周期 |
| `phy_write_data_pipeline_cycles` | write-data pipeline 周期 |
| `phy_read_return_pipeline_cycles` | read-return pipeline 周期 |
| `phy_initialization_cycles` | PHY initialization 周期 |

### A.5 Refresh、RFM、低功耗和协议模式

| 配置键 | 含义 | 说明 |
|---|---|---|
| `refresh_policy` | per_bank/all_bank | 选择 REFpb/REFab 维护策略 |
| `refresh_temperature` | normal/high/extended | 刷新温度模式 |
| `refresh_postpone_limit` | 最大延后 | 策略参数 |
| `refresh_pullin_limit` | 最大提前 | 策略参数 |
| `refresh_credit_limit` | refresh credit 上限 | 策略参数 |
| `rfm_policy` | per_bank/all_bank | RFM 作用域 |
| `rfm_act_threshold` | ACT 累计触发阈值 | 研究/器件参数，需校准 |
| `rfm_decrement` | RFM 后计数下降量 | 研究/器件参数，需校准 |
| `low_power` | off/power_down/self_refresh | 低功耗策略 |
| `low_power_entry_cycles` | 空闲进入阈值 | 控制器策略 |
| `low_power_exit_cycles` | power-down 退出 | 需 profile 依据 |
| `self_refresh_exit_cycles` | self-refresh 退出 | 需 profile 依据 |
| `lpddr_efficiency_mode` | normal/static/dynamic | LPDDR 模式 |
| `lpddr_dvfs_mode` | nominal/low/disabled | LPDDR DVFS |
| `lpddr_wck_mode` | cas_sync/always_on/burst_sync | LPDDR WCK |
| `hbm_link_crc`、`hbm_link_crc_bits` | HBM CRC 模式/开销 | HBM 专用 |
| `hbm_ras_metadata_bits`、`hbm_ecc_bits` | HBM 接口 metadata | HBM 专用 |
| `hbm_link_retry` | HBM retry 入口 | 行为级 |
| `lpddr_link_protection` | LPDDR link protection | LPDDR 专用 |
| `lpddr_dbi`、`lpddr_dbi_bits` | DBI 及开销 | LPDDR 专用 |
| `lpddr_link_ecc`、`lpddr_link_ecc_bits` | link ECC | LPDDR 专用 |
| `lpddr_ca_parity`、`lpddr_ca_parity_bits` | CA parity | LPDDR 专用 |
| `metadata_bits_per_request`、`ecc_bits_per_request` | 通用接口开销 | 影响 interface bandwidth |

能力开关和 profile 专用字段如下。它们可以自定义，但多数是标准/profile 身份的一部分，
用于对照实验时也必须记录，不能只记录 timing：

| 配置键 | 含义/允许口径 |
|---|---|
| `supports_refresh`、`supports_rfm`、`supports_ecc` | refresh、RFM/PRAC、ECC 能力总开关 |
| `refresh_temperature_mode` | `refresh_temperature` 的 profile 长名称 |
| `low_power_mode` | `low_power` 的 profile 长名称 |
| `hbm_full_32_channel_stack` | 是否采用 HBM4 32-channel full-stack 组织 |
| `hbm_sid_interleave`、`hbm_pc_interleave` | SID、pseudo-channel 是否参与地址 interleave |
| `hbm_edge_pairing`、`hbm_strict_edge_pairing` | 是否启用普通/严格 HBM 边沿配对 |
| `hbm_edge_pairing_matrix` | pairing 规则标签；真正约束由 Controller/validator 执行 |
| `hbm_sid_mapping` | SID 映射策略标签 |
| `hbm_ras_policy` | HBM RAS 行为策略标签 |
| `hbm_link_crc_mode` | HBM CRC 模式长名称 |
| `hbm_link_crc_bits_per_request` | 每请求 CRC 开销 bit |
| `hbm_ras_metadata_bits_per_request` | 每请求 RAS metadata bit |
| `hbm_ecc_scheme` | HBM ECC 方案标签 |
| `hbm_ecc_bits_per_request` | 每请求 HBM ECC bit |
| `hbm_link_retry_enabled` | HBM link retry 能力开关 |
| `lpddr_dual_bank_refresh` | 是否启用 LPDDR6 REFdb dual-bank refresh |
| `lpddr_wck_ratio` | WCK:CK 项目内部比率；须按目标标准模式校准 |
| `lpddr_wck_training_mode` | startup/dvfs/cas-sync 等训练策略标签 |
| `lpddr_wck_training_required` | 启动或 DVFS 后是否要求 WCK training |
| `lpddr_dvfs_transition_policy` | DVFS 转换许可策略标签 |
| `lpddr_low_data_rate_mbps` | 低速 DVFS 分支数据率 |
| `lpddr_low_power_state_policy` | 低功耗状态策略标签 |
| `lpddr_mode_register_profile` | mode register profile 标签 |
| `lpddr_link_protection_mode` | link protection 模式长名称 |
| `lpddr_dbi_enabled`、`lpddr_dbi_bits_per_request` | DBI 开关与每请求开销 |
| `lpddr_link_ecc_enabled`、`lpddr_link_ecc_bits_per_request` | link ECC 开关与每请求开销 |
| `lpddr_ca_parity_enabled`、`lpddr_ca_parity_bits_per_command` | CA parity 开关与每命令开销 |

`address_mapping` 是 `addr_mapping` 的配置文件别名。配置片段中的 `note` 是自由文本来源/
假设说明，不进入运行时数值计算，但正式 profile 应填写；它不能代替
`timing_override_source` 的机器可读来源标签。

### A.6 Timing 参数

以下 canonical key 均可由 profile 或配置覆盖。这里的解释同时给出 JEDEC 风格含义和
项目当前真正使用它的方式；两者不完全相同时，以“项目实现”列为准。

#### A.6.1 本节的 JEDEC 参考依据和限制

| 参考 | 可直接支持的内容 | 本手册的使用方法 |
|---|---|---|
| `HBM4 JESD270-4A_dual_Kimi+DeepSeek.pdf`，标准第 5–7 页 | pseudo-channel 独立 timing 作用域、双命令接口、same/different BG 命令序列 | 确定 `nRRDS/nRRDL`、`nCCDS/nCCDL/nCCDR`、行/列总线和 PC 作用域 |
| `HBM4 JESD270-4A_dual_Kimi+DeepSeek.pdf`，§10、表 107/108，标准第 190–198 页 | CK、行、列、refresh、低功耗及 AC timing 注释 | 确定参数定义、单位、适用范围和需要 vendor datasheet 的字段 |
| `jedec JESD209-6：2025 LPDDR6 Standard ）_dual_Kimi+DeepSeek.pdf`，标准第 210–218 页 | ACTIVATE-1/2、`tAAD`、`tRRD`、`tFAW`、REFdb 窗口、`tRPab/tRPpb`、RDA/WRA | 核对 split ACT、滚动激活窗口和自动预充的行为语义 |

用户提供的两份 PDF 是 BabelDOC 生成的 AI 双语版。本手册只把英文原文栏和标准页码
作为引用依据，中文栏只用于辅助阅读。LPDDR6 该 PDF 的实际正文截止于标准第 218 页；
目录虽列出第 450 页开始的 Core AC Timing 表 413–423，但 PDF 内没有这些正文页。
因此，LPDDR6 的 RL/WL、WCK 和核心 AC **数值**仍必须由完整 JESD209-6 原文和目标器件资料
补齐；不能从目录、译文或当前 research profile 反推为 JEDEC 固定值。

JESD270-4A 表 108 注 1 说明 AC timing 按 channel 独立；注 2 说明表中 speed bin 是示例，
vendor 可定义不同档位；注 17 明确 `tCCDR` 的最小值与频率是 vendor-specific。
所以“JEDEC 定义了参数语义”不等于“JEDEC 给出了所有目标器件数值”。

#### A.6.2 单位、换算和读取规则

- `n*` 字段均以 nCK 保存；Controller 内部等待 tick 数为 `nCK × tick_multiplier`；
- `tCK_ps` 是一个 CK 周期的皮秒数；`ns` 输入换算为
  `ceil(ns × 1000 / tCK_ps)`，`us` 输入换算为 `ceil(us × 1,000,000 / tCK_ps)`；
- “最短间隔”表示前一命令在 cycle `t` 发出后，后一命令最早在 `t + 参数` 发出；
- `nREFI/nREFIpb` 是周期性维护调度间隔，其他大多数参数是命令合法性/完成延迟；
- profile、配置和 CLI 全部覆盖结束后才执行 `finalize_spec()`；最终值必须通过
  `--dump-timing-table` 查看，不能只读某一份 profile；
- 来源应标为 `jedec`、`vendor`、`derived` 或 `research_default`。只要目标器件依赖项仍是
  research default，就不能声称器件级校准完成。

#### A.6.3 时钟、Burst 和 CAS 延迟

| 参数 | JEDEC 风格含义 | 项目当前实现/公式 | 适用与校准影响 |
|---|---|---|---|
| `tCK_ps` | CK 周期，所有基于时间的约束换算基准 | ns/us 覆盖先按该值向上换算；调度再乘 `tick_multiplier` | 改速度档必须先改它，再重新 finalize 全部时间项；错误会成比例影响所有延迟和带宽时间基准 |
| `nBL` | burst 占用的 CK 数/列总线数据窗口 | 参与 `read_latency=nCL+nBL`、写恢复 `nCWL+nBL+nWR`、同类 RD/WR 间隔和 DFI beat 完成；不逐 beat 模拟 DQ 电气波形 | 四标准都使用；它是项目调度占用量，不应直接当作 JEDEC 文本中的 BL 编码值 |
| `nCL` | Read Latency/CAS read latency：读命令到首批有效读数据 | 读完成简化为 `nCL+nBL`；LPDDR RD→WR 还使用 `nCL+nBL+2-nCWL` | speed-bin/mode/vendor 相关；增大通常提高读延迟并拉长读写换向 |
| `nCWL` | Write Latency/CAS write latency：写命令到写数据 | WR→PRE 使用 `nCWL+nBL+nWR`；WR→RD 使用 `nCWL+nBL+nWTRS`；PHY 默认写延迟也可由它派生 | speed-bin/mode/vendor 相关；影响写完成后可预充和总线换向 |

#### A.6.4 激活、保持和预充

| 参数 | JEDEC 风格含义 | 项目当前实现/作用域 | 校准影响 |
|---|---|---|---|
| `nRCDRD` | ACT 到 RD 的最短 RAS-to-CAS delay | 同一 bank 的 ACT/ACT1→RD/RDA；LPDDR split ACT 在 executor 中扣除已消耗的 `nAAD` | 过小会允许尚未完成行激活就读；过大会增加 row miss 读延迟 |
| `nRCDWR` | ACT 到 WR 的最短 delay | 同一 bank 的 ACT/ACT1→WR/WRA；LPDDR split ACT 同样扣除 `nAAD` | 主要影响 row miss 写延迟和写队列排空 |
| `nRP` | 单 bank PRE 到下一次 ACT 的 row precharge time；LPDDR6 文本对应 `tRPpb` | Bank scope PREpb→ACT/ACT1；bank 的 `next_act` gate 也由它更新 | 影响 row conflict 代价；不能用平均值替代目标密度/速度档表 |
| `nRPab` | all-bank PRE 到下一次行访问的恢复时间；JESD209-6 第 213 页明确 `tRPab > tRPpb` | PREab 后所有 bank 的 `next_act`；为 0 时回退到 `nRP` | 影响 all-bank refresh/模式切换前后的全局停顿 |
| `nRAS` | ACT 后该行必须保持激活的最短时间 | Bank scope ACT/ACT1→PREpb，阻止过早关闭行 | 必须与 `nRC/nRP` 一致；过小破坏行恢复，过大降低 closed-page 灵活性 |
| `nRC` | 同一 bank 两次 ACT 的最短 row cycle | Bank scope ACT/ACT1→下一次 ACT/ACT1；通常应满足接近 `nRAS+nRP` 的派生关系 | row conflict 的完整周期上限；配置后应检查派生一致性 |
| `nRTP` | RD 到 PRE 的最短 read-to-precharge | Bank scope RD→PREpb；自动预充也受该 gate 控制 | 影响 RDA/closed-page 和读后快速换行 |
| `nWR` | 最后写数据后到 PRE 的 write recovery | 项目组合约束为 `nCWL+nBL+nWR`，不是从 WR 命令单独等待 `nWR` | 写路径关键参数；错误会造成写数据未恢复即预充或不必要停顿 |

#### A.6.5 列命令间隔和读写换向

| 参数 | JEDEC 风格含义 | 项目当前实现/作用域 | 校准影响 |
|---|---|---|---|
| `nCCDS` | different bank group 的 short tCCD；JESD270-4A 表 6 用于跨 BG 的 WR→WR 和通常的 RD→RD | HBM Sid、LPDDR Rank scope 的 RD→RD、WR→WR 最短间隔 | 决定跨 BG 列并行度；数值越大，可持续列吞吐越低 |
| `nCCDL` | same bank group 的 long tCCD；JESD270-4A 表 6 用于同 BG 的 RD→RD/WR→WR | BankGroup scope 的 RD→RD、WR→WR | same-BG 压力场景的关键瓶颈，应与 `nCCDS` 分开验证 |
| `nCCDR` | HBM 跨 SID 连续读的特殊间隔；JESD270-4A 表 108 注 17 规定它在该场景替代 `tCCDS` | HBM PseudoChannel scope 对 RD→RD 保守使用 `max(nBL,nCCDR)`；当前未表达“仅 different SID”谓词 | 标准明确数值与频率为 vendor-specific；当前实现偏保守，差分时要记录作用域归一化 |
| `nRTW` | Read-to-Write bus turnaround | HBM PseudoChannel RD/RDA→WR/WRA 直接使用；LPDDR 使用 `nCL+nBL+2-nCWL` 的组合式 | 影响读写混合流量；纯读/纯写场景不敏感 |
| `nWTRS` | Write-to-Read short turnaround | HBM/LPDDR 当前主要组合为 `nCWL+nBL+nWTRS` | different-BG/短换向基线；增大会增加写切读空窗 |
| `nWTRL` | Write-to-Read long turnaround | 作为 same-BG/vendor 长路径字段进入审计表和输出；当前通用 constraint table 尚未独立使用它 | 必须保留用于标准/厂商表完整性；若研究 same-BG WTR 精度，需要补独立作用域约束后再验证 |

#### A.6.6 激活间隔和窗口

| 参数 | JEDEC 风格含义 | 项目当前实现/作用域 | 校准影响 |
|---|---|---|---|
| `nRRDS` | different BG 的短 ACT-to-ACT 间隔 | HBM PseudoChannel、LPDDR Rank scope 的连续 ACT/ACT1 | 控制跨 BG bank-level parallelism |
| `nRRDL` | same BG 的长 ACT-to-ACT 间隔 | BankGroup scope 的连续 ACT/ACT1；部分 RFM/REFdb fallback 也引用它 | same-BG 激活压力越高越敏感 |
| `nFAW` | Four Activate Window：滚动窗口内最多四个占用事件；HBM4 包含 ACT/PER BANK REFRESH，LPDDR6 包含 ACT/REFdb | TimingEngine 在 activation scope 记录 ACT/ACT1/REFpb/REFdb，第 5 个事件必须等最早事件离开窗口 | JESD270-4A 表 108 注 10 和 JESD209-6 第 212 页均明确“最多四个”；必须用窗口测试，不可化为普通相邻间隔 |
| `nAAD` | LPDDR6 的 ACT1→ACT2 **最大**间隔；JESD209-6 第 210 页规定 ACT2 须在 ACT1 后 8 nCK 内发出 | 当前模型把 `nAAD` 同时用作 `next_act2` 和 deadline，即把 ACT2 固定在该点强制发射；并从 ACT2→RD/WR 剩余 RCD 中扣除 | 这是当前已知的行为级近似，并未表达“ACT2 可在合法窗口内任意时刻发出”；做 LPDDR6 JEDEC 边界声明时必须单独披露 |

#### A.6.7 LPDDR WCK、CAS 和命令间隔

| 参数 | JEDEC 风格含义 | 项目当前实现 | 适用与边界 |
|---|---|---|---|
| `nWCK2CK` | WCK 与 CK 同步后到可使用 WCK/数据命令的等待 | CAS_RD/CAS_WR/WCK sync 后设置 `wck_ready_at`；always-on 模式通常为 0 | LPDDR5/6；影响按需 WCK 的首访问延迟 |
| `nWCKPST` | WCK postamble/同步有效窗口尾部 | 决定 `wck_active_until`，读写完成路径也把它叠加到 WCK 活跃窗口 | 当前是行为级窗口，不模拟真实波形 |
| `nCAS` | CAS_RD/CAS_WR 到实际 RD/WR 的间隔 | Bank scope CASRD→RD/RDA、CASWR→WR/WRA；并与 `nWCK2CK` 共同确定最早数据命令 | cas_sync/burst_sync 模式关键；always-on 可为 0 |
| `nCS` | chip-select/命令选择相关最短间隔 | 当前保留在 TimingTable、配置和审计输出中，尚未形成独立 command constraint | 若目标研究需要 CS bus 精度，应增加对应 scope/命令对测试，不能仅改数值 |
| `nPPD` | precharge-to-precharge/相关命令最小间隔的标准字段 | 当前保留在 TimingTable 和配置输出，PRE 的主要 gate 仍由 `nRP/nRPab` 表达 | 标准表完整性字段；尚不是独立调度约束 |
| `nWCKSYNC` | WCK sync 后到普通 RD/WR 的恢复 | PseudoChannel scope WCKSYNC→RD/WR | LPDDR 行为级同步检查，与 WCK ready window 双重约束 |
| `nWCKTRAIN` | WCK training 占用时间 | Channel scope WCKTRAIN→ACT1/CAS_RD/CAS_WR | 训练为行为级固定时延，真实训练算法/眼图不在模型内 |
| `nDVFS` | DVFS 转换完成时间 | Channel scope DVFS→ACT1/CAS/MRW | 改频后模式恢复/重训练入口；需要与 low/nominal profile 联合校准 |

#### A.6.8 Refresh、dual-bank refresh 和 RFM

| 参数 | JEDEC 风格含义 | 项目当前实现/作用域 | 校准影响 |
|---|---|---|---|
| `nRFC` | all-bank refresh cycle time | HBM PseudoChannel 或 LPDDR Rank 的 REFab→ACT/PRE/REF 恢复；RFMab 为 0 时也可回退使用 | 密度/温度相关；决定 all-bank 维护停顿 |
| `nRFCpb` | per-bank refresh cycle time | Bank scope REFpb/REFdb→ACT；RFMpb 为 0 时回退使用 | per-bank 刷新可与其他 bank 交错，作用域必须正确 |
| `nRFMab` | all-bank RFM 占用/恢复时间 | RFMab 后在 PseudoChannel/Rank scope 阻塞 ACT/PRE/RFMpb；为 0 时回退 `nRFC` | 仅启用 RFM 时有意义；阈值策略与时序应分开校准 |
| `nRFMpb` | per-bank RFM 占用/恢复时间 | Bank scope RFMpb→ACT；为 0 时回退 `nRFCpb` | 影响高 ACT 压力场景的维护损失 |
| `nRREFD` | refresh 到后续 ACT/refresh 的附加间隔 | HBM REFpb 在 PseudoChannel scope 到 ACT；LPDDR6 可作为 REFdb→ACT fallback | 不等同 `nRFCpb`；一个是共享 scope 间隔，一个是目标 bank 占用 |
| `nREFDB2ACT` | LPDDR6 dual-bank refresh 后到 ACT 的恢复 | PseudoChannel scope REFdb/RFMpb→ACT1；为 0 时依次回退 `nRREFD/nRRDS` | LPDDR6 专用，应从对应 REFdb 表获取 |
| `nREFDB2REFDBS` | REFdb 到下一次 short-pair REFdb 间隔 | Bank scope REFdb→REFdb 的短间隔 | 与 long 间隔分开配置，避免合并成一个平均值 |
| `nREFDB2REFDBL` | REFdb 到下一次 long-pair REFdb 间隔 | PseudoChannel scope REFdb→REFdb，当前 scheduler 保守使用 | 通常不小于 short；影响连续 dual-bank refresh 调度 |
| `nREFI` | all-bank refresh 的平均调度周期 | RefreshManager 在 all-bank policy 下生成 refresh 到期点，并受温度倍率影响 | 它不是 refresh 占用时长；减半会提高维护频率 |
| `nREFIpb` | per-bank/轮转 refresh 的调度周期 | per-bank policy 优先使用；为 0 时回退 `nREFI` | 与 bank rotation、stack height、温度模式相关 |

#### A.6.9 Mode、低功耗、ECC 和 RAS

| 参数 | JEDEC 风格含义 | 项目当前实现/作用域 | 校准边界 |
|---|---|---|---|
| `nMRW` | Mode Register Write 后的命令恢复 | Channel scope MRW→ACT/RD/WR/MRW/MRR 或 LPDDR CAS 命令 | 模式编程序列固定时延；具体 MR 内容不由该值表达 |
| `nMRR` | Mode Register Read 后的命令恢复 | Channel scope MRR→普通命令/MR 命令 | 读取返回数据的 pin 行为不在当前模型内 |
| `nPDEX` | power-down exit 到普通命令可用时间 | Channel scope PDE/PDX→ACT1/CAS；executor 在 PDX 后设置 row gate | Behavioral PHY 还要求显式 PDX；不能只改 Controller 空闲阈值 |
| `nSREFEX` | self-refresh exit 到普通访问/维护可用时间 | Channel scope SREFEN/SREFEX→ACT1/REFdb/RFMpb；通常不小于 `nPDEX` | 温度/器件相关，影响深度低功耗恢复 |
| `nECCSCRUB` | ECC scrub/repair 行为占用时间 | ECCSCRUB/RASERR 后与 `nRASERR` 取最大值，阻塞 Channel 内 ACT/refresh/RFM/MR | 厂商/RAS policy 相关；当前是行为级固定时间 |
| `nRASERR` | RAS error handling/recovery 占用时间 | 与 ECCSCRUB 共同形成 Channel control gate；启用 retry 时还关联读写恢复 | 不是错误发生概率，只是恢复路径时延 |
| `nLINKRETRY` | link retry/replay 后到普通数据命令恢复 | HBM RASERR→RD/WR 使用；配置 link protection/retry 时有效 | 厂商链路和训练相关；当前不模拟实际 flit 重放次数 |

#### A.6.10 参数之间的组合关系

项目实际检查的不只是单个字段，还包括组合式：

```text
读完成                  = nCL + nBL
WR → PRE               = nCWL + nBL + nWR
WR → RD                = nCWL + nBL + nWTRS
LPDDR RD → WR          = nCL + nBL + 2 - nCWL
HBM RD → RD/PC scope   = max(nBL, nCCDR)
ECC/RAS control gate   = max(nECCSCRUB, nRASERR)
LPDDR ACT2 → RD/WR     = max(1, nRCD* - nAAD)
```

因此做参数敏感性时一次只改变一个字段，也要说明它参与了哪些组合约束。`nRC`、`nRPab` 等
派生项应与基础项保持物理一致，不能分别填入互相矛盾的值。

#### A.6.11 单位别名和逐项替换方法

当前解析器支持的常用别名包括：`tCK_ps`、`tRCD_RD_ns`、`tRCD_WR_ns`、`tRP_ns`、
`tRPab_ns`、`tRAS_ns`、`tRC_ns`、`tRTP_ns`、`tWTP_ns/tWR_ns`、`tCCDS_ns`、
`tCCDL_ns`、`tRRD_S_ns`、`tRRD_L_ns`、`tFAW_ns`、`tWTR_S_ns`、`tWTR_L_ns`、
`tRTW_ns`、`tRFCab_ns`、`tRFCpb_ns`、`tRFMab_ns`、`tRFMpb_ns`、`tRREFD_ns`、
`tREFI_us`、`tREFIpb_ns/us`、`tREFIdb_ns`、`tREFDB2ACT_ns`、
`tREFDB2REFDB_S_ns`、`tREFDB2REFDB_L_ns`、`tWCKTRAIN_ns` 和 `tDVFS_ns`。

替换目标器件 Timing 的推荐流程：

1. 先确定 speed-bin、`tCK_ps`、density、stack height 和模式；
2. 复制一个相同标准的 profile，写清 `source`/`note`；
3. 先填 JEDEC 固定项和公式项，再填 vendor-dependent RL/WL/row/refresh/RAS；
4. 同一参数只使用 nCK 或一个时间单位版本，避免后写字段无意覆盖；
5. 运行 `--dump-timing-table` 检查值、来源、required/provisional；
6. 对每个被 Controller 使用的核心约束执行 `t-1` 禁止、`t` 允许；
7. 运行 same/different BG、读写换向、tFAW、refresh/RFM 和低功耗序列；
8. 最后才用 `--strict-timing-table` 作为器件数值配置门槛。

### A.7 存储、物理、功耗、热和 ECC

| 配置键 | 含义 | 说明 |
|---|---|---|
| `memory_backend` | sparse/mmap_sparse/chunk_file | 可替换后端 |
| `memory_capacity_bytes` | 后端逻辑容量 | 0 可按 spec 派生；文件后端建议显式 |
| `memory_data_file` | payload 文件 | 多 stack 支持 `{stack}` |
| `memory_init_file` | init bitmap 文件 | 可自定义路径 |
| `memory_meta_file` | metadata 文件 | 可自定义路径 |
| `memory_presence_file` | presence bitmap | 可自定义路径 |
| `memory_chunk_size` | chunk_file 块大小 | 性能参数，不改语义 |
| `memory_chunk_cache_entries` | chunk cache 数 | 控制宿主 RAM/I/O |
| `sparse_density_warning_pct` | 稀疏后端密度告警 | 只告警 |
| `topology_stats_scan_limit` | 详细坐标扫描上限 | 0 为无限制 |
| `floorplan` | 物理放置开关 | 开启坐标/热路径 |
| `subarrays_per_bank` | 每 bank subarray 数 | 器件参数 |
| `mats_per_subarray_x/y` | mat 网格 | 器件参数 |
| `cells_per_mat_x/y` | cell 网格 | 器件参数 |
| `microbumps_x/y` | microbump 网格 | 封装参数 |
| `power_model` | 命令能量开关 | 可 |
| `power_source` | configured_pj/dramsim3_idd | 选择公式来源 |
| `power_scale` | 全部命令能量缩放 | 敏感性/校准参数 |
| `power_vdd`、`idd0`、`idd2n`、`idd3n`、`idd4r`、`idd4w`、`idd5ab`、`idd5pb`、`idd6x` | 电压/IDD 输入 | 必须来自目标器件才有绝对意义 |
| `idd_devices_per_rank` | 每 rank 器件数 | 与组织匹配 |
| `idd_burst_cycles` | IDD burst 公式的持续周期 | DRAMsim3 辅助验证/校准参数 |
| `thermal_model` | RC 热模型开关 | 可 |
| `thermal_ambient_c` | 环境温度 | 边界条件 |
| `thermal_rise_c_per_pj` | 能量到温升比例 | 必须校准 |
| `thermal_cooling_per_cycle` | 每周期冷却 | 必须校准 |
| `thermal_grid_cols_per_tile`、`thermal_grid_rows_per_tile` | tile 内热网格 | 仅改变热离散 |
| `thermal_coupling` | 邻域耦合 | 可 |
| `thermal_lateral_coupling` | 横向耦合系数 | 校准参数 |
| `thermal_vertical_coupling` | 纵向耦合系数 | 校准参数 |
| `thermal_tsv_coupling_scale` | TSV 纵向缩放 | 校准参数 |
| `thermal_tsvs_per_grid` | 每 grid TSV 数 | 封装参数 |
| `thermal_chip_dim_x_m`、`thermal_chip_dim_y_m` | 芯片平面尺寸（米） | 热网格几何输入，须来自 floorplan/封装 |
| `ecc_shadow` | SECDED shadow 开关 | 数据正确性模型 |
| `ecc_check_on_read` | 读时检查 | 可 |
| `ecc_correct_single_bit` | 单比特纠正 | 可 |
| `ecc_inject_period` | 周期性注错间隔 | 0 为关闭 |

### A.8 输出和验证参数

| 配置键/CLI | 含义 |
|---|---|
| `cmd_trace` | command CSV 路径 |
| `response_trace` | HostResponse CSV 路径 |
| `dfi_trace` | DFI beat CSV 路径 |
| `dfi_signal_trace` | DFI-like signal CSV 路径 |
| `dump_timing_table` | 最终 timing CSV |
| `memory_image` | 初始 image |
| `dump_memory_image` | 最终 txt/bin image |
| `dump_memory_csv` | 最终 CSV image |
| `verify_golden` | 仿真后 golden 校验 |
| `mismatch_report` | 不匹配报告 |
| `dump_thermal_map` | 热 map 输出 |
| `validate_cmd_trace` | 运行 command validator |
| `validate_dfi_trace` | 运行 DFI validator |
| `fail_on_data_mismatch` | mismatch 时退出 3 |
| `allow_data_mismatch` | mismatch 只记录不失败 |

---

## 附录 B：输出全指标说明

stdout 的精确字段以当前版本为准。以下覆盖稳定公共字段和动态字段族。

### B.1 身份、周期和完成

| 字段 | 解释 |
|---|---|
| `standard` | 当前标准 |
| `timing_profile`、`vendor_profile` | profile 与 vendor 标签 |
| `timing_source_*` | timing 字段来源 |
| `cycles` | 兼容周期字段；多 controller 时勿直接当系统壁钟 |
| `system_cycles` | 系统推进周期，计算系统性能的主时间基准 |
| `aggregate_ctrl_cycles` | 所有 controller 周期求和 |
| `host_requests` | 输入 host 请求数 |
| `dram_transactions` | 拆分后 DRAM transaction 数 |
| `injected_requests` | 成功注入系统的 transaction 数 |
| `reads`、`writes` | 读写 transaction 数 |
| `completed_reads`、`completed_writes` | 完成 transaction 数 |
| `remaining_requests` | 到 cycle limit 时仍未完成的数量 |
| `hit_cycle_limit` | 是否命中 max_cycles |

### B.2 性能、延迟和队列

| 字段 | 解释 |
|---|---|
| `avg_read_latency` | 完成读 transaction 的平均 controller tick 延迟 |
| `avg_queue_depth` | 采样平均队列深度 |
| `avg_read_queue_depth`、`avg_write_queue_depth` | 读写队列平均深度 |
| `injection_stall_cycles` | frontend 因系统拒收而停顿的周期 |
| `peak_bandwidth_GBps` | 由数据率和聚合总线宽度计算的理论峰值 |
| `achieved_bw_GBps` | 完成 payload / system time |
| `bandwidth_util_pct` | achieved/peak 百分比 |
| `achieved_if_bw_GBps` | 包含 metadata/ECC 的接口带宽 |
| `interface_bytes` | payload 加接口开销字节 |
| `payload_bytes` | 完成有效数据字节 |
| `metadata_bytes`、`ecc_bytes` | 接口 metadata/ECC 开销 |
| `bytes_per_cycle` | 完成 payload / system_cycles |

### B.3 行行为和命令

| 字段/字段族 | 解释 |
|---|---|
| `row_hits`、`row_misses`、`row_conflicts` | 调度时行缓冲关系 |
| `commands.ACT/ACT1/ACT2` | 激活/分裂激活数 |
| `commands.PRE/PREpb/PREab` | 预充数 |
| `commands.RD/WR/RDA/WRA` | 列读写及自动预充命令数 |
| `commands.CAS_RD/CAS_WR` | LPDDR CAS 同步/列入口计数 |
| `commands.REFab/REFpb/REFdb` | 各刷新命令数 |
| `commands.RFMab/RFMpb` | RFM 命令数 |
| `commands.MRW/MRR` | 模式寄存器命令数 |
| `commands.WCK_SYNC/WCK_TRAIN/DVFS` | LPDDR WCK/DVFS 命令数 |
| `commands.PDE/PDX/SREFEN/SREFEX` | 低功耗命令数 |
| `commands.ECC_SCRUB/RAS_ERR` | RAS 行为命令数 |
| `maintenance_requests/served` | 请求和已服务维护事务 |
| `refresh_batches`、`refresh_pb_events`、`refresh_ab_events` | 刷新批次/作用域事件 |
| `refresh_postpones`、`refresh_pullins`、`refresh_credit_peak` | 刷新调度行为 |
| `rfm_events_pb`、`rfm_events_ab` | RFM 事件数 |
| `dual_issue_cycles` | HBM 行列双总线同拍发射周期 |
| `row_bus_issues`、`column_bus_issues`、`unified_bus_issues` | 总线发射计数 |
| `rising_edge_issues`、`falling_edge_issues` | 边沿发射计数 |
| `write_mode_cycles` | 控制器处于写 drain 模式的周期 |

### B.4 数据、后端与拓扑

| 字段 | 解释 |
|---|---|
| `data_reads_checked` | 有 expected 的读检查数 |
| `data_mismatches` | payload 不匹配数 |
| `uninitialized_read_bytes` | 读取未初始化字节数 |
| `masked_write_bytes` | 被 mask 禁止写入的字节数 |
| `read_forwards` | 从在途/合并写转发的读数 |
| `write_coalesces` | 同地址写合并数 |
| `memory_backend` | 当前 backend 名称 |
| `memory_capacity_bytes` | 逻辑容量 |
| `storage_allocated_bytes` | 宿主实际已分配/触达存储量 |
| `storage_density_pct` | 已分配占逻辑容量比例 |
| `storage_read_bursts`、`storage_write_bursts` | 后端 burst 访问数 |
| `storage_row_buffer_hits/misses/conflicts` | MemoryImage row buffer 计数 |
| `touched_stacks/dies/layers/tiles/grids/subarrays/mats/cells/microbumps` | 已访问物理层级数量 |

### B.5 PHY 与 DFI

| 字段 | 解释 |
|---|---|
| `mem_phy_mode`、`phy_protocol`、`dfi_version` | PHY 模式、adapter、DFI 标签 |
| `phy_state` | 结束时生命周期状态 |
| `phy_commands_accepted/completed` | PHY 接收/完成命令数 |
| `phy_write_data_accepted/completed` | 写数据 FIFO/流水计数 |
| `phy_read_data_completed` | 读返回完成数 |
| `phy_command_backpressure` | 命令总线因 PHY 阻塞的周期数 |
| `phy_data_backpressure` | 数据路径因 PHY 阻塞的周期数 |
| `phy_command_fifo_peak`、`phy_write_fifo_peak`、`phy_read_fifo_peak` | FIFO 峰值占用 |
| `dfi_command_events` | DFI-oriented 命令事件数 |
| `dfi_write_beats`、`dfi_read_beats` | 数据 beat 数 |
| `dfi_expected_beats`、`dfi_matched_beats` | expected 数据验证 beat |
| `dfi_mismatched_beats` | DFI 数据不匹配 beat |
| `dfi_protocol_errors` | DFI 事件协议错误 |

### B.6 功耗、热、ECC、RAS 和模式

| 字段/字段族 | 解释 |
|---|---|
| `power_energy_pJ` | 总命令/存储事件能量 |
| `power_*_pJ` | ACT/PRE/RD/WR/refresh/background 等分项能量 |
| `thermal_peak_temp_c` | 已触达节点峰值温度 |
| `thermal_avg_temp_c` | 已触达节点平均温度 |
| `thermal_touched_nodes` | 热状态节点数 |
| `thermal_coupling_events` | 邻域/TSV 热耦合更新数 |
| `ecc_checks` | ECC shadow 检查数 |
| `ecc_corrected_errors` | 单比特纠正数 |
| `ecc_uncorrectable_errors` | 不可纠正数 |
| `ecc_injected_errors` | 注入错误数 |
| `wck_sync_events`、`wck_train_events` | WCK 行为事件 |
| `dvfs_events` | DVFS 事件 |
| `low_power_entries/exits/cycles` | 低功耗入口、退出和驻留周期 |
| `ras_error_events`、`link_retry_events`、`ecc_scrub_events` | 行为级 RAS/retry/scrub |

### B.7 多 Stack 和验证动态字段

| 字段族 | 解释 |
|---|---|
| `stack_count` | stack 数 |
| `aggregate_capacity_bytes` | 所有 stack 聚合容量 |
| `stack_N_active_ctrls` | 第 N stack 实际活跃的 controller 数 |
| `stack_N_reads/writes` | 第 N stack 完成的读/写 DRAM transaction |
| `stack_N_bw_GBps` | 第 N stack payload 带宽 |
| `stack_N_avg_read_latency` | 第 N stack 平均读延迟 |
| `stack_N_ingress_stalls` | 第 N stack ingress 无法接收的 cycle 数 |
| `stack_N_qos_dispatches` | 第 N stack QoS 优先级分发数 |
| `stack_N_power_pJ` | 第 N stack 能量 |
| `stack_N_peak_temp_C` | 第 N stack 峰值温度 |
| `cmd_validation` | PASS/FAIL/NOT_RUN |
| `cmd_validation_errors` 及分类字段 | command trace 时序/状态/地址错误计数 |
| `dfi_validation` | PASS/FAIL/NOT_RUN |
| `dfi_validation_errors` 及分类字段 | DFI timing/signal/mask/payload 错误计数 |

### B.8 其余精确输出字段

以下字段是当前 CLI/Stats 的细分输出。它们与前述聚合指标同时存在，列在这里用于脚本
对接和交付审计；同一行字段共享后面的解释。

#### B.8.1 系统、输入与队列

| 精确字段 | 解释 |
|---|---|
| `request_source` | synthetic/trace 等请求来源 |
| `controller_count`、`active_controllers`、`active_stacks` | 已创建 controller、实际活跃 controller/stack 数 |
| `dual_command_bus` | 是否使用独立行/列命令总线 |
| `stack_ingress_peak`、`stack_ingress_stalls`、`qos_priority_dispatches` | stack ingress 峰值、满队列停顿、优先级分发数 |
| `read_queue_len_avg`、`write_queue_len_avg`、`priority_queue_avg`、`active_queue_len_avg` | 全局读/写/优先/active 平均队列长度 |
| `read_q_avg_per_ctrl`、`write_q_avg_per_ctrl`、`priority_q_avg_per_ctrl`、`active_q_avg_per_ctrl` | 上述队列按 controller 归一化值 |
| `remaining_pending` | 结束时仍在 pending 的 transaction |
| `burst_trace_lines`、`burst_split_requests` | burst trace 行数及拆分出的 transaction 数 |
| `burst_read_requests`、`burst_write_requests` | burst 读/写 host 请求数 |
| `burst_read_bytes`、`burst_write_bytes` | burst 读/写 payload 字节 |
| `host_responses_exported` | 已在线消费并写入 response trace 的 HostResponse 数 |

#### B.8.2 数据闭环与 golden

| 精确字段 | 解释 |
|---|---|
| `data_checked_reads` | 执行 expected 检查的读数 |
| `data_uninitialized_reads` | 包含未初始化字节的读事务数 |
| `data_write_commits` | 已提交到 MemoryImage 的写数 |
| `data_masked_write_commits` | 含写掩码的提交数 |
| `data_forward_checks` | 读转发正确性检查数 |
| `golden_verified` | 实际与 golden 执行逐 line 比较的记录数；是否通过还要看 `golden_mismatches=0` 和进程退出状态 |
| `golden_mismatches` | 与 golden 不匹配的 line/记录数 |
| `golden_uninitialized_lines` | golden 中未初始化 line 数 |
| `golden_actual_uninitialized_lines` | 实际 image 中对应未初始化 line 数 |
| `memory_image_dump`、`memory_csv_dump` | 最终 image/CSV 输出路径或状态 |

#### B.8.3 命令、模式和总线

除前述命令族外，当前精确键还包括 `commands.ACT1`、`commands.ACT2`、
`commands.PREpb`、`commands.PREab`、`commands.CAS_WR`、`commands.RD`、
`commands.WR`、`commands.RDA`、`commands.WRA`、`commands.REFpb`、
`commands.REFdb`、`commands.RFMpb`、`commands.MRR`、`commands.WCK_TRAIN`、
`commands.DVFS`、`commands.PDX`、`commands.SREFEN`、`commands.SREFEX` 和
`commands.RAS_ERR`；含义均为相应命令的发射计数。

| 精确字段 | 解释 |
|---|---|
| `act2_deadline_forced` | LPDDR ACT2 因 deadline 被强制优先的次数 |
| `split_activate` | 分裂激活序列数 |
| `mode_register_ops` | MRW/MRR 模式寄存器操作总数 |
| `rising_edge_ticks`、`falling_edge_ticks` | 上升/下降边沿实际占用 tick 数 |
| `refresh_pb_batches`、`refresh_ab_batches` | per-bank/all-bank refresh 批次 |
| `refresh_high_temp_mult` | 当前高温刷新倍率 |
| `rfm_pb_events`、`rfm_ab_events`、`rfm_decrements` | RFM 作用域事件和计数递减次数 |
| `dvfs_transitions` | 已完成 DVFS 转换数 |
| `wck_syncs`、`wck_sync_skips`、`wck_training_events` | WCK 同步、可跳过同步、训练事件数 |
| `low_power_cycles`、`low_power_exits`、`low_power_exit_blocked` | 低功耗驻留、退出和退出阻塞周期/次数 |
| `row_policy_ap_upgrades`、`row_policy_precharges` | 行策略把 RD/WR 升级为 AP 和主动 PRE 的次数 |

#### B.8.4 Command validator

| 精确字段 | 解释 |
|---|---|
| `command_trace_retained` | 为验证/输出保留的 command event 数 |
| `cmd_validation_checked` | validator 检查的 command 数 |
| `cmd_validation_state_checks` | bank/协议状态检查次数 |
| `cmd_validation_timing_checks` | timing 约束比较次数 |
| `cmd_validation_timing_updates` | timing 历史更新次数 |
| `cmd_validation_bus_checks` | 命令总线冲突检查次数 |
| `cmd_validation_edge_checks` | 边沿合法性检查次数 |
| `cmd_validation_pair_checks` | HBM edge-pair 规则检查次数 |
| `cmd_validation_faw_events` | 纳入 tFAW 窗口的 ACT 事件数 |
| `cmd_validation_wck_checks` | LPDDR WCK 状态/间隔检查次数 |

#### B.8.5 DFI 细分

| 精确字段 | 解释 |
|---|---|
| `dfi_beat_bytes` | 一个 DFI 数据 beat 的 byte 数 |
| `dfi_data_bytes` | DFI trace 中数据总字节 |
| `dfi_forwarded_read_beats` | 读转发产生的 DFI beat 数 |
| `dfi_masked_write_beats` | 含 mask 的写 beat 数 |
| `dfi_validation_events` | validator 消费的 DFI event 数 |
| `dfi_validation_command_checks` | DFI 命令合法性检查数 |
| `dfi_validation_data_checks` | DFI data beat 检查数 |
| `dfi_validation_expected_checks` | expected payload 比较数 |
| `dfi_validation_latency_checks` | command→data latency 检查数 |
| `dfi_validation_payload_checks` | payload 内容检查数 |
| `dfi_validation_phase_checks` | phase 编号/顺序检查数 |
| `dfi_validation_signal_checks` | signal-like 组合检查数 |

#### B.8.6 PHY 细分

| 精确字段 | 解释 |
|---|---|
| `phy_reset_config_cycles`、`phy_init_config_cycles`、`phy_train_config_cycles` | 配置的 reset/init/training 周期 |
| `phy_max_command_fifo`、`phy_max_write_fifo`、`phy_max_read_fifo` | 三类 FIFO 实测峰值 |
| `phy_write_requests`、`phy_write_completions` | PHY 写请求与完成数 |
| `phy_read_requests`、`phy_read_completions` | PHY 读请求与完成数 |
| `phy_write_service_cycles`、`phy_read_service_cycles` | 写/读 PHY 服务周期 |
| `phy_hbm_row_commands`、`phy_hbm_column_commands` | HBM adapter 行/列命令数 |
| `phy_ca_edges` | adapter 生成的 CA 边沿事件数 |
| `phy_lpddr_split_act` | LPDDR adapter 分裂 ACT 数 |
| `phy_lpddr_wck_events` | LPDDR adapter WCK 相关事件数 |

#### B.8.7 接口、标准身份和开销

| 精确字段 | 解释 |
|---|---|
| `if_xfer_rate_Gbps` | 每 pin 外部接口配置速率，即 `data_rate_mbps/1000`；不是乘以总线宽度后的聚合带宽 |
| `interface_read_bytes`、`interface_write_bytes` | 含开销的读/写接口字节 |
| `interface_command_bits` | 命令接口 bit 数 |
| `interface_overhead_bits` | metadata/ECC/CRC/parity 等总开销 bit |
| `payload_efficiency_pct` | payload / 总接口传输百分比 |
| `read_bytes_per_cycle`、`write_bytes_per_cycle`、`total_bytes_per_cycle` | 读、写、总接口字节/系统周期 |
| `hbm_full_32ch_stack` | 最终 HBM full-32-channel 开关快照 |
| `hbm_pairing_matrix` | 最终 HBM pairing matrix 标签 |
| `hbm_link_crc_bits_req`、`hbm_ras_meta_bits_req`、`hbm_ecc_bits_req` | HBM 每请求 CRC/RAS/ECC bit |
| `lpddr_family` | LPDDR 标准族标签 |
| `lpddr_wck_train_req` | 最终 WCK retraining 开关 |
| `lpddr_low_rate_mbps`、`lpddr_dvfs_policy` | 低速分支数据率与 DVFS 策略 |
| `lpddr_low_power_policy`、`lpddr_mr_profile` | 低功耗与 MR profile 标签 |
| `lpddr_link_mode` | link protection 模式 |
| `lpddr_dbi_bits_req`、`lpddr_link_ecc_bits_req`、`lpddr_ca_parity_bits_cmd` | LPDDR DBI/link ECC/CA parity bit |
| `lpddr_metadata_bits_req` | LPDDR 每请求 metadata bit |

#### B.8.8 MemoryImage、row buffer 与拓扑

| 精确字段 | 解释 |
|---|---|
| `memory_backend_line_bytes` | backend 的物理 line/block byte |
| `total_addressable_lines` | 逻辑容量可寻址 line 数 |
| `unique_written_lines` | 曾被写入的唯一 line 数 |
| `storage_bytes_allocated`、`storage_lines_allocated` | 实际分配 byte/line |
| `storage_read_line_accesses`、`storage_write_line_accesses` | backend line 读写次数 |
| `storage_backend_recommendation` | 根据稀疏密度生成的后端建议 |
| `storage_topology_lines_scanned`、`storage_topology_scan_skipped` | 物理统计实际扫描/跳过 line 数 |
| `storage_stacks_touched`、`storage_dies_touched`、`storage_layers_touched` | 已触达 stack/die/layer 数 |
| `storage_channels_touched`、`storage_pcs_touched`、`storage_sids_touched` | 已触达 channel/PC/SID 数 |
| `storage_ranks_touched`、`storage_bgs_touched`、`storage_banks_touched` | 已触达 rank/BG/bank 数 |
| `storage_rows_touched`、`storage_columns_touched` | 已触达 row/column 数 |
| `storage_subarrays_touched`、`storage_mats_touched`、`storage_cells_touched`、`storage_microbumps_touched` | 细粒度物理坐标触达数 |
| `rowbuf_activations`、`rowbuf_precharges` | MemoryImage 行缓冲 ACT/PRE 数 |
| `rowbuf_hits`、`rowbuf_misses` | MemoryImage 行缓冲命中/未命中 |
| `rowbuf_reads`、`rowbuf_writes` | 行缓冲读写访问数 |
| `rowbuf_lazy_loads` | 首次访问时从 backend 延迟装载行数 |
| `rowbuf_dirty_rows`、`rowbuf_dirty_writebacks` | 脏行形成与写回数 |
| `rowbuf_open_rows` | 仿真结束时仍打开的 MemoryImage 行数 |
| `rowbuf_clean_precharges`、`rowbuf_forced_closes` | 干净预充和强制关闭数 |

#### B.8.9 功耗、热、ECC 与 RAS

| 精确字段 | 解释 |
|---|---|
| `power_model_enabled`、`power_events` | 功耗模型开关与累计事件数 |
| `power_act_pJ`、`power_read_pJ`、`power_write_pJ`、`power_refab_pJ`、`power_refpb_pJ` | 配置/派生的单事件能量参数 |
| `power_act_energy_pJ`、`power_pre_energy_pJ`、`power_read_energy_pJ`、`power_write_energy_pJ` | ACT/PRE/RD/WR 累计能量 |
| `power_refresh_energy_pJ`、`power_rfm_energy_pJ`、`power_control_energy_pJ` | refresh/RFM/control 累计能量 |
| `thermal_model_enabled`、`thermal_ambient_C`、`thermal_rise_C_per_pJ` | 热模型开关、环境温度和能量—温升参数 |
| `thermal_peak_temp_C`、`thermal_avg_temp_C` | 当前触达节点峰值/平均温度 |
| `thermal_updates`、`thermal_coupled_delta_C` | 热更新次数和耦合引起的累计温差变化 |
| `thermal_tiles_touched`、`thermal_grid_cells_touched` | 已触达 tile/grid 数 |
| `thermal_grid_cols_tile`、`thermal_grid_rows_tile` | 每 tile 热网格划分 |
| `thermal_hotspot_layer`、`thermal_hotspot_x`、`thermal_hotspot_y` | 峰值热点的 thermal grid 坐标 |
| `thermal_lateral_transfers`、`thermal_vertical_transfers`、`thermal_tsv_transfers` | 横向、纵向、TSV 耦合传热事件 |
| `thermal_map_dump` | 热图输出路径或状态 |
| `floorplan_enabled`、`floorplan_tiles_touched` | floorplan 开关和触达 tile 数 |
| `ecc_checked_reads`、`ecc_shadow_updates` | ECC 检查读数和 shadow 更新数 |
| `ecc_parity_repairs` | ECC parity/shadow 修复数 |
| `ras_ecc_events` | ECC 相关 RAS 行为事件数 |

#### B.8.10 Timing 和配置快照

| 精确字段 | 解释 |
|---|---|
| `timing_table_entries` | finalize 后 Timing 约束条目数 |
| `timing_source_vendor`、`timing_source_jedec`、`timing_source_research`、`timing_source_derived` | 各来源的 Timing 字段数量 |
| `timing_vendor_required` | 仍标记为需 vendor 校准的字段数 |
| `timing_vendor_required_only` | 其中没有替代研究值的字段数 |
| `timing_table_dump` | timing table 输出路径或状态 |
| `cells_per_mat_y`、`mats_per_subarray_y`、`microbumps_y` | stdout 中的物理 Y 维配置快照；X 维同理 |

`maintenance_served` 是已执行维护命令/请求的兼容聚合字段。若它与更细的 refresh/RFM
计数同时存在，优先使用细分字段解释维护组成。

首次使用新版本时建议保存一次完整 stdout，并用 `rg '^[A-Za-z0-9_.]+\s*:'` 获取实际字段；
工具不要假定未出现字段的值为 0。

### B.9 带宽、延迟、传输率、功耗和热的联合分析实例

本例使用仓库已有的 `outputs/visualization/example/stats.txt`。它是一次 HBM4 Behavioral
PHY 短仿真，用于演示“如何从原始字段得出结论”，不是目标厂商器件的实测报告。

#### B.9.1 第一步：先确认运行可用

```bash
rg '^(standard|system_cycles|tick_multiplier|tCK_ps|host_requests|dram_transactions|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|data_mismatches|cmd_validation|dfi_validation)\s*:' \
  outputs/visualization/example/stats.txt
```

该例中 128 个 host request 拆成 256 个 DRAM transaction，132 读 + 124 写全部完成；
`remaining_requests=0`、未命中 cycle limit、数据 mismatch 为 0，command/DFI validation 均通过。
只有这些前提成立，后面的带宽和延迟才能与其他完整运行比较。

#### B.9.2 把 system tick 换成真实时间

该例 `tCK_ps=500`、`tick_multiplier=2`，因此：

```text
1 system tick = 500 ps / 2 = 250 ps = 0.25 ns
total time    = 3615 tick × 0.25 ns/tick = 903.75 ns
```

不能直接把 `system_cycles=3615` 当作 3615 nCK，也不能用多 controller 求和后的
`aggregate_ctrl_cycles` 做系统壁钟时间。本项目的带宽分母是 `system_cycles` 对应的时间。

#### B.9.3 区分速率、理论带宽和实际带宽

原始数值为：

| 字段 | 值 | 解释 |
|---|---:|---|
| `if_xfer_rate_Gbps` | 8.000 | 8000 Mbps/pin 换算成的每 pin 传输率 |
| `peak_bandwidth_GBps` | 2048.000 | 8 Gbps/pin × 2048 pin ÷ 8，单 stack 理想 payload 峰值 |
| `read_bytes + write_bytes` | 4224 + 3968 = 8192 B | 已完成的有效 payload |
| `interface_read_bytes + interface_write_bytes` | 4488 + 4216 = 8704 B | payload 加模型的 ECC/metadata 开销；本例 command bits 为 0 |

可手工复算：

```text
payload BW   = 8192 B / 903.75 ns = 9.064 GB/s
interface BW = 8704 B / 903.75 ns = 9.631 GB/s
utilization  = 9.064 / 2048 × 100% = 0.443% ≈ 0.44%
efficiency   = 8192 / 8704 × 100% = 94.118% ≈ 94.12%
```

这四个数分别对应 `achieved_bw_GBps`、`achieved_if_bw_GBps`、
`bandwidth_util_pct`、`payload_efficiency_pct`。接口带宽高于 payload 带宽不是性能
更高，而是它把保护/metadata 也算为接口传输字节。

本例 `controller_count=32`但 `active_controllers=1`，所以 0.44% 的全栈理论峰值利用率主要
说明这个短 stream workload 没有展开通道并行度，不说明一个活跃 channel 的局部总线
只利用了 0.44%。要评估饱和带宽，应换用 channel-friendly 地址映射、更大 random 负载，
并建立注入率—吞吐曲线。

#### B.9.4 延迟应与吞吐一起看

`avg_read_latency=2030.85` 的单位是 system/controller tick，从 transaction 进入系统到读完成累计：

```text
average read latency = 2030.85 tick × 0.25 ns/tick = 507.71 ns
```

该值不是单纯 `nCL+nBL`，还包括排队、ACT/PRE、读写换向、PHY 和维护等延迟。
本例 `row_hits=245`、`row_misses=11`、`row_conflicts=0`，但读/写队列平均深度约为 71.63/41.71；
因此“高行命中”不保证“低请求延迟”，单一活跃 controller 的排队是需要优先排查的原因。

比较注入率时，正常饱和特征是：注入率提高后 achieved BW 先上升后趋平，而平均延迟
和队列深度开始快速上升。不要用单个运行点宣称“最大带宽”。

#### B.9.5 从能量得到平均功率

该例 `power_energy_pJ=543861.20`，仿真时间 903.75 ns：

```text
average modeled power
  = energy(pJ) / time(ns) × 10^-3
  = 543861.20 / 903.75 × 10^-3
  = 0.602 W
```

这是对整个运行窗口的平均**模型功率**。查 `power_act_energy_pJ`、
`power_read_energy_pJ`、`power_write_energy_pJ`、refresh/background 分项可判断哪类事件主导。
若能量使用 `configured_pj/research_default`，0.602 W 只适合作为相同参数下的相对对照；
只有使用目标器件 VDD/IDD 或校准过的活动能量后，才能做器件绝对功率声明。

#### B.9.6 热结果和功耗的联合解读

该例：

```text
ambient = 40.00 °C
peak    = 40.29 °C, delta-to-ambient = 0.29 °C
average = 40.05 °C
thermal updates = 722
```

峰值高于环境温度、平均低于峰值，方向上与能量注入一致。但 903.75 ns 是极短的瞬态
窗口，0.29 °C 不是稳态结温。正式热分析至少要：

1. 使用足够长的负载，直到峰值/平均温度的时间斜率趋近 0；
2. 固定 ambient、cooling、rise-per-pJ、网格和耦合参数；
3. 同时查 hotspot layer/x/y、活跃 stack/channel 和 power 分项；
4. 做冷却系数、能量系数和 TSV/纵向耦合的敏感性区间；
5. 未经实物热阻/热容校准时，只声明“热点趋势”而不声明绝对结温精度。

#### B.9.7 实验对照时的最小记录表

| 类别 | 必须一起记录 | 主要问题 |
|---|---|---|
| 完整性 | completed、remaining、cycle limit、validation、mismatch | 是否比较了两次真正完成的运行？ |
| 工作量 | requests/transactions、read ratio、seed、mapping、active controllers/stacks | 输入和并行度是否一致？ |
| 带宽 | payload BW、interface BW、peak、utilization、efficiency | 提升的是有效数据，还是接口开销？ |
| 延迟 | avg latency、queue depth、row hit/miss/conflict、PHY/maintenance stall | 时间花在排队、DRAM timing 还是 PHY？ |
| 能效 | total/sub-energy、平均功率、pJ/payload-byte | 吞吐改善是否以更高能量为代价？ |
| 热 | ambient、peak/avg/delta、hotspot、run duration、thermal parameters | 是瞬态趋势、稳态结果，还是未校准参数的敏感性？ |

最终结论应写成“在固定 profile、workload、seed、映射和热/功耗参数下，方案 A 相对
方案 B 的 payload 带宽、延迟、能量和峰值温度分别变化多少”，不要只报一个带宽数字。

---

## 附录 C：四标准交付用例

每个用例都应保存 `stats.txt`、timing table、command/DFI trace（小窗口）、验证 JSON 和
dashboard，并记录 commit 与运行命令。

### C.0 四标准通用的三后端人工审计函数

将下面整段粘贴到 Bash 终端一次。它为指定标准用相同初始 image 和 trace 分别运行
`sparse`、`mmap_sparse`、`chunk_file`，并自动导出可人工打开的 text/CSV。

```bash
audit_case_backends() {
  case_cfg="$1"
  case_out="$2"
  mkdir -p "$case_out/sparse" "$case_out/mmap" "$case_out/chunk"

  ./build-clang-debug/hbm_sim --config "$case_cfg" \
    --trace examples/data_check.trace --requests 0 \
    --memory-image examples/memory_image.txt \
    --memory-backend sparse --memory-capacity-bytes 1048576 \
    --dump-memory-image "$case_out/sparse/final.txt" \
    --dump-memory-csv "$case_out/sparse/final.csv" \
    --mismatch-report "$case_out/sparse/mismatch.txt" \
    | tee "$case_out/sparse/stats.txt"

  ./build-clang-debug/hbm_sim --config "$case_cfg" \
    --trace examples/data_check.trace --requests 0 \
    --memory-image examples/memory_image.txt \
    --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
    --memory-data-file "$case_out/mmap/data.bin" \
    --dump-memory-image "$case_out/mmap/final.txt" \
    --dump-memory-csv "$case_out/mmap/final.csv" \
    --mismatch-report "$case_out/mmap/mismatch.txt" \
    --verify-golden "$case_out/sparse/final.txt" \
    | tee "$case_out/mmap/stats.txt"

  ./build-clang-debug/hbm_sim --config "$case_cfg" \
    --trace examples/data_check.trace --requests 0 \
    --memory-image examples/memory_image.txt \
    --memory-backend chunk_file --memory-capacity-bytes 1048576 \
    --memory-data-file "$case_out/chunk/data.bin" \
    --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
    --dump-memory-image "$case_out/chunk/final.txt" \
    --dump-memory-csv "$case_out/chunk/final.csv" \
    --mismatch-report "$case_out/chunk/mismatch.txt" \
    --verify-golden "$case_out/sparse/final.txt" \
    | tee "$case_out/chunk/stats.txt"

  rg '^(standard|memory_backend|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|data_mismatches|golden_verified|golden_mismatches|storage_lines_allocated|unique_written_lines)\s*:' \
    "$case_out"/*/stats.txt
  diff -u "$case_out/sparse/final.txt" "$case_out/mmap/final.txt"
  diff -u "$case_out/sparse/final.txt" "$case_out/chunk/final.txt"
  od -An -tc -N8 "$case_out/mmap/data.bin.meta"
  od -An -tc -N8 "$case_out/chunk/data.bin.meta"
  stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
    "$case_out"/mmap/data.bin* "$case_out"/chunk/data.bin*
}
```

判定方法与 [6.5.2–6.5.5](#652-共同的可重放人工查验准备) 相同：三份 stats 都必须完成且
mismatch 为 0；两次 `diff` 都应无输出；两个 `.meta` 都应显示 `HBMBACK\0`。
这一小测试检查的是“同一标准在三后端上的数据语义一致性”；正式大容量运行还需按
[6.5.3](#653-mmap_sparse-的人工查验) 和 [6.5.4](#654-chunk_file-的人工查验) 做重开与宿主资源检查。

### C.1 HBM3

#### C.1.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/hbm3
./build-clang-debug/hbm_sim --config configs/run/hbm3.cfg \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/hbm3/commands.csv \
  --response-trace outputs/delivery/hbm3/responses.csv \
  --dfi-trace outputs/delivery/hbm3/dfi.csv \
  --dfi-signal-trace outputs/delivery/hbm3/dfi_signal.csv \
  --dump-timing-table outputs/delivery/hbm3/timing.csv \
  --dump-thermal-map outputs/delivery/hbm3/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  | tee outputs/delivery/hbm3/stats.txt
```

#### C.1.2 指标查看与预计效果

```bash
rg '^(standard|timing_profile|vendor_profile|data_rate_mbps|data_bus_bits|controller_count|mem_phy_mode|phy_protocol|completed_reads|completed_writes|remaining_requests|hit_cycle_limit|peak_bandwidth_GBps|achieved_bw_GBps|avg_read_latency|cmd_validation|dfi_validation)\s*:' \
  outputs/delivery/hbm3/stats.txt

rg '^(commands\.(ACT|RD|WR|PRE|REFpb|RFMpb)|row_hits|row_misses|row_conflicts|dual_issue_cycles|phy_command_backpressure)\s*:' \
  outputs/delivery/hbm3/stats.txt

sed -n '1,12p' outputs/delivery/hbm3/timing.csv
sed -n '1,8p' outputs/delivery/hbm3/commands.csv
```

预计：`standard : HBM3`、`phy_protocol : hbm`、Behavioral PHY；请求全部完成、未命中周期
上限、command/DFI 验证通过。random 75% read 通常同时产生 RD/WR、行命中和行冲突；
command CSV 应能看到 PC/SID/BG/bank 解码。当前参数是 Ramulator2.1-derived 研究基线，
不能写成目标厂商 HBM3 的实测结果。

#### C.1.3 可视化

```bash
python3 tools/visualize.py \
  --command-trace outputs/delivery/hbm3/commands.csv \
  --dfi-trace outputs/delivery/hbm3/dfi.csv \
  --stats outputs/delivery/hbm3/stats.txt \
  --thermal-map outputs/delivery/hbm3/thermal_map.txt \
  --title 'HBM3 delivery case' \
  --out outputs/delivery/hbm3/dashboard.html
xdg-open outputs/delivery/hbm3/dashboard.html
```

预计：Command mix 同时出现 ACT、RD/WR、PRE；bank view 可检查 same/different BG 的命令
间距，DFI 面板显示 HBM command/data event，热图仅用于相对热点趋势。

#### C.1.4 其他有价值的命令

```bash
# Direct/Behavioral 对照
./build-clang-debug/hbm_sim --config configs/run/hbm3.cfg \
  --requests 1024 --mem-phy direct | tee outputs/delivery/hbm3/direct.txt

# all-bank refresh/RFM 策略对照
./build-clang-debug/hbm_sim --config configs/run/hbm3.cfg \
  --requests 4096 --refresh-policy all_bank --rfm-policy all_bank \
  | tee outputs/delivery/hbm3/all_bank.txt

# HBM3 注入率曲线
python3 tools/performance_curve.py --binary ./build-clang-debug/hbm_sim \
  --standards hbm3 --intervals 1,2,4,8,16 --read-ratios 50,100 \
  --requests 512 --pattern random --seed 11 \
  --csv-out outputs/delivery/hbm3/performance.csv \
  --json-out outputs/delivery/hbm3/performance.json
```

预计：Direct 对照隔离 PHY pipeline/反压；all-bank 维护通常比 per-bank 造成更大阻塞；性能
曲线在高注入率处吞吐趋于饱和、平均延迟上升。

#### C.1.5 HBM3 三后端人工查验

```bash
audit_case_backends configs/run/hbm3.cfg \
  outputs/delivery/hbm3/backend_audit
sed -n '1,12p' outputs/delivery/hbm3/backend_audit/sparse/final.csv
```

除 C.0 的通用判据外，三份 stats 的 `standard` 都应为 `HBM3`、backend line size 应与
HBM3 DRAM transaction 粒度一致。若后端一致而 command validation 失败，应转向 timing/
命令层排查，不要归因于存储容器。

### C.2 HBM4

#### C.2.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/hbm4
./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/hbm4/commands.csv \
  --response-trace outputs/delivery/hbm4/responses.csv \
  --dfi-trace outputs/delivery/hbm4/dfi.csv \
  --dfi-signal-trace outputs/delivery/hbm4/dfi_signal.csv \
  --dump-timing-table outputs/delivery/hbm4/timing.csv \
  --dump-thermal-map outputs/delivery/hbm4/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  | tee outputs/delivery/hbm4/stats.txt
```

#### C.2.2 指标查看与预计效果

```bash
rg '^(standard|timing_profile|timing_source_|data_rate_mbps|data_bus_bits|dram_transaction_bytes|hbm_full_32ch_stack|controller_count|peak_bandwidth_GBps|achieved_bw_GBps|achieved_if_bw_GBps|payload_efficiency_pct|remaining_requests|hit_cycle_limit|cmd_validation|dfi_validation)\s*:' \
  outputs/delivery/hbm4/stats.txt

rg '^(dual_issue_cycles|row_bus_issues|column_bus_issues|rising_edge_ticks|falling_edge_ticks|commands\.(ACT|RD|WR|PRE|REFpb|RFMpb)|rfm_|hbm_.*bits_req)\s*:' \
  outputs/delivery/hbm4/stats.txt

sed -n '1,15p' outputs/delivery/hbm4/timing.csv
sed -n '1,8p' outputs/delivery/hbm4/dfi_signal.csv
```

预计：`standard : HBM4`、32 channel full stack、2048-bit 聚合数据总线、32B DRAM
transaction；理论峰值约 2048 GB/s。`achieved_if_bw_GBps` 因 16-bit/transaction ECC
metadata 通常高于 payload `achieved_bw_GBps`。stream workload 可能只激活部分 channel，
所以短测试不应期待接近理论峰值。

#### C.2.3 可视化

```bash
python3 tools/visualize.py \
  --command-trace outputs/delivery/hbm4/commands.csv \
  --dfi-trace outputs/delivery/hbm4/dfi.csv \
  --stats outputs/delivery/hbm4/stats.txt \
  --thermal-map outputs/delivery/hbm4/thermal_map.txt \
  --title 'HBM4 full-stack delivery case' \
  --out outputs/delivery/hbm4/dashboard.html
xdg-open outputs/delivery/hbm4/dashboard.html
```

重点观察行列双总线是否形成 dual issue、rising/falling edge pairing、32 个 channel 的负载
分布，以及 payload/interface 两种带宽口径。短时间热图只表示访问位置，不表示稳态结温。

#### C.2.4 多 Stack、链路开销和性能对照

```bash
# 六颗独立 HBM4 stack
mkdir -p outputs/delivery/hbm4_6stack
./build-clang-debug/hbm_sim --config configs/run/hbm4_6stack.cfg \
  --requests 4096 --cmd-trace outputs/delivery/hbm4_6stack/commands.csv \
  | tee outputs/delivery/hbm4_6stack/stats.txt
rg '^(stack_count|controller_count|aggregate_capacity_bytes|peak_bandwidth_GBps|stack_[0-9]+_)' \
  outputs/delivery/hbm4_6stack/stats.txt

# CRC16/retry 行为开销入口
./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg --requests 1024 \
  --hbm-link-crc crc16 --hbm-link-crc-bits 16 --hbm-link-retry true \
  | tee outputs/delivery/hbm4/link_protection.txt

# random 高并行 workload
./build-clang-debug/hbm_sim --config configs/run/hbm4.cfg --requests 10000 \
  --pattern random --seed 42 --addr-mapping RoBaRaCoCh \
  | tee outputs/delivery/hbm4/random_mapping.txt
```

预计：六 Stack 输出 `stack_0_*` 到 `stack_5_*`，controller 数和聚合容量随 stack 数增加；
CRC/retry 配置增加接口开销/事件入口；random + channel-friendly 映射通常激活更多 controller，
但实际带宽和延迟必须以输出为准。

#### C.2.5 HBM4 三后端人工查验

```bash
audit_case_backends configs/run/hbm4.cfg \
  outputs/delivery/hbm4/backend_audit
sed -n '1,12p' outputs/delivery/hbm4/backend_audit/sparse/final.csv
```

三份 stats 的 `standard` 都应为 `HBM4`，当前 full-stack 模型的 backend line 应为 32 B DRAM
transaction，而不是 64 B host line。还要检查 `storage_channels_touched`和 CSV 中的
PC/SID/BG/bank 坐标，不能只比较 payload 字符串。多 Stack 文件查验另按 [6.5.6](#656-多-stack-后端查验)执行。

### C.3 LPDDR5

#### C.3.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/lpddr5
./build-clang-debug/hbm_sim --config configs/run/lpddr5.cfg \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/lpddr5/commands.csv \
  --response-trace outputs/delivery/lpddr5/responses.csv \
  --dfi-trace outputs/delivery/lpddr5/dfi.csv \
  --dfi-signal-trace outputs/delivery/lpddr5/dfi_signal.csv \
  --dump-timing-table outputs/delivery/lpddr5/timing.csv \
  --dump-thermal-map outputs/delivery/lpddr5/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  | tee outputs/delivery/lpddr5/stats.txt
```

#### C.3.2 指标查看与预计效果

```bash
rg '^(standard|timing_profile|vendor_profile|lpddr_family|lpddr_wck_mode|lpddr_wck_ratio|lpddr_dvfs_mode|lpddr_link_protection|mem_phy_mode|phy_protocol|completed_reads|remaining_requests|hit_cycle_limit|peak_bandwidth_GBps|achieved_bw_GBps|avg_read_latency|cmd_validation|dfi_validation)\s*:' \
  outputs/delivery/lpddr5/stats.txt

rg '^(commands\.(ACT|CAS_RD|CAS_WR|RD|WR|WCK_SYNC|WCK_TRAIN|PDE|PDX)|wck_|phy_lpddr_|low_power_)\s*:' \
  outputs/delivery/lpddr5/stats.txt

sed -n '1,12p' outputs/delivery/lpddr5/timing.csv
sed -n '1,8p' outputs/delivery/lpddr5/dfi.csv
```

预计：`standard : LPDDR5`、`phy_protocol : lpddr`、WCK `cas_sync`、默认 100% read；写命令
可能为 0 是 workload 结果，不是故障。当前 profile 是 Ramulator2.1-derived 研究基线，
适合与 LPDDR6 进行相同 workload 的趋势对照，不代表指定 LPDDR5 器件。

#### C.3.3 可视化和模式对照

```bash
python3 tools/visualize.py \
  --command-trace outputs/delivery/lpddr5/commands.csv \
  --dfi-trace outputs/delivery/lpddr5/dfi.csv \
  --stats outputs/delivery/lpddr5/stats.txt \
  --thermal-map outputs/delivery/lpddr5/thermal_map.txt \
  --title 'LPDDR5 delivery case' \
  --out outputs/delivery/lpddr5/dashboard.html
xdg-open outputs/delivery/lpddr5/dashboard.html

# 增加写流量，观察 WCK 和读写换向
./build-clang-debug/hbm_sim --config configs/run/lpddr5.cfg \
  --requests 4096 --pattern random --read-ratio 50 --seed 21 \
  | tee outputs/delivery/lpddr5/mixed_rw.txt

# power-down 行为入口
./build-clang-debug/hbm_sim --config configs/run/lpddr5.cfg \
  --requests 256 --inject-interval 16 --low-power power_down \
  --low-power-entry-cycles 8 --low-power-exit-cycles 4 \
  | tee outputs/delivery/lpddr5/power_down.txt
```

预计：混合读写会出现 CAS_WR/WR 和 turnaround 空窗；稀疏注入的 power-down 用例应出现
低功耗入口/退出计数。若没有低功耗事件，先检查空闲阈值和请求间隔，不要直接判断状态机失效。

#### C.3.4 LPDDR5 三后端人工查验

```bash
audit_case_backends configs/run/lpddr5.cfg \
  outputs/delivery/lpddr5/backend_audit
sed -n '1,12p' outputs/delivery/lpddr5/backend_audit/sparse/final.csv
```

三份 stats 的 `standard` 都应为 `LPDDR5`、`phy_protocol` 都应为 `lpddr`；最终 image
必须一致。后端对照不会单独验证 WCK，因此仍需保留 C.3.2 的 WCK/CAS stats 和
DFI trace 检查。

### C.4 LPDDR6

#### C.4.1 完整仿真与产物

```bash
mkdir -p outputs/delivery/lpddr6
./build-clang-debug/hbm_sim --config configs/run/lpddr6.cfg \
  --requests 1024 --floorplan true --power-model true --thermal-model true \
  --cmd-trace outputs/delivery/lpddr6/commands.csv \
  --response-trace outputs/delivery/lpddr6/responses.csv \
  --dfi-trace outputs/delivery/lpddr6/dfi.csv \
  --dfi-signal-trace outputs/delivery/lpddr6/dfi_signal.csv \
  --dump-timing-table outputs/delivery/lpddr6/timing.csv \
  --dump-thermal-map outputs/delivery/lpddr6/thermal_map.txt \
  --validate-cmd-trace --validate-dfi-trace \
  | tee outputs/delivery/lpddr6/stats.txt
```

#### C.4.2 指标查看与预计效果

```bash
rg '^(standard|timing_profile|data_rate_mbps|data_bus_bits|prefetch_size|dram_transaction_bytes|pseudo_channels|lpddr_efficiency_mode|lpddr_dvfs_mode|lpddr_wck_mode|completed_reads|remaining_requests|hit_cycle_limit|peak_bandwidth_GBps|achieved_bw_GBps|cmd_validation|dfi_validation)\s*:' \
  outputs/delivery/lpddr6/stats.txt

rg '^(commands\.(ACT1|ACT2|CAS_RD|CAS_WR|RD|WR|WCK_SYNC|WCK_TRAIN|DVFS|REFdb)|split_activate|act2_deadline_forced|wck_|dvfs_|phy_lpddr_)\s*:' \
  outputs/delivery/lpddr6/stats.txt

sed -n '1,15p' outputs/delivery/lpddr6/timing.csv
sed -n '1,10p' outputs/delivery/lpddr6/commands.csv
```

预计：`standard : LPDDR6`、10667 Mbps、24-bit、BL24/prefetch 24、两个 pseudo-channel、
32B DRAM transaction，64B host line 拆成两个 transaction。命令面可出现 ACT1/ACT2 和
WCK/CAS 行为；默认 link protection 关闭。RL/WL、训练和保护开销未作目标器件校准时只能
形成架构趋势结论。

#### C.4.3 可视化和专用模式

```bash
python3 tools/visualize.py \
  --command-trace outputs/delivery/lpddr6/commands.csv \
  --dfi-trace outputs/delivery/lpddr6/dfi.csv \
  --stats outputs/delivery/lpddr6/stats.txt \
  --thermal-map outputs/delivery/lpddr6/thermal_map.txt \
  --title 'LPDDR6 delivery case' \
  --out outputs/delivery/lpddr6/dashboard.html
xdg-open outputs/delivery/lpddr6/dashboard.html

# 完整初始化/低功耗控制序列
./build-clang-debug/hbm_sim --config configs/run/lpddr6.cfg \
  --requests 256 --init-sequence lpddr6_full --init-sequence-interval 8 \
  | tee outputs/delivery/lpddr6/full_init.txt

# 链路保护/DBI/link ECC/CA parity 研究入口
./build-clang-debug/hbm_sim --config configs/run/lpddr6.cfg --requests 1024 \
  --lpddr-link-protection true --lpddr-dbi true \
  --lpddr-link-ecc true --lpddr-ca-parity true --lpddr-wck always_on \
  | tee outputs/delivery/lpddr6/link_protection.txt

# low DVFS 模式
./build-clang-debug/hbm_sim --config configs/run/lpddr6.cfg --requests 1024 \
  --lpddr-dvfs low --lpddr-wck burst_sync \
  | tee outputs/delivery/lpddr6/low_dvfs.txt
```

预计：`lpddr6_full` 形成成对的 PDE/PDX、SREFEN/SREFEX 控制行为；链路保护用例增加
interface overhead、降低 payload efficiency；low DVFS 改变模式/训练路径。若需要严格的
低速 timing 数值，应加载对应 low-DVFS profile，而不能只依赖模式标签。

#### C.4.4 LPDDR6 三后端人工查验

```bash
audit_case_backends configs/run/lpddr6.cfg \
  outputs/delivery/lpddr6/backend_audit
sed -n '1,12p' outputs/delivery/lpddr6/backend_audit/sparse/final.csv
```

三份 stats 的 `standard` 都应为 `LPDDR6`，backend line 应是 32 B DRAM transaction；
64 B host line 对应两个 transaction 的拆分结果不得因后端而变。还应对比
`split_activate`、`commands.ACT1/ACT2`和 `data_write_commits`，把 split-ACT 协议问题与存储持久化
问题分开定位。

---

## 附录 D：新 HBM4 自定义用例

本例假定一款名为 **HX-HBM4-9000-48G-16Hi** 的研究器件：9000 Mbps/pin、48Gb/die、
16Hi、两颗 stack。它用于展示完整配置流程，名称和数值不对应任何真实厂商；在获得厂家表
之前，所有新增 timing、功耗和热参数必须标为 `research_default`，不得用于器件级声明。

### D.1 定义研究问题和不可变条件

研究问题：在相同 70% read random workload 下，假定的 9000 Mbps HBM4 双 Stack 系统采用
Behavioral PHY 时，吞吐、延迟、行冲突、PHY 反压、接口开销和热点分布如何；Direct PHY、
blocked mapping 和文件后端会带来什么变化。

为保证实验可比较，固定 requests、seed、read ratio、地址映射、Controller 队列、timing、
功耗热参数；每次只改变一个模式。需要同时记录 Git commit、配置 SHA256 和完整命令。

### D.2 完整自定义配置文件

创建 `configs/run/hbm4_hx9000_2stack.cfg`，内容如下：

```ini
# 身份与来源：假定研究器件，不是真实 vendor profile
standard = hbm4
timing_profile = hx_hbm4_9000_48gb_16hi_research
timing_profile_file = configs/profiles/hbm4/jedec_8000_32gb_8hi.cfg
vendor_profile = hypothetical_hx_research
mode_profile = full_stack_crc16_ras_research
speed_bin_mbps = 9000
density_gb = 48
stack_height = 16
strict_timing_table = false

# 完整组织
data_rate_mbps = 9000
data_bus_bits = 2048
tick_multiplier = 2
tCK_ps = 444
channels = 32
pseudo_channels = 2
sids = 4
ranks = 1
bank_groups = 2
banks_per_group = 8
rows = 16384
columns = 32
line_size = 64
dram_transaction_bytes = 32
full_stack_model = true

# HBM4 协议能力
supports_refresh = true
supports_rfm = true
supports_ecc = true
hbm_full_32_channel_stack = true
hbm_sid_interleave = true
hbm_pc_interleave = true
hbm_edge_pairing = true
hbm_strict_edge_pairing = true
hbm_edge_pairing_matrix = hbm4_synthetic_full_matrix
hbm_sid_mapping = sid_per_4hi_slice
hbm_ecc_scheme = metadata_16b_per_32B_transaction
hbm_ras_policy = ras_metadata_sideband
hbm_link_crc_mode = crc16
hbm_link_retry_enabled = true
hbm_link_crc_bits_per_request = 16
hbm_ras_metadata_bits_per_request = 16
hbm_ecc_bits_per_request = 16
metadata_bits_per_request = 0
ecc_bits_per_request = 0

# Timing：全部为研究假定；真实交付必须由目标器件表逐项替换
timing_override_source = research_default
nBL = 2
nCL = 36
nCWL = 12
nRCDRD = 36
nRCDWR = 20
nRP = 36
nRPab = 36
nRAS = 64
nRC = 100
nRTP = 10
nWR = 34
nCCDS = 2
tCCDL_ns = 2.5
nCCDR = 4
nRRDS = 10
nRRDL = 12
nFAW = 36
nRTW = 30
nWTRS = 12
nWTRL = 16
tRFCab_ns = 560
tRFCpb_ns = 350
tRREFD_ns = 8
tREFI_us = 3.9
tREFIpb_ns = 60.9375
nRFMab = 1262
nRFMpb = 789
nMRW = 10
nMRR = 10
nECCSCRUB = 160
nRASERR = 96
nLINKRETRY = 40

# 两颗独立 Stack 和系统入口
stack_count = 2
stack_mapping = interleaved
stack_interleave_bytes = 256
stack_ingress_buffer_size = 256
stack_dispatch_width = 4
stack_qos_policy = strict_priority

# workload 与 Controller
pattern = random
requests = 20000
read_ratio = 70
seed = 900048
addr_stride = 64
inject_interval = 2
max_cycles = 100000000
address_mapping = RoBaRaCoCh
channel_mapper = decoded
read_buffer_size = 128
write_buffer_size = 128
priority_buffer_size = 4096
scheduler = frfcfs
row_policy = open_page

# Behavioral Mem PHY
mem_phy_mode = behavioral
dfi_version = 6.0.1
phy_command_fifo_depth = 64
phy_read_fifo_depth = 64
phy_write_fifo_depth = 64
phy_command_pipeline_cycles = 2
phy_read_return_pipeline_cycles = 2
phy_write_data_pipeline_cycles = 2
phy_reset_cycles = 8
phy_initialization_cycles = 16
phy_training_cycles = 32
phy_auto_train = true

# Refresh/RFM
refresh_policy = per_bank
rfm_policy = per_bank
rfm_act_threshold = 256
rfm_decrement = 256
refresh_postpone_limit = 4
refresh_pullin_limit = 4
refresh_credit_limit = 8
refresh_temperature_mode = normal

# Mem Stack、物理、功耗、热、ECC
memory_backend = sparse
memory_capacity_bytes = 0
sparse_density_warning_pct = 30
topology_stats_scan_limit = 100000
floorplan = true
subarrays_per_bank = 16
mats_per_subarray_x = 8
mats_per_subarray_y = 8
cells_per_mat_x = 512
cells_per_mat_y = 512
microbumps_x = 64
microbumps_y = 64
power_model = true
power_source = configured_pj
power_scale = 1.0
thermal_model = true
thermal_ambient_c = 40
thermal_cooling_per_cycle = 0.00025
thermal_rise_c_per_pj = 0.00002
thermal_grid_cols_per_tile = 4
thermal_grid_rows_per_tile = 4
thermal_coupling = true
thermal_lateral_coupling = 0.05
thermal_vertical_coupling = 0.10
thermal_tsv_coupling_scale = 1.25
thermal_tsvs_per_grid = 16
ecc_shadow = true
ecc_check_on_read = true
ecc_correct_single_bit = true
ecc_inject_period = 0

# 全套输出与验证
cmd_trace = outputs/hx9000/base/commands.csv
response_trace = outputs/hx9000/base/responses.csv
dfi_trace = outputs/hx9000/base/dfi.csv
dfi_signal_trace = outputs/hx9000/base/dfi_signal.csv
dump_timing_table = outputs/hx9000/base/timing.csv
dump_memory_image = outputs/hx9000/base/final_memory_{stack}.txt
dump_memory_csv = outputs/hx9000/base/final_memory_{stack}.csv
mismatch_report = outputs/hx9000/base/mismatch.txt
dump_thermal_map = outputs/hx9000/base/thermal_map_{stack}.txt
validate_cmd_trace = true
validate_dfi_trace = true
fail_on_data_mismatch = true
```

配置为什么合理：它显式给出身份、组织、全部核心 timing、系统/Controller/PHY、维护策略、
后端、物理/功耗/热和输出；64B host line 拆成两个 32B HBM transaction；两颗 stack 各自
拥有完整 32-channel Controller/PHY/MemoryImage。配置为什么仍不是真实器件：9000/48Gb/
16Hi timing、能量和热参数没有厂家证据，`strict_timing_table` 因此保持 false。

### D.3 配置审查和小规模预运行

```bash
mkdir -p outputs/hx9000/base
git rev-parse HEAD | tee outputs/hx9000/base/git_commit.txt
sha256sum configs/run/hbm4_hx9000_2stack.cfg \
  | tee outputs/hx9000/base/config.sha256

./build-clang-debug/hbm_sim \
  --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 128 --max-cycles 1000000 \
  | tee outputs/hx9000/base/smoke_stats.txt

rg '^(standard|timing_profile|vendor_profile|speed_bin_mbps|density_gb|stack_height|stack_count|controller_count|mem_phy_mode|phy_protocol|remaining_requests|remaining_pending|hit_cycle_limit|cmd_validation|dfi_validation|data_mismatches)\s*:' \
  outputs/hx9000/base/smoke_stats.txt
```

预计：standard HBM4、9000 Mbps、48Gb、16Hi、2 stack、Behavioral/HBM PHY；128 个 host
请求全部完成，`remaining_*` 为 0，未命中 cycle limit，command/DFI validation PASS，
data mismatch 为 0。任何一项不满足都应先修配置或实现，再运行 20000 请求。

### D.4 正式仿真命令和预计效果

```bash
./build-clang-debug/hbm_sim \
  --config configs/run/hbm4_hx9000_2stack.cfg \
  | tee outputs/hx9000/base/stats.txt
```

预计效果不是预设某个“必须达到”的实测数，而是以下不变量和趋势：

- 2 个 stack、每 stack 32 个 channel-local Controller/PHY，系统输出 per-stack 字段；
- 9000 Mbps × 2048 bit × 2 stack / 8000 对应约 4608 GB/s 聚合理论峰值；
- `achieved_bw_GBps` 小于理论峰值，且短/局部 workload 可能远低于峰值；
- CRC、RAS metadata、ECC 使 `achieved_if_bw_GBps` 高于 payload 带宽、效率低于 100%；
- random workload 产生 row hit/miss/conflict，FR-FCFS 尽量优先 ready/row-hit；
- Behavioral PHY 产生 HBM row/column、FIFO/pipeline/DFI 事件；
- 两个 stack 均应有完成量，是否均衡取决于 256B interleave 和地址分布；
- thermal peak 高于或等于 ambient，但绝对温度只代表当前研究参数。

### D.5 模式选择、命令和预期差异

#### D.5.1 PHY 模式

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --mem-phy behavioral \
  | tee outputs/hx9000/behavioral.txt

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --mem-phy direct \
  | tee outputs/hx9000/direct.txt
```

Behavioral 保留状态机、FIFO、2-cycle pipeline、adapter 和 DFI；Direct 是兼容旁路。
预计完成数据相同，但延迟、PHY 事件和可能的命令/数据反压不同。

#### D.5.2 调度器和行策略

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --scheduler fcfs --row-policy closed_page \
  | tee outputs/hx9000/fcfs_closed.txt

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --scheduler frfcfs --row-policy open_page \
  | tee outputs/hx9000/frfcfs_open.txt
```

预计 open-page/FR-FCFS 在有局部性的 workload 下提高 row hit；closed-page 会增加自动关闭
和后续 ACT。random workload 下提升幅度可能有限，必须用相同 seed 比较。

#### D.5.3 Stack 映射和 QoS

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --stack-mapping interleaved --stack-qos-policy strict_priority \
  | tee outputs/hx9000/interleaved.txt

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --stack-mapping blocked --stack-qos-policy fcfs \
  | tee outputs/hx9000/blocked.txt
```

预计 interleaved 更容易让两个 stack 同时活跃；blocked 是否均衡取决于地址是否跨越容量分区。
合成地址范围较小时，blocked 可能主要使用 stack 0，这是映射结果而非多 Stack 失效。

#### D.5.4 存储后端

```bash
./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --memory-backend mmap_sparse \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file 'outputs/hx9000/mmap/stack_{stack}.bin' \
  | tee outputs/hx9000/mmap_stats.txt

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --requests 4096 --memory-backend chunk_file \
  --memory-capacity-bytes 1073741824 \
  --memory-data-file 'outputs/hx9000/chunk/stack_{stack}.bin' \
  --memory-chunk-size 2097152 --memory-chunk-cache-entries 16 \
  | tee outputs/hx9000/chunk_stats.txt
```

预期：后端改变宿主保存/持久化行为，不改变 DRAM 命令和 payload。每个 stack 生成独立文件及
sidecar；1GiB 是本实验的后端容量限制，不是 48Gb×16Hi 的真实全容量声明。

#### D.5.5 双 Stack 三后端的人工查验

D.5.4 是正式 synthetic 性能运行；下面再用可重放数据 trace 做一次小容量语义审计。
该用例是双 Stack，因此 golden、导出和 backend 全部显式包含 `{stack}`：

```bash
mkdir -p outputs/hx9000/backend_audit/{sparse,mmap,chunk}

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --trace examples/data_check.trace --requests 0 \
  --memory-image examples/memory_image.txt \
  --memory-backend sparse --memory-capacity-bytes 1048576 \
  --dump-memory-image 'outputs/hx9000/backend_audit/sparse/final_{stack}.txt' \
  --dump-memory-csv 'outputs/hx9000/backend_audit/sparse/final_{stack}.csv' \
  | tee outputs/hx9000/backend_audit/sparse/stats.txt

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --trace examples/data_check.trace --requests 0 \
  --memory-image examples/memory_image.txt \
  --memory-backend mmap_sparse --memory-capacity-bytes 1048576 \
  --memory-data-file 'outputs/hx9000/backend_audit/mmap/data_{stack}.bin' \
  --dump-memory-image 'outputs/hx9000/backend_audit/mmap/final_{stack}.txt' \
  --dump-memory-csv 'outputs/hx9000/backend_audit/mmap/final_{stack}.csv' \
  --verify-golden 'outputs/hx9000/backend_audit/sparse/final_{stack}.txt' \
  | tee outputs/hx9000/backend_audit/mmap/stats.txt

./build-clang-debug/hbm_sim --config configs/run/hbm4_hx9000_2stack.cfg \
  --trace examples/data_check.trace --requests 0 \
  --memory-image examples/memory_image.txt \
  --memory-backend chunk_file --memory-capacity-bytes 1048576 \
  --memory-data-file 'outputs/hx9000/backend_audit/chunk/data_{stack}.bin' \
  --memory-chunk-size 65536 --memory-chunk-cache-entries 4 \
  --dump-memory-image 'outputs/hx9000/backend_audit/chunk/final_{stack}.txt' \
  --dump-memory-csv 'outputs/hx9000/backend_audit/chunk/final_{stack}.csv' \
  --verify-golden 'outputs/hx9000/backend_audit/sparse/final_{stack}.txt' \
  | tee outputs/hx9000/backend_audit/chunk/stats.txt
```

然后遍历两个 stack 查一致性，并检查文件四件套：

```bash
for stack_id in 0 1; do
  diff -u "outputs/hx9000/backend_audit/sparse/final_${stack_id}.txt" \
    "outputs/hx9000/backend_audit/mmap/final_${stack_id}.txt"
  diff -u "outputs/hx9000/backend_audit/sparse/final_${stack_id}.txt" \
    "outputs/hx9000/backend_audit/chunk/final_${stack_id}.txt"
done

ls -lh outputs/hx9000/backend_audit/{mmap,chunk}/data_*.bin*
stat --format='%n logical_bytes=%s allocated_blocks=%b block_bytes=%B' \
  outputs/hx9000/backend_audit/{mmap,chunk}/data_*.bin*
od -An -tc -N8 outputs/hx9000/backend_audit/mmap/data_0.bin.meta
od -An -tc -N8 outputs/hx9000/backend_audit/chunk/data_0.bin.meta
rg '^(memory_backend|stack_count|active_stacks|remaining_requests|hit_cycle_limit|data_mismatches|golden_verified|golden_mismatches|stack_[01]_.*)\s*:' \
  outputs/hx9000/backend_audit/{sparse,mmap,chunk}/stats.txt
```

通过标准是两个 stack 的两组 `diff` 均无输出，文件后端每 stack 都有 data/
`.init`/`.meta`/`.present`，magic 正确，所有数据/golden mismatch 为 0。如果该小 trace
未触达 stack 1，其 final image 为空但三后端仍应一致；是否实际利用两个 stack 要以
D.5.3 的 random 工作量和 `stack_N_*` 统计另行验证。更完整的原始文件解读及重开
步骤见 [6.5.2–6.5.6](#652-共同的可重放人工查验准备)。

### D.6 仿真过程查看

在另一个终端可以观察进程和产物增长：

```bash
# 查找正在运行的仿真进程和 CPU/内存占用
pgrep -af 'build-clang-debug/hbm_sim'
ps -o pid,etime,%cpu,%mem,rss,cmd -C hbm_sim

# 每秒观察 trace/统计文件大小；Ctrl+C 退出 watch
watch -n 1 'ls -lh outputs/hx9000/base/commands.csv outputs/hx9000/base/dfi.csv outputs/hx9000/base/stats.txt 2>/dev/null'

# command CSV 若被运行时刷新，可查看最新事件；缓冲时会分批出现
tail -n 10 -f outputs/hx9000/base/commands.csv
```

预期：进程 CPU 时间持续增加，command/DFI 文件随事件写出而增长；stdout 统计主要在仿真结束
时完整出现。如果文件暂时不增长，可能是 stdio 缓冲，不能仅据此判断死锁；应结合进程 CPU、
`max_cycles`、小规模复现和调试器判断。

### D.7 完整指标和参数查看

```bash
# 身份、组织、来源
rg '^(standard|timing_profile|timing_profile_file|vendor_profile|mode_profile|speed_bin_mbps|density_gb|stack_height|data_rate_mbps|data_bus_bits|channels|pseudo_channels|sids|stack_count|aggregate_capacity_bytes|timing_source_|timing_vendor_required)\s*:' \
  outputs/hx9000/base/stats.txt

# 完成性和性能
rg '^(system_cycles|host_requests|dram_transactions|injected_requests|completed_reads|completed_writes|remaining_requests|remaining_pending|hit_cycle_limit|peak_bandwidth_GBps|achieved_bw_GBps|achieved_if_bw_GBps|bandwidth_util_pct|payload_efficiency_pct|avg_read_latency)\s*:' \
  outputs/hx9000/base/stats.txt

# 调度、行行为、命令、维护
rg '^(row_hits|row_misses|row_conflicts|read_queue_len_avg|write_queue_len_avg|commands\.|refresh_|rfm_|dual_issue_cycles|row_bus_issues|column_bus_issues)' \
  outputs/hx9000/base/stats.txt

# PHY、DFI 和接口开销
rg '^(phy_|dfi_|interface_|hbm_|mem_phy_mode|payload_efficiency_pct)' \
  outputs/hx9000/base/stats.txt

# 后端、物理、功耗、热和 ECC
rg '^(memory_backend|storage_|rowbuf_|power_|thermal_|ecc_|floorplan_)' \
  outputs/hx9000/base/stats.txt

# per-stack 完成量、延迟、带宽、能量和温度
rg '^stack_[0-9]+_' outputs/hx9000/base/stats.txt

# 完整统计不筛选查看
less outputs/hx9000/base/stats.txt

# 最终 timing 表和关键研究默认值
sed -n '1,30p' outputs/hx9000/base/timing.csv
rg 'n(CL|CWL|RCDRD|RCDWR|RP|RAS|RC|FAW|RTW|WTR|RFC|RFM)' \
  outputs/hx9000/base/timing.csv
```

查看命令和请求过程：

```bash
# CSV 表头、前后事件和总行数
sed -n '1,10p' outputs/hx9000/base/commands.csv
tail -n 10 outputs/hx9000/base/commands.csv
wc -l outputs/hx9000/base/commands.csv outputs/hx9000/base/dfi.csv \
  outputs/hx9000/base/responses.csv

# 找第一个 refresh/RFM、写命令和 stack 1 事件
rg -m 5 ',(REF|RFM)' outputs/hx9000/base/commands.csv
rg -m 5 ',(WR|WRA),' outputs/hx9000/base/commands.csv
rg -m 5 ',1,' outputs/hx9000/base/commands.csv

# 验证和数据错误
rg '^(cmd_validation|dfi_validation|data_mismatches|uninitialized_read_bytes|ecc_)' \
  outputs/hx9000/base/stats.txt
sed -n '1,30p' outputs/hx9000/base/mismatch.txt
```

预计：完整 stats 可对应附录 B 每个字段；timing CSV 能看到最终 nCK 和研究来源；command
CSV 能把 request 追到 stack/channel/PC/SID/BG/bank/row；validator PASS 且非故障注入时
data mismatch 为 0。

### D.8 可视化运行和分析

#### D.8.1 生成性能基线（可选）

```bash
python3 tools/performance_curve.py \
  --binary ./build-clang-debug/hbm_sim --standards hbm4 \
  --intervals 1,2,4,8,16,32 --read-ratios 50,70,100 \
  --requests 1024 --pattern random --seed 900048 \
  --csv-out outputs/hx9000/base/performance_standard_hbm4.csv \
  --json-out outputs/hx9000/base/performance_standard_hbm4.json
```

注意：这是项目标准 HBM4 基线曲线，不自动加载 HX9000 自定义配置，dashboard 中应标注为
standard baseline。若要得到 HX9000 精确 sweep，需要扩展脚本使其传入该 config。

#### D.8.2 生成并打开 dashboard

```bash
python3 tools/visualize.py \
  --command-trace outputs/hx9000/base/commands.csv \
  --dfi-trace outputs/hx9000/base/dfi.csv \
  --stats outputs/hx9000/base/stats.txt \
  --performance-json outputs/hx9000/base/performance_standard_hbm4.json \
  --thermal-map outputs/hx9000/base/thermal_map_0.txt \
  --title 'Hypothetical HX-HBM4-9000 48Gb 16Hi / 2-stack' \
  --max-events 100000 \
  --out outputs/hx9000/base/dashboard.html

xdg-open outputs/hx9000/base/dashboard.html
```

#### D.8.3 预计可视化效果

- Run summary：显示 HBM4、Behavioral、2 stack、9000 Mbps、完成量、带宽/延迟和 PASS；
- Overview：命令密度随 random workload 展开，初始化/训练后开始普通访问；
- Bank view：可看到 HBM 行/列命令、edge pairing、ACT→RD/WR→PRE 和 refresh/RFM；
- Request swimlanes：64B host line 拆分的两个 32B transaction 命令可按 request 关联；
- Stack filter：stack 0/1 都应有事件，数量接近与否由地址和 interleave 决定；
- DFI activity：Behavioral PHY 的 command、write/read beat 非空；
- Performance curve：显示标准 HBM4 基线的饱和趋势，不能冒充 HX9000 自定义 sweep；
- Thermal map：当前页面显示 stack 0 并可在不同 layer 找热点；stack 1 可用
  `thermal_map_1.txt` 另生成页面，温度是研究参数下的趋势而非真实结温。

发现异常时按[第 7.3.7 节](#737-空白或异常页面排查)检查原始 CSV、范围和过滤器。

### D.9 验收模板

```text
用例名称：HX-HBM4-9000-48G-16Hi 双 Stack 研究用例
研究问题：
代码 commit：
构建器/构建类型：
CTest 结果：
配置文件：configs/run/hbm4_hx9000_2stack.cfg
配置 SHA256：
standard/profile/vendor/speed-bin：
参数来源分级：JEDEC / external / derived / research_default
完整命令：
随机种子：900048
输入 trace/image：synthetic random / 无初始 image
host_requests：
dram_transactions：
completed_reads/writes：
remaining_requests/pending：
hit_cycle_limit：
cmd_validation：
dfi_validation：
data_mismatches：
理论峰值与计算公式：
achieved payload/interface bandwidth：
平均读延迟与队列：
row hit/miss/conflict：
PHY FIFO/反压：
per-stack 负载均衡：
功耗/热结论及校准状态：
Direct/Behavioral 对照结论：
后端一致性结论：
可视化结论：
已知限制：假定器件，未进行厂商/电气/硬件校准
产物目录：outputs/hx9000/base
验收人/日期：
```

一个合格的自定义用例至少应满足：所有请求完成、未命中 cycle limit、命令/DFI 验证通过、
无非预期数据 mismatch、关键参数来源明确、理论峰值口径正确、模式差异可解释，并可由固定
配置、seed、commit 和命令重放。只有获得真实厂家或硬件证据并完成误差评估后，才能把
`hypothetical_hx_research` 改为真实 vendor identity。

---

## 附录 E：交付验收清单

- [ ] Clang 和 G++ 分别在独立目录构建成功；
- [ ] `ctest` 当前全部通过并保存日志；
- [ ] 四标准主配置均完成且未命中 `max_cycles`；
- [ ] 单 stack 与多 stack 的路由、容量和统计聚合检查通过；
- [ ] Direct/Behavioral 对照差异可解释；
- [ ] sparse/mmap_sparse/chunk_file 的最终 payload 一致；
- [ ] command 和 DFI validator 通过；
- [ ] 写后读、mask、expect、golden、mismatch 退出码通过；
- [ ] Timing `t-1/t` 边界矩阵通过；
- [ ] 性能曲线呈现合理饱和趋势；
- [ ] 功耗/热结果标明是否器件级校准；
- [ ] 外部差分记录参考版本、配置、规范化和 unsupported 项；
- [ ] dashboard 可离线打开，关键视图非空；
- [ ] 最终参数表、命令、commit、输入和产物完整归档。

---

## 附录 F：代码与文档索引

| 内容 | 位置 |
|---|---|
| 公共头文件 | `include/hbm_sim/` |
| CLI 与参数解析 | `src/cli/` |
| 系统/存储/运行核心 | `src/core/` |
| Controller | `src/controller/` |
| DRAM traits/profile/semantics | `src/dram/` |
| Mem PHY | `src/phy/`、`include/hbm_sim/dram/mem_phy.hpp` |
| Frontend/trace | `src/frontend/` |
| Stats | `src/stats/` |
| 验证器 | `src/validation/`、`tests/`、`tools/` |
| 可运行配置 | `configs/run/` |
| profiles/calibration | `configs/profiles/`、`configs/calib/` |
| 验证配置 | `configs/validation/` |
| 示例输入 | `examples/` |
| 持久输出 | `outputs/` |
| 构建入口 | `CMakeLists.txt`、`Makefile` |

本手册不复制 JEDEC 或厂商保密手册。引用外部标准时应由交付方另行保存合法获得的版本、
章节和校准记录。
