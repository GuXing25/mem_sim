# 堆叠存储仿真模型验证依据与可信度论证

> 项目：hbm_sim  
> 适用模型：HBM3、HBM4、LPDDR5、LPDDR6  
> 文档性质：独立验证依据、可信度论证、接收判据和后续校准路线  
> 当前代码基线：Git 4190c32a3054  
> 内部验收日期：2026-08-15  
> Ramulator2.1 外部差分补验日期：2026-08-16  
> 结论等级：研究型周期级模型可行；未达到具体厂商器件精度认证

---

## 0. 2026-08-16 增量验证状态（优先于本文旧快照）

本轮已经把原路线中的 P0～P3 从“建议”推进为可执行工具和证据；本文后面仍出现的
旧验收快照已由本轮实现和自动化结果取代，以本节为准：

| 新增证据 | 实测结果 | 产物 |
| --- | ---: | --- |
| 四标准 Timing `t-1` 禁止/`t` 允许及 scope 边界 | 315/315 行，8/8 检查 | `timing_boundaries.csv/json` |
| Ramulator2.1 四标准共同面差分 | 225/225 检查 | `ramulator2_diff_v2.json` |
| 注入率—延迟—吞吐量曲线 | 84 点，32/32 趋势检查 | `performance_curves.csv/json` |
| Timing 单因素敏感性 | 72 点 | `sensitivity.csv` |
| 联合输入不确定性传播 | 每协议 24 样本，34/34 总检查 | `sensitivity_uncertainty.json` |
| DRAMsim3 HBM2 公共命令/IDD/热辅助验证 | 9/9 检查 | `dramsim3_aux.json` |
| 默认 CTest | 7/7 | CTest 输出 |

实现过程中差分测试实际发现并修复了两个模型缺陷：LPDDR 写激活路径误用
`nRCDRD`，以及 LPDDR5 在总线换向 gate 尚未结束时重复发送 CAS。它还修正了 HBM3
参考配置的 SID 数和 `nRREFD` 单位。这说明新增工作不是只补文档，而是测试、模型、
配置和外部执行链共同落地。

## 1. 文档目的

本文件专门回答：

> 在拿不到目标厂商手册、厂商 Verilog 模型和真实硬件测量，只拥有 JEDEC 标准及
> Ramulator2/DRAMsim3 等开源模拟器时，依据什么判断 hbm_sim 模型可行？

本文件不把“程序运行成功”“两个模拟器输出相同”或“采用 JEDEC 字段名”中的任何
一项单独当作充分证明，而是建立一条相互独立、可重复、可审计的证据链：

~~~text
JEDEC 可见规则
  + 内部状态、数据和统计一致性
  + 独立解析公式
  + Ramulator2 共同面差分
  + 变形测试和参数敏感性
  + 系统级性能趋势
  + 参数来源与未知项治理
  = 研究模型可行性
~~~

文档同时规定每类证据能够证明什么、不能证明什么，防止把内部一致性错误地扩大为
器件精度，把参考模拟器错误地当作规范裁判。

## 2. 最终结论摘要

### 2.1 当前能够成立的结论

截至 2026-08-16，当前证据可以支持以下结论：

1. hbm_sim 能在独立 CMake 构建目录完成 C++20 构建，默认 7 项 CTest 回归通过。
2. 四类标准入口均能运行；请求、命令、数据、后端和统计路径不存在已知回归失败。
3. 项目定义的 ACT/PRE/RD/WR、refresh、RFM、HBM pairing、LPDDR split activate、
   WCK/CAS、低功耗等已覆盖路径满足项目状态机和 Timing gate。
4. 真实 payload、初始化掩码、masked write、read forwarding、golden image、
   mismatch 退出码和三种后端持久化已通过正反例测试。
5. 容量、接口速率、理论峰值、事务拆分和固定 HBM4 单通道延迟/带宽具有独立解析
   检查，不仅依赖 C++ 模型自身输出。
6. 在刻意统一组织、Timing、地址映射、调度和维护开关后，HBM3、HBM4、LPDDR5、
   LPDDR6 与 Ramulator2.1 的共同面合计 225/225 检查通过。
7. 项目把 JEDEC、vendor、derived、research_default 分开记录，当前未知厂商参数
   没有被隐藏为已校准值。
8. 因此模型适合用于控制器架构、地址映射、调度、维护策略、数据正确性、接口开销
   和相对趋势研究。

### 2.2 当前不能成立的结论

当前证据不能支持：

- 精确复现某家厂商、某个料号、某个 speed-bin 的 HBM4/LPDDR6。
- HBM3/LPDDR5 已完成完整 JEDEC 数值级校准。
- DFI 输出已经通过完整 DFI 5.x pin-level 合规认证。
- ECC/RAS/link retry 模型能够预测真实 FIT、纠错率或可靠性。
- 默认 pJ/IDD 和 RC 参数能给出真实封装绝对功耗、温度或热点位置。
- Ramulator2.1 与本项目一致即可替代 JEDEC 或真实器件。
- DRAMsim3 的 HBM2 辅助结果不能外推成 HBM3/HBM4/LPDDR5/LPDDR6 器件级验证。

### 2.3 推荐正式声明

建议论文、答辩或交付报告使用：

> hbm_sim 是面向 HBM3/HBM4/LPDDR5/LPDDR6 的研究型周期级内存模型。模型的
> 软件执行、状态机、数据语义、解析公式、来源治理和固定性能回归已通过项目自动化
> 验证；在统一 HBM4 单通道参考配置下，其 Timing、命令序列、地址坐标和发令周期
> 与 Ramulator2.1 的共同抽象面一致。HBM4/LPDDR6 采用 JEDEC-oriented 配置并显式
> 保留 research defaults，因缺少目标厂商资料和真实硬件，结果用于架构研究和相对
> 趋势分析，不作为具体器件绝对延迟、功耗或温度认证。

## 3. “模型可行”的操作性定义

“可行”必须拆成可以验证的命题，而不是主观评价。

| 层级 | 名称 | 核心问题 | 当前状态 | 通过后允许声明 |
| --- | --- | --- | --- | --- |
| F0 | 软件可执行 | 能否构建、运行、结束、复现 | 已完成 | 软件平台可用 |
| F1 | 内部一致 | 状态、数据、队列、统计是否自洽 | 已完成已覆盖范围 | 项目语义自洽 |
| F2 | 标准导向 | JEDEC 可见组织、命令、边界是否落实 | 核心 Timing 自动边界已完成；章节绑定仍待标准原文 | JEDEC-oriented |
| F3 | 外部共同面 | 独立模拟器在对齐范围是否一致 | 四标准 Ramulator + DRAMsim3 辅助完成 | 共同抽象可对照 |
| F4 | 趋势可信 | 参数和负载变化是否产生可解释趋势 | 负载曲线和输入不确定性已自动化 | 可做声明范围内相对趋势研究 |
| F5 | 器件校准 | 是否预测目标料号真实数值 | 未完成 | 暂不能作器件声明 |
| F6 | 硬件外推 | 是否预测真实系统行为 | 未完成 | 暂不能作硬件声明 |

本项目当前的顶层可信度主张是：

~~~text
G0：hbm_sim 是可用于架构和相对趋势研究的周期级堆叠存储模型。
~~~

G0 由以下子主张共同支撑：

| 子主张 | 内容 | 主要证据 |
| --- | --- | --- |
| G1 | 软件实现能够稳定、确定地执行 | CMake、CTest、smoke |
| G2 | 命令状态和 Timing gate 在已覆盖路径内部一致 | sequence tests、command validator |
| G3 | 数据和后端语义正确 | payload/golden/mismatch/backend tests |
| G4 | 关键数值具有独立解析依据 | model_validation.py |
| G5 | 四标准共同命令面及较老 HBM 公共面获得独立实现交叉支持 | Ramulator2/DRAMsim3 differential |
| G6 | 项目扩展能力有自己的验证，不依赖参考模拟器背书 | project_identity.csv |
| G7 | 未知参数和结论边界显式可见 | timing source、vendor_parameters.csv |

只有 G1～G7 同时成立，才能支持 G0。任何单项都不是充分条件。

## 4. 证据来源及权威顺序

### 4.1 证据优先级

发生冲突时采用：

~~~text
目标器件厂商资料/实测
  > JEDEC 规范性规则和所选模式表
  > 可独立推导的数学与物理约束
  > 多个独立模拟器在严格对齐条件下的共同结果
  > 单个参考模拟器
  > 论文示例或公开研究参数
  > 项目 research_default
~~~

这里的“优先”不是说厂商资料可以违反 JEDEC，而是指器件允许范围内的实际
speed-bin、模式和 guardband 由厂商资料决定；JEDEC 决定标准允许的命令和规则。

### 4.2 当前直接依据

| 依据 | 当前使用方式 | 独立性 | 边界 |
| --- | --- | --- | --- |
| JESD270-4:2025 | HBM4 组织、命令面、部分固定规则和换算 | 规范性外部来源 | 不是 vendor speed-bin |
| JESD209-6:2025 | LPDDR6 组织、BL24、模式和部分 Timing | 规范性外部来源 | 需确认目标 MR/温度分支 |
| Ramulator2.1 | 四标准单通道共同面差分 | 独立代码实现 | 非规范性参考 |
| DRAMsim3 | HBM2 公共命令、IDD 公式及热趋势辅助验证 | 独立代码实现 | 不代表同代器件验证 |
| 独立 Python 验收 | 容量、带宽、延迟、敏感性和证据清单 | 与 C++ 热路径分离 | 配置输入仍需来源审计 |
| C++ sequence tests | 精确状态、Timing、数据和模块边界 | 实现级白盒/灰盒证据 | 不能独立证明标准数值 |
| CLI smoke | 端到端输入输出和错误路径 | 黑盒证据 | 多数只检查关键字段/关系 |

Ramulator2.1 的公开设计强调细粒度 device Timing、controller scheduling 和系统级
latency-throughput 验证，适合作为本项目外部交叉验证方法参考：
[Ramulator2.1 论文](https://arxiv.org/abs/2606.13844)、
[Ramulator2 官方仓库](https://github.com/CMU-SAFARI/ramulator2)。

DRAMsim3 官方模型覆盖 DDR3/4、LPDDR3/4、GDDR5/6、HBM 等协议，并提供命令 trace、
功耗、热和部分 Verilog validation 路径；它不应被强行当作 HBM4/LPDDR6 默认数值
标准：[DRAMsim3 官方仓库](https://github.com/umd-memsys/dramsim3)、
[DRAMsim3 论文](https://user.eng.umd.edu/~blj/papers/cal2020.pdf)。

HBM4 公开发布信息对应 JESD270-4:2025，LPDDR6 对应 JESD209-6:2025：
[HBM4 发布公告](https://www.businesswire.com/news/home/20250416843598/en/)、
[LPDDR6 发布公告](https://www.businesswire.com/news/home/20250709315796/en/)。
当前仓库的部分注释和说明文件将 HBM4 写为“JESD270-4A”，但项目内没有附带
可独立核验该后缀的正式标准文件。因此本验证基线使用公开可核验的
JESD270-4:2025 版本标识；“-4A”在完成原始文件号、修订日期和差异页核对前，
只能当作项目内部来源标签，不得用于声称已按某个修订版完成合规验证。

## 5. 验证对象分解

模型验证不能只检查最终带宽，需要逐层定位。

~~~text
配置来源
  -> 组织/容量
  -> 地址解码
  -> 请求拆分
  -> Bank 状态
  -> 命令前置条件
  -> Timing scope/gate
  -> 调度和维护
  -> 请求完成
  -> 真实数据
  -> DFI/接口事件
  -> 系统统计
  -> 功耗/热/ECC 扩展
~~~

| 验证域 | 必须回答的问题 |
| --- | --- |
| 配置 | 最终值来自哪里，覆盖顺序是什么 |
| 组织 | channel/PC/SID/BG/bank/row/column 是否一致 |
| 地址 | 同一地址是否稳定映射到预期坐标 |
| 事务 | host line 是否按标准事务粒度正确拆分 |
| 状态 | 命令前置条件和状态转换是否正确 |
| Timing | t-1 是否禁止、t 是否允许，scope 是否正确 |
| 调度 | ready、row hit、arrival 和维护优先级是否符合策略 |
| 数据 | 读回、mask、forward、初始化状态是否正确 |
| 系统 | 多 channel/Stack 是否隔离且正确聚合 |
| 输出 | 指标口径、单位和分母是否正确 |
| 扩展 | DFI、功耗、热、ECC 是否与核心协议解耦且自洽 |

## 6. 当前验证基线和实测证据

### 6.1 版本与环境

| 项目 | 当前记录 |
| --- | --- |
| hbm_sim Git | 4190c32a3054（源码/配置/测试基线） |
| Ramulator2 Git | b30320bc9385 + 构建代码生成后工作树 |
| hbm_sim compiler | Clang 18.1.8 |
| Ramulator2 compiler | GCC 15.2.0 |
| CMake | 4.2.3 |
| Ninja | 1.13.2 |
| Python | 3.14.4 |
| 平台 | Linux x86_64 |

正式复现必须同时记录两个仓库 commit、编译器、配置、随机种子、trace、容差和输出，
不能只写“使用 Ramulator”。

当前 Ramulator2 构建会重新生成 src/ramulator/dram/impl 下的实现文件；本次差分
使用的 build-hbm-diff 对应 15 个已跟踪生成文件变更，完整 diff 的 SHA-256 为：

~~~text
a1bca35f9c618e13e7e25c45a5e2606098e78bc6fe4b32ad892e3bb86685f19b
~~~

这是当前外部证据的复现注记，不是能够忽略的杂项。释放正式证据包时，应在干净
worktree 中重现构建，并将代码生成器版本、生成命令和 diff/hash 一并保存。

### 6.2 自动化回归

独立验收构建：

~~~bash
cmake -S . -B build-delivery-validation -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18 \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-delivery-validation --parallel
ctest --test-dir build-delivery-validation --output-on-failure
~~~

实测：

~~~text
sequence_tests、timing_boundary_tests、smoke、timing_boundary_validation、
model_validation、performance_curve_validation、sensitivity_uncertainty_validation 均 Passed
100% tests passed, 0 failed out of 7
~~~

顶层 CTest 是 7 个聚合入口，内部实际覆盖：

| 类型 | 数量 | 作用 |
| --- | ---: | --- |
| sequence test 调用 | 54 | 精确命令、状态、Timing、数据和模块关系 |
| smoke 命名场景 | 37 | 四标准、CLI、文件、错误路径和端到端统计 |
| model validation | 28 | 解析公式、固定阈值、数据、DFI、来源和身份 |

### 6.3 28 项项目原生验收

执行：

~~~bash
python3 tools/model_validation.py \
  --binary ./build-delivery-validation/hbm_sim \
  --json-out outputs/validation/model_validation.json
~~~

核心实测：

| 项目 | 实测 | 判定 |
| --- | ---: | --- |
| HBM4 单 channel 理论峰值 | 64.000 GB/s | 与独立公式一致 |
| 每 pin 速率 | 8.000 Gbps | 与配置一致 |
| Timing 表 | 46 项 | 来源均合法 |
| research defaults | 19 项 | 未被隐藏 |
| ACT→RD | 60 ticks | 与期望相同 |
| closed-row read | 125 ticks | 与期望相同 |
| DFI read latency | 121 ticks | 与期望相同 |
| sustained payload BW | 41.318 GB/s | 固定回归基线 |
| bandwidth utilization | 64.56% | 高于 60% 回归门槛 |
| nCL sensitivity | 117→133 | 输入 +16、结果 +16 |
| full-stack 理论峰值 | 2048.0 GB/s | 32 channel 公式一致 |
| data mismatch 负例 | return code 3 | 错误路径正确 |
| file backend reopen | mmap/chunk true | 持久化通过 |
| identity evidence | missing={} | 10 项能力均有证据 |

完整结果为 28/28 passed。60% 是项目固定 workload 的回归门槛，不是 JEDEC 或
厂商保证效率。

### 6.4 Ramulator2.1 四标准共同面差分

配置：

- hbm_sim：四个 `configs/validation/*_reference_1ch.cfg`。
- Ramulator2：HBM3/HBM4/LPDDR5/LPDDR6 对应 preset。
- 单 channel、32 B transaction、FR-FCFS、open-page、decoded mapping。
- 关闭 refresh、RFM、ECC、floorplan、power 和 thermal 干扰。
- 默认周期容差：±2 hbm_sim ticks；当前 HBM tick_multiplier=2，约等于 ±1 nCK。

执行：

~~~bash
python3 tools/ramulator2_differential.py \
  --binary ./build-delivery-validation/hbm_sim \
  --ramulator-root /home/guxing/learn/memorystack/ramulator2 \
  --json-out outputs/validation/ramulator2_diff_v2.json
~~~

结果为 225/225。每个协议覆盖 resolved Timing、closed read/write、row hit/conflict、
same/different BG、读写双向换向、四/五次激活与 tFAW、自动预充电、refresh；协议支持
时还覆盖 RFM、并行 pseudo-channel 和 SID。报告同时保存规范化事件和原始事件，
LPDDR split activate 的中间阶段及 HBM 并行 PC 的局部容差均有具名原因，而不是全局
放宽阈值。

该证据只能支持：

~~~text
在这些单通道、无项目扩展的四标准共同配置下，
Timing、基础命令序列、地址坐标和发令周期可比较。
~~~

它不支持：

~~~text
真实 payload、DFI、三种后端、多 Stack、ECC、功耗、热、
完整 refresh/RFM、厂商器件和硬件性能已经被 Ramulator 验证。
~~~

## 7. JEDEC 规则验证依据

### 7.1 组织和容量

组织类规则通过配置、地址解码和独立公式三方验证。

HBM4 默认容量：

~~~text
32 channel × 2 PC × 2 SID × 1 rank × 2 BG × 8 bank
× 16384 row × 32 column × 32 B = 32 GiB
~~~

LPDDR6 默认容量：

~~~text
1 channel × 2 subchannel × 1 SID × 1 rank × 4 BG × 4 bank
× 65536 row × 64 column × 32 B = 4 GiB
~~~

HBM4 和 LPDDR6 都区分：

~~~text
64 B host/frontend line
32 B DRAM transaction
1 host request -> 2 transactions
~~~

对应证据：

- test_hbm4_host_line_transaction_split。
- test_lpddr6_host_line_transaction_split。
- project_full_stack_scope。
- peak_bandwidth_formula。
- AddressMapper 坐标测试。

### 7.2 状态机规则

当前命令集覆盖 ACT、ACT1、ACT2、PRE/PREPB/PREAB、CASRD/CASWR、RD/WR、
RDA/WRA、REFAB/REFPB/REFDB、RFMAB/RFMPB、MRW/MRR、WCK sync/training、
DVFS、低功耗和 RAS/ECC 控制入口。

验证方法：

1. 构造 closed/open/activating 状态。
2. 检查 prerequisite command。
3. 在非法状态尝试命令并要求拒绝。
4. 发出合法命令，检查状态和 ready time。
5. 用离线 validator 重放实际 command trace。

当前证据：

- test_command_state_machine。
- test_command_executor_state_transitions。
- test_control_command_state_and_validator。
- test_initialization_control_sequence_execution。
- test_hbm4_edge_pairing_validator_matrix。
- test_lpddr6_cas_read_sequence。
- test_lpddr5_cas_write_sequence。

### 7.3 Timing 边界验证

规范级 Timing 测试的目标形式应是：

~~~text
preceding command at t0
following command at t0 + constraint - 1：必须禁止
following command at t0 + constraint：必须允许
~~~

核心矩阵：

| 约束 | 前命令 | 后命令 | 主要 scope |
| --- | --- | --- | --- |
| nRCDRD | ACT/ACT2 | RD | bank |
| nRCDWR | ACT/ACT2 | WR | bank |
| nRP | PRE | ACT | bank |
| nRAS | ACT | PRE | bank |
| nRC | ACT | ACT | bank |
| nRTP | RD | PRE | bank |
| nWR | WR | PRE | bank |
| nCCDS/L | RD/WR | RD/WR | PC/BG |
| nRRDS/L | ACT | ACT | rank/BG |
| nFAW | ACT×4 | ACT×5 | activation scope |
| nWTRS/L | WR | RD | BG/PC |
| nRTW | RD | WR | PC |
| nRFC | REF | ACT/访问 | channel/rank |
| nREFDB2ACT | REFDB | ACT | dual-bank scope |
| nWCK2CK/nCAS | CAS/WCK | RD/WR | WCK scope |

`timing_boundary_tests` 会枚举四个默认标准的全部 active `TimingConstraint`，对普通
约束检查内部 tick 的 `t-1` 禁止、`t` 允许和错误 scope 不受阻；对窗口约束另行检查
tFAW 第五次激活边界。本次产生 315 行：HBM3 59、HBM4 73、LPDDR5 91、LPDDR6 92，
315/315 通过；10 条零延迟/不适用规则显式跳过。核心参数覆盖检查 8/8 通过。

这已经完成“项目约束表的自动边界验证”，但尚不能写成“完整 JEDEC 全章节合规”：
每条规则对应的标准章节/表格仍需在取得可引用标准原文后逐项绑定。

### 7.4 来源审计

TimingValueSource 只有：

~~~text
JEDEC
vendor
derived
research_default
~~~

当前 HBM4 native 验收表有 46 项，其中 19 项 research_default。验证脚本要求来源
合法，并故意检查 research defaults 仍可见。该机制防止“模型能跑”被误写为
“器件已校准”。

HBM3/LPDDR5 当前主要来自 Ramulator2-derived 研究基线；HBM4/LPDDR6 是
JEDEC-oriented 与研究值的组合。只有真实可追溯厂商资料才允许标为 vendor。

## 8. 内部一致性验证依据

### 8.1 为什么内部测试仍然必要

外部模拟器通常不覆盖本项目的真实 payload、文件后端、DFI payload、物理坐标和
多 Stack 扩展。因此这些能力必须由项目自己的独立断言验证。

内部测试能证明实现符合项目定义，但如果验证器与被测实现共享同一错误参数，可能发生
共同模式错误。因此内部证据必须与解析公式、JEDEC 和外部差分组合使用。

### 8.2 数据正确性

必须满足：

~~~text
写入 payload 后读回相同 byte
masked write 只改变 mask 覆盖 byte
未初始化 byte 被单独记录
read forwarding 返回最新未提交写
先到读不被后到重叠写越过
golden init mask 和 payload 独立比较
错误 expect 默认返回退出码 3
~~~

当前证据：

- test_real_storage_write_read_correctness。
- test_overlapping_read_before_write_ordering。
- test_real_storage_read_forward_correctness。
- test_real_storage_masked_write_correctness。
- test_memory_image_text_checkpoint_and_mismatch_report。
- data_mismatch_exit_code。
- project_storage_payload_semantics。

验收门槛：所有要求检查的数据用例 data_mismatches=0；注入错误的负例必须被捕获。

### 8.3 三种后端等价

协议状态和数据语义由 MemoryImage 定义，sparse、mmap_sparse、chunk_file 只改变
宿主保存介质。

等价性要求：

~~~text
相同初始镜像 + 相同 trace
  -> 相同读回数据
  -> 相同 init mask
  -> 相同 version/last writer
  -> 相同 command trace
  -> 相同协议统计
~~~

当前 mmap/chunk 已通过写入、关闭、重开和 golden 验证。文件格式版本、line size
或容量不一致时必须拒绝，而不能静默重解释。

### 8.4 多通道和多 Stack

要求：

- 每个 [stack][channel] controller 状态独立。
- 每个 Stack MemoryImage/backend 独立。
- local/global address 可追踪。
- 系统聚合统计等于各 Stack 合理求和。
- 一个 Stack 反压不应错误阻塞其他 Stack。
- strict-priority QoS 只在配置作用域内改变分发顺序。

当前证据：

- test_multi_controller_parallel_channels。
- test_active_six_stack_memory_system_routing_qos_and_stats。
- test_passive_multistack_memory_model_isolation。
- test_physical_storage_multichannel_memory_system。
- hbm4_6stack smoke：stack_count=6、controller_count=192。

### 8.5 DFI 自洽

当前 DFI 是 Behavioral PHY 在线完成事件或 Direct 兼容路径生成的 MC/PHY 边界研究视图。validator 检查：

- COMMAND 与 READ_DATA/WRITE_DATA 对应。
- beat 数、phase、lane byte。
- Direct 模式的配置延迟，以及 Behavioral 模式的真实 PHY completion cycle。
- enable/valid/mask。
- request_payload 与 memory_image payload source。
- expected payload 的独立比较。
- Stack-local 和 system address。

当前证据：`phy_tests` 覆盖 HBM3/HBM4/LPDDR5/LPDDR6、训练禁止/允许边界、
FIFO 反压/零深度负例、DVFS/WCKTRAIN、真实 payload 写读往返和 DFI validator；
原有 command/DFI validator、mask/source 及故障注入反例继续保留。

## 9. 独立解析验证

### 9.1 解析公式

以下指标不应只相信模型输出：

~~~text
Capacity = organization product × transaction_bytes
Peak_BW = data_rate_mbps × data_bus_bits / 8000
Interface_rate_Gbps = data_rate_mbps / 1000
Storage_density = unique_written_lines / addressable_lines
Payload_efficiency = payload_bytes / interface_bytes
~~~

工具 tools/model_validation.py 作为外部 Python 进程读取 CLI 输出并重新计算，降低与
C++ 热路径共享实现错误的风险。

### 9.2 守恒关系

每次运行至少检查：

~~~text
completed_reads + completed_writes <= dram_transactions
hit_cycle_limit=false 时 remaining_requests=0
hit_cycle_limit=false 时 remaining_pending=0
64 B host request 对 HBM4/LPDDR6 产生 2 个 32 B transaction
interface bytes >= payload bytes
payload bandwidth <= theoretical peak（口径一致时）
aggregate capacity = per-stack capacity × stack_count
~~~

如果 read forwarding 或 write coalescing 避免真实 DRAM data command，逻辑完成数与
物理 RD/WR 数可以不同；这必须由 read_forwards/write_coalesces 解释，不能机械判错。

### 9.3 延迟下界

短序列可以用命令路径计算可解释下界：

~~~text
closed-row read：
  ACT gate + nRCDRD + RD + read completion latency

row-hit read：
  column timing gate + RD + read completion latency

row-conflict read：
  PRE gate + nRP + ACT gate + nRCDRD + RD + completion
~~~

具体 tick 必须包含 tick_multiplier、HBM edge、DFI/read completion 定义；报告要列出
公式口径，不能只比较一个最终平均延迟。

## 10. 外部差分验证方法

### 10.1 为什么差分有效

两个独立实现接收相同配置和请求，如果 Timing、地址、状态、命令和周期逐层一致，
可以显著降低单实现编码错误的可能性。但两个模型也可能共享相同的标准理解错误，因此
差分是增强证据，不是数学证明。

### 10.2 必须先对齐的变量

直接比较两个默认配置没有意义。至少锁定：

| 类别 | 必须一致 |
| --- | --- |
| 标准 | generation、density、stack height、speed-bin |
| 组织 | channel、PC、SID、rank、BG、bank、row、column |
| 粒度 | host line、transaction byte、burst |
| 时钟 | tCK、nCK、tick multiplier 和周期原点 |
| 映射 | address mapping、channel mapping |
| 控制器 | scheduler、row policy、queue、write drain |
| 维护 | refresh/RFM 开关、策略和初始相位 |
| trace | 地址、类型、注入 cycle、请求顺序 |
| 统计 | system cycle、controller cycle、payload/interface byte |

### 10.3 比较顺序

~~~text
Timing table
  -> decoded coordinates
  -> command names
  -> command issue cycles
  -> completion cycles
  -> row statistics
  -> latency-throughput curve
~~~

前一层不同就先停止解释后一层。否则最终带宽差异无法定位。

### 10.4 下一阶段差分矩阵

当前四场景是基础证明。完整研究模型建议扩展：

| 分类 | 场景 | 精确比较 |
| --- | --- | --- |
| 单 bank | closed R/W、hit、conflict、AP | 命令、坐标、cycle |
| BG | same/different BG | nCCD/nRRD |
| 总线换向 | R→R、W→W、R→W、W→R | nWTR/nRTW |
| 激活 | 四次 ACT、第五次 ACT | nFAW |
| 并行 | bank/PC/SID/channel | scope 与并行周期 |
| 维护 | REFpb/ab、REFDB、RFM | PRE、恢复周期 |
| HBM | edge/PRE pairing | rising/falling 规则 |
| LPDDR | ACT1/2、CAS/WCK、DVFS | 控制序列 |
| 调度 | FCFS/FR-FCFS、row policy | 选择顺序 |
| 系统 | 注入率和局部性 sweep | 延迟/吞吐曲线 |

### 10.5 差异裁决

| 观察 | 处理 |
| --- | --- |
| hbm_sim 不同，两个独立参考一致 | 优先检查 hbm_sim |
| hbm_sim 与一个参考一致 | 检查第三方配置和抽象 |
| 三者都不同 | 检查是否真正对齐、统计口径是否相同 |
| 开源模型与 JEDEC 冲突 | 规范规则优先 |
| JEDEC 不给具体器件值 | 保留不确定性，不能投票产生 vendor 值 |

差异必须归类为：

~~~text
配置差异
时间基准差异
地址映射差异
抽象粒度差异
调度策略差异
实现缺陷
标准理解差异
未知
~~~

只有“实现缺陷”才直接修改代码；其余项先修正实验或记录边界。

## 11. DRAMsim3 的正确定位

### 11.1 当前不能直接做什么

DRAMsim3 官方常用模型是 DDR3/4、LPDDR3/4、GDDR5/6、HBM 等较早协议。本项目当前
重点为 HBM3/4、LPDDR5/6，因此不能把 DRAMsim3 的 HBM/LPDDR4 默认配置直接当作
HBM4/LPDDR6 的逐周期 oracle。

### 11.2 当前可以做什么

- 独立检查 bank/channel 状态机和 command queue 方法。
- 对较老共同协议或专门构造的公共抽象做趋势对照。
- 检查 VDD×IDD×时间功耗公式。
- 对比命令事件到热网格的映射和归一化温升趋势。
- 借鉴 command trace 到 Micron Verilog testbench 的验证流程。
- 检查 latency-throughput 曲线形状和饱和行为。

### 11.3 报告方式

在没有同代协议和同参数时，只能写：

~~~text
参考 DRAMsim3 的建模结构、公式和趋势进行辅助验证。
~~~

不能写：

~~~text
HBM4/LPDDR6 已经与 DRAMsim3 数值一致。
~~~

本轮已经增加 `dramsim3_hbm2_common.cfg` 兼容切片和
`dramsim3_aux_validation.py`。hbm_sim 明确以 HBM3 状态机承载较老 HBM 公共命令，
不把它伪装成 HBM2 产品模型。实测 9/9：ACT/RD 序列、tRCD=14、读延迟，以及
ACT/RD/WR/REF 四类 IDD 能量逐项对齐；DRAMsim3 SuperLU 热求解器和 hbm_sim 稀疏
RC 在相同 40°C、8 mm×8 mm 输入下均产生正温升。绝对峰温不同只记录为不同热网络
的定性趋势，不做误差百分比校准。

## 12. 变形测试与敏感性依据

当没有外部真值时，验证输入变化与输出变化之间的必然关系。

| 变换 | 不变量 | 必须变化 |
| --- | --- | --- |
| nCL + Δ | 地址、命令类型 | read completion + 对应 Δ |
| nRCDRD + Δ | ACT 数、地址 | ACT→RD 间隔 +Δ |
| nRP + Δ | 请求和坐标 | conflict path 延长 |
| data bus ×2 | 单请求数据 | 理论峰值 ×2 |
| channel ×2 且均匀负载 | 每请求语义 | aggregate peak ×2 |
| sparse→mmap→chunk | 命令和最终数据 | 只改变宿主介质/I/O |
| power_scale ×2 | command 数 | 配置能量近似 ×2 |
| cooling 增强 | command/energy | 稳态温升下降 |
| ECC metadata 开启 | payload | interface overhead 增加 |
| 同 seed 重跑 | 全部确定性输出 | 不应变化 |
| refresh 首次到期前 on/off | 短 trace | 不应变化 |
| 64 B→2×32 B | 重组 payload | transaction 数 ×2 |

当前已有：

- nCL +16 导致固定读延迟 +16。
- 三后端数据语义一致。
- transaction split 测试。
- power/thermal 事件路径、IDD 网格和 TSV coupling 测试。
- 同配置 compare_stats 零误差 smoke。
- `sensitivity_uncertainty.py` 对四协议的 nCL、nRCDRD、nRP、nCCDS、nRRDS、nFAW
  生成 72 个 low/nominal/high 点，并检查功耗缩放和温升单调性。
- 每协议 24 个固定种子的联合 ±10% 输入扰动样本，报告 latency/throughput 的
  p05/p50/p95；这是输入假设传播区间，不是硅片统计置信区间。
- `performance_curve.py` 生成 84 个注入率—延迟—吞吐量点，32/32 响应形状和峰值
  边界检查通过。

后续仍可扩展 channel/Stack 均匀负载、局部性、refresh 温度分支和更多调度策略曲线。

## 13. 系统级性能趋势验证

### 13.1 为什么不能只比较单点

单次带宽不同可能来自映射、队列、调度、工作集、维护相位或统计分母。可信模型应形成
延迟—吞吐量曲线，而不是只给一个峰值。

### 13.2 推荐 sweep

| 维度 | 建议取值 |
| --- | --- |
| 注入间隔/速率 | 从低负载逐步到饱和 |
| read ratio | 0、25、50、75、100% |
| row locality | closed、hit、conflict 混合比例 |
| 工作集 | 单 bank、同 BG、跨 BG、PC、channel、Stack |
| scheduler | FCFS、FR-FCFS |
| row policy | open、closed、ClosedCAP |
| refresh | off、per-bank、all-bank、高温 |
| address mapping | 四种模板 |
| stack_count | 1、2、4、6 |

每个点保存：

~~~text
unloaded latency
average/tail latency
payload/interface bandwidth
bandwidth utilization
queue occupancy
row hit/miss/conflict
command counts
refresh/RFM counts
active controllers/stacks
cycle-limit and remaining state
~~~

### 13.3 判定方法

当两模拟器控制器结构完全相同时：

- command/address 要求精确一致。
- 微序列周期要求一致或有固定、书面的时间基准解释。
- completion 和 byte 数要求一致。
- 性能曲线可使用预先规定的相对误差。

当调度、维护或抽象不同：

- 不强求绝对带宽相等。
- 比较空载延迟、饱和点、趋势方向和排序。
- 每个差异必须能回到命令/队列证据解释。

性能误差阈值必须在观察结果前固定，不能看完数据再选择能通过的阈值。

## 14. 功耗、热和 ECC 的验证依据

### 14.1 当前允许的功耗结论

当前功耗支持 configured_pj 和 DRAMsim3-style IDD 公式。没有目标器件 IDD 时：

- 可以验证命令事件是否进入正确能量类别。
- 可以验证 VDD×电流差×时间×器件数公式和单位。
- 可以验证 scaling 和相对 workload 趋势。
- 不能声明绝对功耗与 HBM4 产品一致。

最低不变量：

~~~text
energy >= 0
power_events 与受支持命令事件对应
power_scale=0 -> configured event energy=0
power_scale×2 -> 相同事件能量×2
写多 workload 的 write energy 不应减少
~~~

### 14.2 当前允许的热结论

没有真实 floorplan、材料、TSV 和散热边界时：

- 可以验证能量注入、RC 更新和邻接耦合。
- 可以验证无功耗时保持环境温度。
- 可以验证功率增加时温升不下降。
- 可以验证冷却增强时稳态温升下降。
- 可以比较归一化热点和相对趋势。
- 不能把输出温度当作封装绝对预测。

### 14.3 ECC/RAS

SECDED shadow 可以验证数据路径上的校验、注错、单 bit 修复和不可纠正计数。HBM/LPDDR
接口 ECC/CRC/DBI metadata 可以验证接口开销。它们不能证明目标芯片的内部 code、
syndrome、scrub 策略、FIT 或 retry 时序。

## 15. 参数不确定性与来源治理

### 15.1 十类厂商缺口

configs/validation/vendor_parameters.csv 已列出：

1. RL/WL、nCL/nCWL、nRCD。
2. nRP/nRAS/nRC/nRTP/nWR/nRRD/nFAW。
3. nBL/nCCD/nWTR/nRTW 模式确认。
4. density/温度相关 refresh。
5. RAA/RFM/PRAC。
6. ECC/CRC/retry/syndrome/scrub。
7. DFI/WCK/DVFS/training。
8. VDD/IDD/命令能量。
9. 材料、TSV、封装和散热。
10. subarray/mat/cell/microbump 几何。

### 15.2 不确定性处理

拿不到唯一值时不能选择一个 research default 后隐藏不确定性，应：

1. 给出来源和合理范围。
2. 对关键参数进行 low/nominal/high sweep。
3. 检查论文结论在参数范围内是否稳定。
4. 报告结果区间，而不是伪精确单点。
5. 如果结论随参数翻转，明确写成“依赖待校准参数”。

推荐输出：

~~~text
parameter
source
nominal
low/high
reason for range
metric sensitivity
whether conclusion changes
vendor data needed
~~~

### 15.3 Strict Timing 的边界

strict timing 通过只说明所有 required/vendor-required 字段已被显式覆盖，不证明：

- 覆盖值来自真实厂商。
- mode register 与目标产品一致。
- 温度和电压条件正确。
- profile 中 source 标签真实。

必须同时审查 timing.csv 的 source、note、文档版本、表格页码和器件条件。

## 16. 主张—证据—边界矩阵

| 编号 | 当前主张 | 权威证据 | 状态 | 不能外推 |
| --- | --- | --- | --- | --- |
| C01 | 软件可构建运行 | CMake + CTest 7/7 | 已证实 | 所有未测路径无错 |
| C02 | 四标准入口可运行 | smoke + supported scope | 已证实 | 四类均已 vendor 校准 |
| C03 | HBM4 组织和峰值公式正确 | capacity/BW formula | 已证实当前配置 | 真实可达带宽 |
| C04 | LPDDR6 64→2×32 B | split test + capacity | 已证实 | pin-level BL24 波形 |
| C05 | 已覆盖命令状态正确 | sequence + validator | 已证实已覆盖范围 | 全部 JEDEC 边角穷举 |
| C06 | active Timing gate 边界正确 | 315/315 t-1/t 矩阵 | 已证实 active constraints | 未实现的 JEDEC 模式/条款 |
| C07 | 数据语义正确 | payload/mask/golden tests | 已证实 | 芯片内部存储电气行为 |
| C08 | 三后端语义等价 | reopen/golden tests | 已证实 | 异常断电事务一致性 |
| C09 | DFI trace 自洽 | DFI validator | 已证实项目抽象 | DFI 合规认证 |
| C10 | 六 Stack 路由隔离 | Multi Stack tests | 已证实 | 真实封装互连 |
| C11 | 四标准共同面与 Ramulator 一致 | 225/225 JSON | 已证实对齐范围 | 项目全部功能 |
| C12 | 固定性能没有回归 | 64.56% ≥60% | 已证实固定 workload | 器件保证效率 |
| C13 | 功耗/热事件路径可用 | sequence/model tests | 已证实结构和趋势入口 | 绝对功耗/温度 |
| C14 | 未知参数显式 | source/vendor manifest | 已证实 | 未知项已经校准 |
| C15 | 具体厂商器件精度 | 无 vendor/硬件证据 | 未证实 | 禁止声明 |

## 17. 验收判据

### 17.1 必须零误差

| 项目 | 门槛 |
| --- | --- |
| 构建和默认回归 | 100% 通过 |
| JEDEC/项目命令合法性测试 | 0 非预期错误 |
| 对齐配置 Timing 表 | 0 未解释差异 |
| 地址解码 | 0 差异 |
| 共同面命令序列 | 0 差异 |
| expected/golden 数据 | 0 非注入 mismatch |
| 后端重开数据 | 0 mismatch |
| 来源标签 | 0 非法/缺失来源 |
| 确定性重复 | 指定字段 0 差异 |

### 17.2 允许有解释容差

| 项目 | 当前门槛 | 必须记录 |
| --- | --- | --- |
| Ramulator 命令 cycle | ±2 internal ticks | tick_multiplier、周期原点、原因 |
| aggregate performance | 实验前定义 | 两边是否同控制器/映射/维护 |
| power/thermal | 趋势和量级分阶段 | 参数来源和归一化方式 |

容差不是忽略错误的工具。若可以通过统一周期定义消除，优先消除；无法消除时才保留
固定、有物理或抽象原因的容差。

### 17.3 单次运行有效性

任何用于结论的数据至少满足：

~~~text
hit_cycle_limit = false
remaining_requests = 0
remaining_pending = 0
completed transaction 数与输入/拆分一致
data_mismatches = 0（数据正确性实验）
cmd_validation = pass（声称命令验证时）
dfi_validation = pass（声称 DFI 验证时）
Timing 来源分布已保存
~~~

validation=off 或检查计数为 0 时，不能写成“验证通过”。

## 18. 可复现实验与证据包

### 18.1 目录建议

~~~text
outputs/validation/<date>_<experiment>/
  hbm_sim_git.txt
  reference_git.txt
  environment.txt
  command.txt
  config.cfg
  final_config_or_stats.txt
  timing.csv
  commands.csv
  dfi.csv
  dfi_signal.csv
  final_memory.csv
  mismatch.txt
  thermal_map.txt
  model_validation.json
  ramulator2_diff.json
  source_manifest.csv
  notes.md
~~~

### 18.2 notes.md 必填项

~~~text
研究问题
允许声明的结论
禁止声明的结论
标准及文档版本
profile/density/speed/mode/temperature
地址映射、调度器、行策略、队列
trace、seed、请求数、注入率
refresh/RFM/DFI/power/thermal 开关
统计口径和单位
外部模拟器版本
容差及原因
已知差异
结果摘要
~~~

### 18.3 最小复现命令

~~~bash
cmake -S . -B build-validation -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18
cmake --build build-validation --parallel
ctest --test-dir build-validation --output-on-failure

python3 tools/model_validation.py \
  --binary ./build-validation/hbm_sim \
  --json-out outputs/validation/model_validation.json

python3 tools/ramulator2_differential.py \
  --binary ./build-validation/hbm_sim \
  --ramulator-root /home/guxing/learn/memorystack/ramulator2 \
  --json-out outputs/validation/ramulator2_diff.json
~~~

## 19. 当前不足和验证路线完成状态

### 19.1 P0：形成更强标准符合性证据

已完成：

1. 为每个 TimingConstraint 自动生成 t-1 禁止、t 允许测试。
2. 测试记录参数和来源；JEDEC 章节/表格绑定因缺少可引用标准原文仍待补。
3. 扩展 same/different BG、读写换向、nFAW、refresh/RFM 矩阵。
4. 检查标准作用域，不只检查数值。

完成标志：核心 required Timing 的边界测试覆盖率 100%，无未绑定来源规则。

### 19.2 P1：扩展 Ramulator2.1 差分（已完成共同面）

1. HBM3、HBM4、LPDDR5、LPDDR6 分别建立 reference cfg。
2. 从四个场景扩展到单 bank、BG、bus turnaround、maintenance 和调度矩阵。
3. 增加 completion latency、row statistics 和曲线对比。
4. 所有差异输出机器可读 JSON，并有分类和原因。

完成标志：所有共同场景命令和坐标零差异；周期差在预先定义且可解释的容差内。

### 19.3 P2：性能曲线与不确定性（已完成首版自动化）

1. 自动 sweep 注入率、读写比、局部性、channel/Stack。
2. 输出 unloaded latency、饱和点、峰值和队列变化。
3. 对 research defaults 做 low/nominal/high 敏感性。
4. 检查结论是否随未知参数翻转。

完成标志：主要架构结论在声明的参数范围内稳定，或明确标记依赖关系。

### 19.4 P3：DRAMsim3 辅助验证（已完成公共 HBM/IDD/热首版）

1. 只选择真正共有的协议和功能。
2. 对齐功耗输入和命令事件，比较公式与归一化趋势。
3. 对齐热网格、边界条件和能量输入后比较热点迁移。
4. 参考其 Verilog validation 流程准备未来厂商模型接入。

完成标志：输出差异报告，明确哪些是共同协议结果，哪些只是方法参考。

### 19.5 P4：厂商和硬件校准

获得条件后补：

- 目标料号、speed-bin、density、stack height。
- RL/WL、行列 Timing、refresh/RFM。
- VDD/IDD、ECC/RAS、PHY/DFI、封装/TSV。
- 硬件 unloaded latency、stream/mixed bandwidth、功耗和温度。

只有完成这一层，才能把部分 research_default 升级为 vendor，并声明器件级误差。

## 20. 结论

### 20.1 对当前模型可行性的判断

当前 hbm_sim 已经拥有六类相互补充的证据：

1. 构建、smoke 和精确 sequence 的软件证据。
2. 真实数据、后端、DFI 和多 Stack 的项目能力证据。
3. 容量、带宽、延迟和敏感性的独立解析证据。
4. 四协议 Ramulator2.1 共同面 225/225 的外部实现证据。
5. DRAMsim3 较老 HBM 公共命令、IDD 公式和热趋势 9/9 的辅助证据。
6. JEDEC/vendor/derived/research_default 分级及缺口清单的审计证据。

因此，可以判断模型对于“周期级控制器和堆叠存储架构研究”是可行的。这个判断不是
基于某一个漂亮的带宽数字，而是基于状态、数据、公式、外部参考和来源边界共同构成的
证据链。

### 20.2 可信度边界

当前最准确的结论是：

~~~text
内部一致：是
关键公式可解释：是
四标准 Ramulator 外部共同面一致：是
JEDEC-oriented：是
项目 active TimingConstraint 边界矩阵：是（315/315）
完整 JEDEC 章节/模式穷举：尚未
DRAMsim3 较老公共 HBM/IDD/热辅助验证：是
DRAMsim3 直接 HBM4/LPDDR6 器件差分：不适用，协议不完全同代
具体厂商器件校准：否
真实硬件校准：否
~~~

### 20.3 接收结论

> 本模型可接收为研究型、可配置、可审计的周期级堆叠存储仿真模型，用于架构设计、
> 控制器策略、数据路径和相对性能趋势研究。所有绝对器件结论必须等待目标厂商或
> 真实硬件校准；当前剩余的关键外部缺口是逐条 JEDEC 章节绑定，以及取得厂家模型或
> 硬件后的器件级绝对数值校准。

## 附录 A：验证证据索引

| 证据 | 文件 |
| --- | --- |
| 默认回归 | CMakeLists.txt、tests/README.md |
| 精确序列 | tests/sequence_tests.cpp |
| CLI 端到端 | tests/smoke.sh |
| 项目原生验收 | tools/model_validation.py |
| 项原生验收结果 | outputs/validation/model_validation.json |
| Ramulator 差分 | tools/ramulator2_differential.py |
| Ramulator 结果 | outputs/validation/ramulator2_diff_v2.json |
| Timing 边界 | tests/timing_boundary_tests.cpp、tools/timing_boundary_validation.py |
| Timing 边界结果 | outputs/validation/timing_boundaries.csv/json |
| 性能曲线 | tools/performance_curve.py、outputs/validation/performance_curves.csv/json |
| 敏感性/区间 | tools/sensitivity_uncertainty.py、outputs/validation/sensitivity_uncertainty.json |
| DRAMsim3 辅助 | tools/dramsim3_aux_validation.py、outputs/validation/dramsim3_aux.json |
| Command validator | src/validation/validator.cpp |
| DFI validator | src/validation/dfi.cpp |
| Native 配置 | configs/validation/hbm4_native_1ch.cfg |
| Reference 配置 | configs/validation/hbm4_reference_1ch.cfg |
| 项目能力清单 | configs/validation/project_identity.csv |
| 厂商缺口 | configs/validation/vendor_parameters.csv |
| Timing/profile | configs/profiles/ |
| 校准路线 | VALIDATION_AND_CALIBRATION.md |
| 交付总览 | 堆叠存储仿真模型交付文档.md |

## 附录 B：验证记录模板

| 字段 | 内容 |
| --- | --- |
| Validation ID | 唯一编号 |
| Claim | 要证明的单一命题 |
| Standard reference | 文档、版本、章节/表格 |
| Model configuration | 完整 cfg/profile |
| Input | trace/seed/sweep |
| Independent oracle | 公式、Ramulator、DRAMsim 或人工表 |
| Expected | 精确值、关系或范围 |
| Actual | 实测 |
| Tolerance | 数值和原因 |
| Result | PASS/FAIL/INCONCLUSIVE |
| Evidence path | JSON/CSV/log |
| Can prove | 允许声明 |
| Cannot prove | 禁止外推 |
| Reviewer/date | 审查人和日期 |

## 附录 C：发布前检查清单

- [ ] 两个仓库 commit 和环境已记录。
- [ ] 配置、trace、seed、命令行已保存。
- [ ] hit_cycle_limit=false。
- [ ] remaining_requests/remaining_pending=0。
- [ ] 数据实验 data_mismatches=0。
- [ ] 声称 command/DFI pass 时验证器确实启用且检查数非零。
- [ ] Timing table 和 source 已导出。
- [ ] 所有 research defaults 已披露。
- [ ] 差分实验已对齐组织、时钟、映射、调度和维护。
- [ ] 容差在查看结果前定义并有原因。
- [ ] 外部模拟器不被称为规范裁判。
- [ ] 功耗/热结果按参数可信度声明。
- [ ] 报告明确列出可证明和不能证明的内容。
