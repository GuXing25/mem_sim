# 堆叠存储仿真模型完整交付文档

> 项目名称：hbm_sim  
> 交付对象：HBM3、HBM4、LPDDR5、LPDDR6 堆叠/高带宽存储仿真模型  
> 文档性质：总体设计说明、模块详细设计说明、配置手册、功能测试与仿真验证报告、完成情况汇总  
> 验收日期：2026-08-15；Ramulator2.1 外部差分补验：2026-08-16（Asia/Shanghai）  
> 验收代码基线：Git 4190c32a3054；验收时 C++ 源码与配置文件相对该基线无修改，工作区另有文档类未提交修改  
> 文档状态：交付版

---

> **2026-08-16 增量实现说明：** 本文原始验收快照已被新增自动化证据取代：
> CTest 7/7、四标准 Timing 边界
> 315/315、Ramulator2.1 共同面 225/225、性能曲线 84 点/32 项检查、敏感性 72 点加
> 每协议 24 个不确定性样本、DRAMsim3 辅助 9/9。下文若仍出现旧数字，以本说明和
> `outputs/validation/*.json` 为准。

## 1. 交付结论

本项目已经形成一个可构建、可配置、可执行、可审计的 C++20 内存仿真器。模型面向
HBM3、HBM4、LPDDR5、LPDDR6 四类标准，覆盖请求生成、地址映射、多通道和多 Stack
路由、控制器调度、命令状态机、表驱动时序、刷新与 RFM/PRAC、真实 payload
读写、三种存储后端、逻辑物理坐标、功耗/热事件、ECC shadow、命令轨迹和
DFI-oriented 轨迹验证。

本次交付验收使用全新独立构建目录重新编译全部 30 个目标步骤，CTest 三层回归
全部通过：

| 验收层 | 实际内容 | 本次结果 |
| --- | --- | --- |
| C++ sequence tests | 54 次精确测试调用，覆盖状态、时序、调度、数据、后端、功耗、热和多 Stack | 通过 |
| CLI smoke | 37 个命名端到端场景，另含错误退出、golden、burst、QoS 等辅助断言 | 通过 |
| project model validation | 28 项公式、性能、数据、DFI、来源和身份验收 | 28/28 通过 |
| CTest 汇总 | 7 个自动化入口 | 7/7 通过，0 失败 |
| Timing 边界 | 四标准 active constraints | 315/315 行通过 |
| Ramulator2.1 外部共同面差分 | 四标准 Timing/命令/坐标/周期/维护 | 225/225 通过 |
| 性能与不确定性 | 84 点曲线、72 点 OAT、每协议 24 样本 | 32/32、34/34 通过 |
| DRAMsim3 辅助 | 较老 HBM 公共命令、IDD、热趋势 | 9/9 通过 |

项目当前可以用于控制器策略、命令序列、时序敏感性、地址映射、并行度、数据正确性、存储后端、接口开销以及功耗/热相对趋势研究。HBM3/LPDDR5 主要是 Ramulator2.1-derived 研究基线；HBM4 和LPDDR6 是 JEDEC-oriented 与研究默认值的组合；目标器件 RL/WL、行时序、RFM/RAS、PHY/DFI、功耗、热和几何仍需厂商资料或真实硬件校准。

## 2. 文档目的、范围与验收口径

### 2.1 文档目的

本文件是单一交付入口，回答以下问题：

1. 仿真器解决什么问题，边界在哪里。
2. 模型由哪些模块构成，各模块如何协作。
3. 如何构建、配置、运行、导出和复现实验。
4. 已实现功能经过了哪些测试，判据和实测结果是什么。
5. 哪些指标已经完成，哪些只能作为研究基线，哪些仍需外部条件。
6. 后续维护人员应从哪些接口扩展标准、时序、调度、后端和验证。

### 2.2 交付范围

交付范围包括：

- 四类标准入口：HBM3、HBM4、LPDDR5、LPDDR6。
- 单控制器、单 Stack 多通道和多 Stack 多控制器运行模式。
- 合成流量、普通 trace、带注入时间 trace 和 BW/BR burst trace。
- 控制器请求队列、FCFS/FR-FCFS、三种行策略和四种地址映射。
- ACT、PRE、RD、WR、refresh、RFM、WCK、DVFS、低功耗、RAS/ECC 等抽象命令。
- 真实数据、字节掩码、期望值、golden image、row buffer、SECDED shadow。
- sparse、mmap_sparse、chunk_file 三种 payload 后端。
- stack/die/layer/tile/grid/subarray/mat/cell/microbump 逻辑坐标。
- configured-pJ 和 DRAMsim3-style IDD 功耗入口、稀疏三维 RC 热状态。
- command CSV、DFI beat/signal CSV、timing table、memory image、mismatch 和 thermal map。
- 自动化回归、项目原生模型验收和可选 Ramulator2.1 共同面差分工具。

### 2.3 不在本次完成声明内的事项

- 不提供 transistor/circuit/SPICE 级 DRAM cell 或 PHY 电气仿真。
- DFI 输出是 controller-to-PHY 边界研究视图，不是完整 pin-level DFI 5.0
  合规实现或认证结果。
- ECC/RAS/link protection 是数据、接口开销、shadow code 和事件入口，不是某款
  芯片完整的 syndrome、scrub、retry buffer 和失效率模型。
- 默认功耗、热、封装、TSV、subarray/mat/cell 参数不是目标厂商标定值。
- Ramulator2.1 差分只覆盖双方刻意对齐的四标准单通道命令、地址和
  Timing 共同面；本次该范围 225/225 通过，但不能外推为项目扩展功能
  或真实器件精度认证。
- 没有真实 HBM/LPDDR 板卡测量或厂商 Verilog/model 结果，不能宣称器件级数值精度。

## 3. 需求与交付物对应关系

| 交付要求 | 本文对应章节 | 主要代码/证据 |
| --- | --- | --- |
| 总体设计说明 | 第 4 章 | src/core、src/controller、src/dram |
| 模块详细设计 | 第 5 章 | include/hbm_sim 与 src 分层实现 |
| 四类存储模型说明 | 第 6 章 | standard_traits.cpp、profiles.cpp、配置 profile |
| 仿真模型配置手册 | 第 7～9 章 | configs、CLI help、examples |
| 功能测试报告 | 第 10 章 | tests/smoke.sh、tests/sequence_tests.cpp |
| 仿真验证报告 | 第 11 章 | tools/model_validation.py、本次实测 |
| 已实现功能汇总 | 第 12 章 | 代码、测试和输出字段 |
| 完成指标总结 | 第 13 章 | 数量、公式、性能和验收结果 |
| 局限、风险和后续校准 | 第 14 章 | vendor_parameters.csv、审计资料 |
| 交付复现和接收步骤 | 第 15 章 | CMake/CTest/运行命令 |

## 4. 仿真模型总体设计

### 4.1 设计目标

模型采用“平台与器件语义分层、协议状态与真实数据并行、所有关键行为可导出审计”的
设计。主要目标是：

- 同一控制器框架支持 HBM 与 LPDDR 的不同命令面。
- 组织结构、时序表、来源标签和实验策略可配置，不在调度器中散落常数。
- host 请求粒度与 DRAM transaction 粒度分离。
- 多通道、多 Stack 的系统时间和统计口径正确聚合。
- 数据读写不是计数器占位，而是真实 payload、init mask 和 metadata 的变化。
- 大容量逻辑空间不要求宿主机预分配同等大小的 DRAM 数组。
- 命令、数据、DFI、功耗和热事件能回溯到请求、地址和物理坐标。
- 自动化测试既检查能否运行，也检查数值、顺序、错误路径和来源边界。

### 4.2 总体分层

~~~text
配置文件 / CLI / trace
          |
          v
StandardTraits + Profile + Config Overrides + finalize_spec
          |
          v
TrafficStream / RequestSource
  host request -> 物理 DRAM transactions
          |
          v
MemorySystem
  StackAddressMapper -> 每 Stack ingress/QoS -> ChannelMapper
          |
          v
Controller[stack][channel]
  queues -> scheduler -> row policy -> state/timing gate -> command issue
          |
          +--------------------+
          |                    |
          v                    v
CommandExecutor          Command/DFI trace
bank/timing/refresh/RFM   offline validator
          |
          v
MemoryImage -> RowBuffer -> MemoryBackend
          |
          +--> PhysicalAddress / floorplan
          +--> ECC shadow
          +--> command energy / sparse 3D RC thermal
          |
          v
Stats + dumps + validation reports
~~~

### 4.3 一次仿真的完整数据流

1. CLI 解析标准、配置文件和后置参数。
2. 程序先创建 standard draft，再展开内置 profile，加载外部 profile 和当前 cfg，
   最后应用 CLI 覆盖并调用 finalize_spec。
3. TrafficStream 从 synthetic pattern 或 trace 按需产生 host request。
4. 当 line_size 大于 dram_transaction_bytes 时，请求拆成多个 transaction；
   HBM4 和 LPDDR6 默认均为 64 B host line 拆成两个 32 B transaction。
5. MemorySystem 把全局地址映射到 Stack，进入每 Stack 独立 ingress；再按 decoded、
   round-robin 或 xor 选择 channel controller。
6. Controller 将请求放入 read、write、priority 或 active 队列，调度器选择
   timing-ready 候选。
7. next_command 根据 bank state、标准族和请求阶段产生 ACT/ACT1/ACT2、CAS、RD/WR、
   PRE 或维护命令。
8. TimingEngine 检查 bank 与 channel/PC/SID/rank/BG scope 约束、tFAW 和 WCK window。
9. CommandExecutor 集中更新 bank state、ready time、统计、RFM 计数和控制状态。
10. RD/WR 事件进入 MemoryImage：处理 row buffer、真实数据、mask、forward、
    metadata、ECC、功耗和热状态。
11. 已发命令按需生成 command/DFI event；validator 离线回放检查状态和时间关系。
12. 完成时聚合 controller、Stack、接口、数据、后端和热统计，导出指定文件。

### 4.4 时间和粒度模型

- Timing 表以 nCK 保存。
- 内部 controller tick = nCK × tick_multiplier。
- HBM edge pairing 常用 tick_multiplier=2，以半周期表达 rising/falling edge。
- LPDDR 当前 profile 通常使用 tick_multiplier=1。
- system_cycles 是外层真实仿真时间；aggregate controller cycles 是所有 controller
  cycle 求和，只用于队列归一化，不能作为系统带宽分母。
- 理论峰值：

~~~text
peak_bandwidth_GBps = data_rate_mbps × data_bus_bits / 8000
~~~

- 实际 payload 带宽以完成的 read/write payload byte、system_cycles 和 tCK 换算。
- interface bandwidth 还包含配置的 ECC、metadata、CRC、DBI 或 CA parity 开销。

### 4.5 主要设计原则

| 原则 | 实现方式 |
| --- | --- |
| 标准身份与数值分离 | StandardTraits 保存能力和 scope，profiles 保存组织与 timing |
| 状态变化集中 | CommandExecutor 是已发命令状态转换的集中入口 |
| timing 表驱动 | TimingConstraint 声明 preceding、following、scope、latency/window |
| 数据与协议协同 | Controller 驱动 MemoryImage 行缓冲和 payload 事件 |
| 存储介质可替换 | MemoryBackend 抽象 load/store/flush，协议语义保持不变 |
| 来源可审计 | timing entry 带 JEDEC/vendor/derived/research_default |
| 系统可扩展 | MemorySystem 按 [stack][channel] 创建独立 controller |
| 验证可重放 | command 和 DFI event 可导出并离线检查 |

## 5. 模块详细设计

### 5.1 CLI 与配置合成模块

主要文件：src/cli/main.cpp、src/cli/help.cpp。

职责：

- 解析命令行和一个或多个 key=value 配置。
- 识别标准、profile、组织、timing、控制器、后端、输出和验证参数。
- 把 ns/us/ps 手册字段向上取整到 nCK。
- 处理输出父目录创建、输入文件加载、结果导出和退出码。
- 运行 strict timing、command validator、DFI validator 和 golden 校验。

配置优先级固定为：

~~~text
standard traits
  < 内置 profile
  < timing_profile_file
  < cfg 中逐项字段
  < 命令行后置参数
~~~

重要退出语义：

- 正常完成且所有启用验证通过：0。
- 配置、文件、状态或 validator 错误：非 0。
- 读期望值不匹配且未使用 allow-data-mismatch：3。

### 5.2 标准 traits、profile 与 DramSpec

主要文件：include/hbm_sim/dram/spec.hpp、src/dram/standard_traits.cpp、
src/dram/profiles.cpp、src/dram/spec.cpp。

DramSpec 是一次仿真的最终模型描述，包含：

- 标准和家族身份。
- Organization：channel、PC、SID、rank、BG、bank、row、column、line 和 transaction。
- Timing：nCL、nRCD、nRP、nRAS、nRC、nCCD、nRRD、nFAW、refresh、RFM、WCK 等。
- 协议能力：dual command bus、split activate、refresh/RFM、ECC、LPDDR 模式。
- timing constraints 与每个 constraint 的 scope。
- profile 名、vendor/mode 标签和 timing 来源元数据。

finalize_spec 负责统一刷新约束和 timing table，避免 profile/config 修改后仍使用旧约束。
strict timing 检查只证明所有要求项被显式覆盖，不自动证明填入值真实可靠。

### 5.3 流量前端

主要文件：include/hbm_sim/frontend/traffic.hpp、src/frontend/traffic.cpp。

支持：

- stream：按 addr_stride 顺序生成。
- random：按 seed 可重复生成。
- 普通 trace：R/W、可选注入 cycle。
- 数据 trace：data、expect、mask、last_write。
- burst trace：BW/BR、len、pattern、check。
- 多 Stack trace：stack=N 与 qos=N。
- 初始化控制序列：none、auto、hbm4、lpddr6、lpddr6_full。

TrafficStream 按需生成请求，不预先构造完整 workload。统计同时保留 host_requests
和拆分后的 dram_transactions，避免把 64 B host 请求与 32 B RD/WR 混为一谈。

### 5.4 地址映射

主要文件：include/hbm_sim/core/addr_map.hpp、src/core/addr_map.cpp。

DecodedAddress 包含 channel、pseudo_channel、sid、rank、bank_group、bank、row、
column。支持四种地址位模板：

- default
- RoBaRaCoCh
- ChRaBaRoCo
- RoCoRaBaCh

模板名按高位到低位书写，实现从低位依次取字段。MemorySystem 还提供 decoded、
round_robin、xor 三种 channel mapper。多 Stack 层再提供 interleaved 和 blocked
映射；显式 stack=N 时 trace 地址按 Stack-local 解释。

### 5.5 MemorySystem 与多 Stack

主要文件：include/hbm_sim/core/system.hpp、src/core/system.cpp。

MemorySystem 默认按 [stack][channel] 创建 Controller。每颗 Stack 具有独立的：

- ingress queue 和 QoS 仲裁。
- channel controller 集合。
- MemoryImage/backend。
- bank、refresh、RFM、ECC、功耗和热状态。
- per-stack 统计。

六 Stack HBM4 默认形成 6×32=192 个 controller。strict_priority 在同一 Stack
入口按 qos_class 选择；目标 channel 反压会造成该 Stack 的 head-of-line 暂停，
其他 Stack 仍可推进。

### 5.6 Controller 请求队列与生命周期

主要文件：include/hbm_sim/controller/controller.hpp、
src/controller/controller.cpp。

队列包括：

- read_buffer：普通读。
- write_buffer：普通写。
- priority_buffer：维护和高优先请求。
- active_buffer：已进入多命令展开过程的请求。
- pending：已发数据命令、等待完成的请求。

控制器支持写队列高/低水位 write-drain、同地址 write coalescing、read forwarding
以及“先到读不得被后到重叠写越过”的顺序约束。完成时分别更新逻辑完成数、真实物理
传输 byte、延迟和数据验证结果。

### 5.7 Scheduler 与 Row Policy

主要文件：src/controller/scheduler.cpp、src/controller/row_policy.cpp。

调度策略：

- FR-FCFS：优先 ready/row-hit，再按 arrival 和 sequence 排序。
- FCFS：最早请求未 ready 时不向后偷跑。

行策略：

- open_page：保持行打开。
- closed_page：数据命令使用 RDA/WRA 或显式预充电关闭行。
- closed_cap：达到 row_policy_cap 后升级 auto-precharge，不能升级时插入 PRE。

Scheduler 只负责候选排序；命令推导、状态合法性和 timing readiness 由 Controller、
state machine 和 TimingEngine 分别负责。

### 5.8 Bank state、命令语义与执行器

主要文件：src/dram/state.cpp、src/dram/semantics.cpp、
src/controller/executor.cpp。

抽象命令集共 29 项：

~~~text
ACT ACT1 ACT2 PRE PREPB PREAB
CASRD CASWR RD WR RDA WRA
REFAB REFPB REFDB RFMAB RFMPB
MRW MRR WCKSYNC WCKTRAIN DVFS
PDE PDX SREFEN SREFEX ECCSCRUB RASERR NOP
~~~

Bank state 记录 closed/activating/open、open row 和 bank-local ready time。语义表定义
命令类别、总线类别和状态前置条件；CommandExecutor 执行状态变更并把跨 scope 影响
交给 TimingEngine。

### 5.9 TimingEngine

主要文件：include/hbm_sim/controller/timing.hpp、
src/controller/timing.cpp。

TimingEngine 为 channel、PC、SID、rank、bank group、bank 和 WCK scope 建立
ready-time bucket，处理：

- 表驱动 preceding→following 时序。
- nRRD、nCCD 的 same/different BG 差异。
- nFAW 最近四次激活窗口。
- row/column bus gate。
- LPDDR WCK ready 和 active window。
- refresh、RFM、低功耗和控制命令后的恢复时间。

bank-local open-row/nRCD/nRP 等状态与跨 scope timing 分开保存，减少把所有规则
错误地全局串行化。

### 5.10 Refresh 与 RFM/PRAC

主要文件：src/controller/refresh.cpp、src/controller/rfm.cpp。

RefreshManager：

- 根据 nREFI/nREFIpb 和温度倍率产生维护 batch。
- 支持 per-bank、all-bank；LPDDR6 可生成 dual-bank REFDB。
- 支持 postpone、pull-in 和 credit 计数。
- 只生成维护请求；PRE、状态和 timing 仍走统一控制器路径。

RfmManager：

- 按 flat bank 累计 ACT/ACT1 次数。
- 达到 rfm_act_threshold 后产生 RFMPB 或 RFMAB。
- 维护完成后按 rfm_decrement 递减计数。
- 防止同一目标在 pending 期间重复插入维护请求。

这些是可配置研究策略。真实 RAA/RFM/PRAC 阈值和器件动作仍需目标标准分支及厂商数据。

### 5.11 MemoryImage 与真实数据

主要文件：include/hbm_sim/core/data.hpp、src/core/data.cpp。

每个 DataBlock 保存：

- payload bytes。
- byte mask 和 initialized mask。
- version、last writer request id、写入和访问 cycle。
- checksum 与 ECC shadow。
- PhysicalAddress 与 StorageKey。

MemoryImage 提供：

- 按 transaction 粒度的读写。
- masked write。
- 未初始化 byte 跟踪。
- row activation、precharge、dirty writeback 和 lazy load。
- 文本/二进制 checkpoint 与 CSV dump。
- golden image 比较和 mismatch 记录。
- read forwarding 后的独立 expected payload 检查。

没有 expect 的普通读表示数据访问，不代表已经做正确性断言；data_checked_reads 才是
独立期望检查的数量。

### 5.12 MemoryBackend

主要文件：include/hbm_sim/core/memory_backend.hpp、
src/core/memory_backend.cpp。

| 后端 | 保存方式 | 适合场景 | 主要边界 |
| --- | --- | --- | --- |
| sparse | 哈希表保存已建立 line | debug、低/中覆盖率、小工作集 | 高覆盖率宿主内存开销大 |
| mmap_sparse | 四个稀疏文件整体 mmap | 大工作集、64 位虚拟空间充足 | 超大容量虚拟映射较大 |
| chunk_file | 稀疏文件加有限 LRU chunk cache | 满容量、长运行、限制宿主内存 | I/O 受 chunk/cache 参数影响 |

文件后端通常生成 data、.init、.meta、.present 四文件，重开时校验格式版本、
容量和 line size。后端只改变宿主保存介质，不改变 DRAM 协议和数据语义。

### 5.13 物理坐标、功耗、热与 ECC

PhysicalAddress 可派生 stack、die、layer、channel、PC、SID、rank、BG、bank、
row、column、tile、thermal grid、subarray、mat、cell 和 microbump 坐标。模型只为
触达 line 计算标签，不预分配完整物理 cell array。

功耗支持：

- configured_pj：按命令固定能量和每 byte 能量累计。
- dramsim3_idd：按 VDD、IDD、时间、devices-per-rank 推导事件能量。

热模型把命令能量注入稀疏三维 RC 网格，支持 lateral、vertical 和 TSV-aware
耦合，输出峰值、平均温度、热点和 transfer 计数。默认参数只适合路径验证和相对趋势。

ECC shadow 为 payload line 维护 SECDED 风格校验信息，支持读检查、单 bit 修复入口、
周期性注错和不可纠正计数。它不等同于 HBM/LPDDR 芯片内部 ECC 实现。

### 5.14 Command trace、DFI 与 Validator

主要文件：src/validation/trace.cpp、src/validation/dfi.cpp、
src/validation/validator.cpp。

Command CSV 保存 cycle、request、command、bus、Stack 和完整 decoded 地址。命令
validator 回放检查：

- bank state 前置条件。
- ACT/PRE/RD/WR、refresh、RFM 和控制命令顺序。
- timing constraint。
- HBM rising/falling edge pairing。

DFI event 包含 COMMAND、READ_DATA、WRITE_DATA，并保存 phase、beat、latency、
address/bank、enable/valid、write mask、真实读写 payload、payload_source 和
init mask。DFI validator 检查 command/data 对应、beat 数、phase、latency、信号、
mask、payload 和独立 expected payload。

### 5.15 Statistics

Stats 覆盖以下层面：

- 请求：注入、完成、剩余、cycle limit。
- row：hit、miss、conflict。
- 命令：29 类命令及维护 batch。
- 调度：队列、stall、write mode、forward、coalesce、row policy。
- 系统：controller/Stack 数、active 数、ingress、QoS。
- 数据：checked read、mismatch、write commit、init。
- 后端：分配、唯一写、容量覆盖率和拓扑触达。
- 物理：row buffer、floorplan、grid、坐标覆盖。
- 功耗/热/ECC。
- DFI beat 和接口开销。
- 带宽、payload efficiency、平均读延迟。

## 6. 四类存储模型说明

### 6.1 横向比较

| 项目 | HBM3 | HBM4 | LPDDR5 | LPDDR6 |
| --- | --- | --- | --- | --- |
| 默认速率 | 6400 Mbps | 8000 Mbps | 6400 Mbps | 10667 Mbps |
| 主要来源 | Ramulator2.1-derived | JESD270-4A-oriented + research | Ramulator2.1-derived | JESD209-6-oriented |
| 默认 channel | 16 | 32 | 1 | 1 |
| PC/subchannel | 2 | 2 | 1 | 2 |
| SID | 1 | 2 | 1 | 1 |
| 默认总线宽度 | 1024 bit | 2048 bit | 16 bit | 24 bit |
| host line | 64 B | 64 B | 64 B | 64 B |
| transaction | profile 口径 | 32 B | 64 B | 32 B |
| 命令总线 | HBM row/column | HBM row/column + strict pairing | LPDDR unified | LPDDR unified |
| 激活 | ACT | ACT | ACT1→ACT2 | ACT1→ACT2 |
| 数据前同步 | 无 CAS/WCK 阶段 | 无 CAS/WCK 阶段 | CASRD/CASWR | CASRD/CASWR |
| refresh | per/all bank | per/all bank | per/all bank | dual-bank REFDB/all bank |
| RFM/PRAC | 研究入口 | RAA/RFM 研究入口 | 默认关闭 | PRAC/RFM 研究入口 |

### 6.2 HBM3

HBM3 用于 HBM4 对照研究。当前 profile 为
configs/profiles/hbm3/ramulator2_6400_16gb_8hi.cfg，组织和 timing 均标为
research_default。支持 HBM row/column 双总线、PC interleave、refresh/RFM 和
full-stack 带宽统计，但没有随项目交付完整 HBM3 JEDEC 原文或真实器件表。

允许声明：HBM3-like 控制器和研究基线可运行、内部状态与测试一致。  
不允许声明：已按某个 HBM3 料号校准。

### 6.3 HBM4

默认 full stack 配置为 32 channel、每 channel 两个 PC、两个 SID、每 PC/SID 下
2 BG×8 bank、8Hi、32 Gb/die、8000 Mbps、2048-bit stack bus。当前地址几何得到
32 GiB 可寻址容量。

主要特性：

- 64 B host line 拆为两个 32 B PC transaction。
- HBM row/column 双总线和 half-cycle edge pairing。
- HBM4 strict PRE pairing matrix。
- SID/PC interleave。
- per-bank/all-bank refresh 和 RFM。
- 默认每个 32 B transaction 计 16 bit 外部 ECC metadata。
- 可选 CRC16、retry、RAS 事件入口。

项目原生单 channel 验证配置的理论峰值为 64 GB/s；完整 32-channel Stack
理论峰值为 2048 GB/s。这里是接口公式结果，不是硬件实测带宽。

### 6.4 LPDDR5

LPDDR5 当前是 Ramulator2.1-derived 6400 Mbps、16 Gb 研究基线，采用
ACT1→ACT2、CAS/WCK→RD/WR、4 BG×4 bank、x16、prefetch 16。DVFS 和 link
protection 默认关闭，refresh 默认 per-bank。没有配套 LPDDR5 JEDEC/厂商全表，
因此用于框架和代际对照，不用于具体器件结论。

### 6.5 LPDDR6

默认按 JESD209-6-oriented x24 device 建模为两个 x12 subchannel：

~~~text
1 channel × 2 subchannel × 4 BG × 4 bank
× 65536 row × 64 column × 32 B = 4 GiB
~~~

每个 subchannel 的 16 Gb 组织合计为 32 Gb device。主要特性：

- BL24 对应项目 32 B transaction，64 B host line 拆成两个 transaction。
- split activate、CAS/WCK、WCK training 和 DVFS 入口。
- nominal、low DVFS、link-protection/efficiency profile 分支。
- dual-bank REFDB、专用 REFDB→ACT 和 REFDB→REFDB timing。
- DBI、link ECC、CA parity、low power 和 metadata lane 统计。

默认 RL/WL 和模式来自所选标准分支，但仍需确认目标器件 mode register、温度、
density 和 speed-bin。

## 7. 构建与部署手册

### 7.1 依赖

- Linux/WSL。
- 支持 C++20 的 Clang 或 GCC。
- CMake 3.16+。
- Ninja 或 Make。
- Python 3；用于 model validation 和外部差分。
- 可选 ccache、clangd、LLDB。

本次验收环境：

| 项目 | 版本 |
| --- | --- |
| OS | Linux 7.0.0-29-generic x86_64 |
| Compiler | Ubuntu Clang 18.1.8 |
| CMake | 4.2.3 |
| Ninja | 1.13.2 |
| Python | 3.14.4 |
| Build type | Release，独立验收目录 |

### 7.2 推荐构建

~~~bash
cmake -S . -B build-clang-debug -G Ninja \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18 \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-clang-debug --parallel
ctest --test-dir build-clang-debug --output-on-failure
~~~

本次验收使用：

~~~bash
cmake -S . -B build-delivery-validation -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18 \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-delivery-validation --parallel
ctest --test-dir build-delivery-validation --output-on-failure
~~~

### 7.3 Make 兼容构建

~~~bash
make clean
make CXX=/path/to/g++ CPPFLAGS="-Iinclude" \
  CXXFLAGS="-std=c++20 -O2 -Wall -Wextra -pedantic"
make CXX=/path/to/g++ test
~~~

跨机器时如果环境预置 CXX、CPPFLAGS 或 CXXFLAGS，Makefile 中的 ?= 会被环境变量
覆盖。编译命令缺少 -Iinclude 时会报找不到 hbm_sim/... 头文件；自定义 Clang
找不到 GCC 的 libstdc++ 时会报 iostream file not found。正式交付环境应记录
command -v g++/clang++、清理 Python 构建环境变量，并优先使用 CMake target 的
target_include_directories。

### 7.4 产物

- build-clang-debug/hbm_sim：主程序。
- build-clang-debug/sequence_tests：C++ 序列测试。
- build-clang-debug/compile_commands.json：clangd compilation database。
- outputs/：建议保存实验输出；构建目录和 outputs 均不是源代码。

## 8. 仿真模型配置手册

### 8.1 配置语法

~~~ini
# comment
key = value
~~~

- key 不区分大小写。
- bool 可使用 true/false、yes/no、on/off、1/0。
- nXXX 表示 nCK；tXXX_ns、tXXX_us、tCK_ps 使用实际时间单位。
- 同字段后值覆盖前值；CLI 最终覆盖 cfg。
- requests=0 在 trace 模式表示读完整 trace。

### 8.2 主配置

| 配置 | 用途 |
| --- | --- |
| configs/run/hbm3.cfg | HBM3 研究基线 |
| configs/run/hbm4.cfg | 单颗 32-channel HBM4 Stack |
| configs/run/hbm4_6stack.cfg | 六颗 HBM4 Stack、192 controllers、QoS |
| configs/run/hbm4_storage.cfg | 数据、后端、功耗和热统一入口 |
| configs/run/lpddr5.cfg | LPDDR5 研究基线 |
| configs/run/lpddr6.cfg | LPDDR6 nominal 研究入口 |
| configs/calib/*.cfg | 显式展开、准备 JEDEC/vendor 校准 |
| configs/validation/*.cfg | 项目原生验收和外部参考共同面 |
| configs/profiles/*/*.cfg | 可复用 speed/density/mode timing 片段 |

### 8.3 常用字段分组

#### 标准、profile 和工作负载

| 字段 | 单位/取值 | 含义 |
| --- | --- | --- |
| standard | hbm3/hbm4/lpddr5/lpddr6 | 标准入口 |
| timing_profile | string | 内置 profile 标签 |
| timing_profile_file | path | 外部 profile 片段 |
| vendor_profile/mode_profile | string | 来源和模式标签 |
| speed_bin_mbps | Mbps/pin | 速率档 |
| density_gb | Gb | die/subchannel density 维度 |
| stack_height | layer | HBM 堆叠高度 |
| pattern | stream/random | 合成流量 |
| trace | path | trace 输入 |
| requests | count | host 请求数或 trace 上限 |
| read_ratio | % | 合成读比例 |
| seed | integer | 随机种子 |
| addr_stride | byte | 顺序流量步长 |
| inject_interval | tick | 注入间隔 |
| max_cycles | tick | 停止上限 |

#### 组织与接口

| 字段 | 含义 |
| --- | --- |
| channels/pseudo_channels/sids/ranks | 组织上层维度 |
| bank_groups/banks_per_group/rows/columns | bank 地址几何 |
| line_size | host/frontend 默认粒度 |
| dram_transaction_bytes | 单条 RD/WR payload |
| data_rate_mbps/data_bus_bits/prefetch_size | 接口速率、宽度、预取 |
| tick_multiplier/tCK_ps | 内部 tick 与 nCK 的关系 |
| dfi_phase_count/dfi_data_lane_bytes | DFI phase 和每 beat byte |
| dfi_read_latency_nck/dfi_write_latency_nck | DFI 数据相位延迟 |

#### 控制器、映射和多 Stack

| 字段 | 取值/作用 |
| --- | --- |
| scheduler | frfcfs、fcfs |
| row_policy | open_page、closed_page、closed_cap |
| row_policy_cap | ClosedCAP 列访问上限 |
| address_mapping | default、RoBaRaCoCh、ChRaBaRoCo、RoCoRaBaCh |
| channel_mapper | decoded、round_robin、xor |
| read/write/priority_buffer_size | 队列容量 |
| stack_count | Stack 数 |
| stack_mapping | interleaved、blocked |
| stack_interleave_bytes | 条带大小，须为 line_size 整数倍 |
| stack_ingress_buffer_size | 每 Stack ingress 容量 |
| stack_dispatch_width | 每 Stack 每 tick 分发宽度 |
| stack_qos_policy | fcfs、strict_priority |

#### Refresh、RFM 和低功耗

| 字段 | 含义 |
| --- | --- |
| supports_refresh/refresh_policy | 启用及 per_bank/all_bank |
| lpddr_dual_bank_refresh | LPDDR6 REFDB |
| refresh_temperature_mode | normal/high/extended |
| refresh_postpone/pullin/credit_limit | 刷新信用策略 |
| supports_rfm/rfm_policy | RFM 开关和作用域 |
| rfm_act_threshold/rfm_decrement | ACT 阈值和维护递减 |
| low_power_mode | off/power_down/self_refresh |
| low_power_entry/exit_cycles | 进入/退出延迟 |
| self_refresh_exit_cycles | self-refresh 退出延迟 |

#### HBM 专用

配置包括 full-32-channel、SID/PC interleave、edge pairing、strict pairing matrix、
SID mapping、ECC scheme、RAS policy、CRC/retry，以及每 transaction 的 CRC、
RAS metadata 和 ECC bit 开销。

#### LPDDR 专用

配置包括 efficiency mode、DVFS nominal/low/disabled、WCK cas_sync/always_on/
burst_sync、WCK training、mode register profile、link protection、DBI、link ECC、
CA parity 和对应接口 bit 开销。

#### 数据、后端、功耗、热和 ECC

| 字段组 | 关键字段 |
| --- | --- |
| 数据输入/输出 | memory_image、dump_memory_image、dump_memory_csv、verify_golden、mismatch_report |
| 后端 | memory_backend、capacity、data/init/meta/presence file、chunk size/cache |
| 稀疏统计 | sparse_density_warning_pct、topology_stats_scan_limit |
| 物理模型 | floorplan、subarrays、mats、cells、microbumps |
| 功耗 | power_model、power_source、power_scale、command pJ 或 VDD/IDD |
| 热 | thermal_model、ambient、cooling、rise、grid、lateral/vertical/TSV coupling |
| ECC | ecc_shadow、check_on_read、correct_single_bit、inject_period |

### 8.4 Timing 字段

常用 nCK 字段：

~~~text
nBL nCL nCWL nRCDRD nRCDWR nRP nRPab nRAS nRC nRTP nWR
nCCDS nCCDL nCCDR nRRDS nRRDL nFAW nWTRS nWTRL nRTW
nAAD nWCK2CK nWCKPST nCAS nCS nPPD
nRFC nRFCpb nRFMab nRFMpb nRREFD
nREFDB2ACT nREFDB2REFDBS nREFDB2REFDBL nREFI nREFIpb
nMRW nMRR nWCKSYNC nWCKTRAIN nDVFS nPDEX nSREFEX
nECCSCRUB nRASERR nLINKRETRY
~~~

支持的手册单位字段包括 tRCD_RD_ns、tRP_ns、tRAS_ns、tRC_ns、tRFCab_ns、
tRFCpb_ns、tREFI_us、tREFIpb_ns/us、tDBR2ACT_ns、tREFdb2REFdb_S/L_ns、
tWCK2CK_ns 等。换算使用最终 tCK_ps 并向上取整。

来源标签：

| 标签 | 含义 |
| --- | --- |
| jedec | 标准表、固定规则或标准公式 |
| vendor | 已由目标器件资料覆盖 |
| derived | 从其他参数推导 |
| research_default | 为模型可运行提供的研究值 |

### 8.5 配置示例

单 Stack HBM4：

~~~bash
./build-clang-debug/hbm_sim \
  --config configs/run/hbm4.cfg \
  --requests 10000
~~~

六 Stack QoS trace：

~~~bash
./build-clang-debug/hbm_sim \
  --config configs/run/hbm4_6stack.cfg \
  --trace examples/multistack_qos.trace --requests 0
~~~

LPDDR6 低速 DVFS profile：

~~~bash
./build-clang-debug/hbm_sim --standard lpddr6 --requests 1000 \
  --timing-profile-file configs/profiles/lpddr6/jedec_lowdvfs_4267_16gb.cfg
~~~

严格 timing 审计：

~~~bash
./build-clang-debug/hbm_sim --config configs/calib/hbm4.cfg \
  --strict-timing-table \
  --dump-timing-table outputs/exp/timing.csv
~~~

strict 失败表示仍有要求覆盖的 provisional/vendor-required 项；strict 通过只表示表项
已覆盖，仍需审查 source、文档版本、页码、speed-bin、density、温度和 mode。

## 9. Trace、运行和输出手册

### 9.1 Trace 语法

~~~text
0 R 0x1000
1 W 0x1000 data=00112233
200 R 0x1000 expect=00112233
W 0x2000 data=aabbccdd mask=ffff00ff
BW 0x4000 len=256 pattern=request_id stack=2 qos=7
BR 0x4000 len=256 check=last_write stack=2
~~~

规则：

- 首列 cycle 可省略；输入 cycle 必须非递减。
- data、expect、mask 使用十六进制 byte 串。
- expect=last_write/check=last_write 使用 frontend golden map。
- stack=N 表示局部地址；无 stack 表示系统全局地址。
- qos 数值越高，strict_priority 下优先级越高。
- BW pattern 支持 request_id、zero、ff；BR check 支持 last_write/auto。

### 9.2 完整数据与 DFI 运行

~~~bash
./build-clang-debug/hbm_sim --standard hbm4 \
  --trace examples/data_check.trace --requests 0 \
  --dump-memory-image outputs/exp/final_memory.txt \
  --dump-memory-csv outputs/exp/final_memory.csv \
  --mismatch-report outputs/exp/mismatch.txt \
  --cmd-trace outputs/exp/commands.csv \
  --dfi-trace outputs/exp/dfi.csv \
  --dfi-signal-trace outputs/exp/dfi_signal.csv \
  --dump-timing-table outputs/exp/timing.csv \
  --validate-cmd-trace --validate-dfi-trace
~~~

### 9.3 输出文件

| 文件 | 内容 | 接收检查 |
| --- | --- | --- |
| stdout/stats.txt | 最终配置和全部统计 | hit_cycle_limit=false、无 remaining |
| commands.csv | 已发命令 | 表头、cycle 顺序、validator pass |
| dfi.csv | command/data beat | beat/phase/latency/payload |
| dfi_signal.csv | signal-like 视图 | enable/valid/mask/source |
| timing.csv | nCK、source、vendor-required | 无未知来源，结论边界清楚 |
| final_memory.txt/.bin | checkpoint | 可重载 |
| final_memory.csv | Excel-friendly 数据 | data/init/metadata 完整 |
| mismatch.txt | 独立期望失败 | 仅表头表示无 mismatch |
| thermal_map.txt | 触达热网格 | hotspot 和温度列存在 |
| model_validation.json | 28 项验收 | passed=true |

### 9.4 结果有效性最低判据

一次可用于分析的运行至少满足：

~~~text
hit_cycle_limit = false
remaining_requests = 0
remaining_pending = 0
completed_reads + completed_writes 与预期 transaction 数一致
data_mismatches = 0（若该用例要求数据检查）
cmd_validation = pass（若启用）
dfi_validation = pass（若启用）
timing source 与实验声明一致
~~~

## 10. 功能测试报告（重点）

### 10.1 测试策略

测试采用项目回归和外部辅助验证相结合的结构：

1. sequence tests 直接构造对象和短序列，检查精确命令、cycle、状态和数据。
2. smoke 运行真实 CLI，检查输入、配置、输出文件、错误码和字段完整性。
3. model validation 以固定 HBM4 项目原生配置检查公式、性能门槛、数据、DFI 和来源。

CTest 显示 7 个顶层 test；内部还包含 54 次 sequence 调用、37 个命名
smoke 场景、28 项项目验收、315 行 Timing 边界以及小型性能/敏感性回归。

### 10.2 本次执行记录

~~~text
Build directory : build-delivery-validation
Compiler        : Clang 18.1.8
Build type      : Release
Compile result  : 30/30 build steps completed
CTest result    : 7/7 passed, 0 failed
Total test time : 7.52 s（最终重跑）
sequence_tests  : 0.03 s
timing_boundary_tests / audit : 0.03 s / 0.07 s
smoke           : 5.78 s
model_validation: 0.68 s
performance / sensitivity CI : 0.20 s / 0.74 s
~~~

### 10.3 Sequence tests 覆盖

54 次调用按功能分组如下：

| 分组 | 用例 |
| --- | --- |
| Spec/Timing | JEDEC 换算、统一 spec factory、timing table validation、scope/window state、source override、profile dimensions |
| Command/State | executor state transition、command state machine、control command、init sequence |
| Trace/DFI | command trace plugin、DFI generation、HBM4 pairing validator matrix |
| HBM4 | scoped timing、per/all-bank refresh、per/all-bank RFM、64→2×32 B split |
| System | multi-controller channel 并行、主动六 Stack 路由/QoS/统计、被动多 Stack 隔离 |
| Scheduling | write forward/coalesce、closed page、closed cap、地址映射模板 |
| LPDDR | REFDB、PRAC/RFM、CAS read/write、WCK skip/always-on、CA parity、efficiency、metadata lane、64→2×32 B split |
| Low power | refresh credit、power-down/self-refresh 状态和退出 |
| Data | 真实写读、重叠读写顺序、forward、masked write、checkpoint/mismatch、trace payload |
| Physical | 坐标与统计、多通道存储放置、row buffer writeback、controller storage event |
| Power/Thermal/ECC | floorplan、configured power、DRAMsim3 IDD、thermal grid、TSV coupling、ECC shadow |
| Backend/Frontend | streaming source、mmap 后端、chunk 后端 |

其中 file_backed_memory_backend 对 mmap_sparse 和 chunk_file 各调用一次，因此源码中
53 个 test 函数对应 54 次实际调用。本次直接运行输出：

~~~text
sequence tests passed
~~~

### 10.4 CLI smoke 命名场景

37 个命名场景全部通过：

| 范围 | 场景 |
| --- | --- |
| 四标准基本流量 | hbm4_stream、hbm3_random、lpddr6_stream、lpddr5_stream |
| Trace/data | lpddr6_trace、lpddr6_timed_trace、hbm4_data_trace、hbm4_memory_image |
| 存储模型 | hbm4_storage_model_cli、hbm4_storage_config、mmap、chunk、IDD grid |
| 接口特性 | lpddr6_efficiency、lpddr6_link_cli、hbm4_link_crc_cli |
| 主配置 | hbm4_config、hbm3_config、lpddr5_config、lpddr6_config |
| Profile/校准 | HBM4 JEDEC/synthetic vendor、HBM3 calib/profile、LPDDR5 calib/profile、LPDDR6 nominal/synthetic/low-DVFS |
| 控制序列 | lpddr6_ca_parity、hbm4_init_sequence、lpddr6_full_init_sequence |
| 调度/维护 | hbm4_closed_cap、hbm4_addr_mapping、hbm4_all_bank_rfm |
| 审计 | hbm4_cmd_trace、hbm4_dfi_real_payload |

smoke 还额外验证：

- 读先于后到同地址写，RD cycle 小于 WR cycle。
- 错误 expected payload 返回退出码 3；allow-data-mismatch 可继续并记录。
- 文本、CSV、二进制 checkpoint 和 golden init-mask。
- 输出父目录自动创建。
- 负 power_scale 被拒绝。
- CA parity 与 WCK always-on 的配置约束。
- 六 Stack active_stacks=6、controller_count=192 和 per-stack 请求。
- 多 Stack QoS trace 有 priority dispatch 且 data_mismatches=0。
- file backend 重开后 golden_verified=2、mismatches=0。
- command/DFI/signal/timing CSV 表头和关键事件。
- compare_stats 对同配置重复运行结果零误差。
- burst trace 6 行展开为 24 请求，读写各 12，数据零 mismatch。

### 10.5 功能测试判定

| 功能域 | 判据 | 结果 |
| --- | --- | --- |
| 构建 | 全部目标完成 | 通过 |
| 四标准入口 | 每类可运行且完成 | 通过 |
| 状态/时序 | 精确短序列无断言失败 | 通过 |
| 多通道/多 Stack | 路由、隔离、QoS、聚合统计符合断言 | 通过 |
| 数据 | write/read/forward/mask/golden/mismatch 正负例 | 通过 |
| 后端 | sparse、mmap、chunk 语义与持久化 | 通过 |
| DFI | command/data/phase/latency/payload/mask/source | 通过 |
| 功耗/热/ECC | 事件路径、网格、TSV、shadow 断言 | 通过 |
| 错误处理 | 配置错误、数据 mismatch、golden init mismatch | 通过 |

## 11. 仿真验证报告（重点）

### 11.1 可信度分层

| 层级 | 证明对象 | 本次状态 | 可声明结论 |
| --- | --- | --- | --- |
| V0 | 构建与基本运行 | 已完成 | 软件可构建、可运行 |
| V1 | 真实数据与后端 | 已完成 | 项目存储语义内部一致 |
| V2 | 命令、状态和 timing gate | 已完成项目级验证 | 按当前配置表和项目语义执行 |
| V3 | 标准可见组织/命令 | 部分完成 | HBM4/LPDDR6 JEDEC-oriented |
| V4 | 外部模拟器共同面 | Ramulator2.1 225/225；DRAMsim3 9/9 | 对齐范围内命令、坐标、周期及辅助公式可比较 |
| V5 | 厂商/外部功耗热模型 | 未完成 | 不作器件级结论 |
| V6 | 真实硬件 | 未完成 | 不作硬件精度结论 |

### 11.2 28 项项目原生验收结果

| # | 验收项 | 本次实测 |
| ---: | --- | --- |
| 1 | validation config identity | project-native HBM4、1 channel |
| 2 | peak bandwidth formula | 64.000000 GB/s，与公式一致 |
| 3 | interface rate formula | 8.000000 Gbps |
| 4 | timing source audit | 46 entries，bad_sources=[] |
| 5 | research defaults visible | 19 项明确可见 |
| 6 | ACT→READ timing | 60 ticks，期望 60 |
| 7 | closed-row read completion | 125 ticks，期望 125 |
| 8 | DFI read latency | 121，期望 121 |
| 9 | online validators | command=pass，dfi=pass |
| 10 | DFI real payload roundtrip | write=32 B，read=32 B |
| 11 | storage payload semantics | commit=1，checked=1，mismatch=0 |
| 12 | DFI mask/source | memory_image、none、request_payload 均出现 |
| 13 | expected payload guard | 独立期望值被检查 |
| 14 | physical coordinates | layer/bank/subarray/cell 坐标出现 |
| 15 | power/thermal path | power_events=3，thermal_updates=3 |
| 16 | same-address ordering | RD=61，WR=125，mismatch=0 |
| 17 | mismatch exit code | return code=3，mismatch=1 |
| 18 | file backend persistence | mmap=true，chunk=true |
| 19 | standard scope | hbm4/hbm3/lpddr6/lpddr5 均可运行 |
| 20 | streaming burst frontend | 2 lines，split requests=4 |
| 21 | sustained bandwidth threshold | 64.56%，门槛 60.00% |
| 22 | nCL sensitivity | 117→133，差值 16 |
| 23 | project full-stack scope | 32 channel、2048 bit、2048 GB/s |
| 24 | vendor manifest completeness | 10 rows |
| 25 | unknown vendor areas | 8 个 vendor-required area |
| 26 | project identity manifest | 10 rows |
| 27 | project-specific capabilities | 10 项 |
| 28 | identity evidence binding | missing_evidence={} |

总结果：

~~~text
model validation: 28/28 passed
passed = true
device_claim = research_baseline_not_vendor_calibrated
scope = hbm_sim_project_native_analytical_storage_dfi_provenance
~~~

### 11.3 固定验收配置核心指标

| 指标 | 数值 |
| --- | ---: |
| timing entries | 46 |
| research-default entries | 19 |
| 单 channel 理论峰值 | 64.000 GB/s |
| 单 channel sustained payload 带宽 | 41.318 GB/s |
| 持续带宽利用率 | 64.56% |
| 回归门槛 | 60.00% |
| ACT→RD | 60 ticks |
| closed-row read | 125 ticks |
| full-stack 理论峰值 | 2048.0 GB/s |

64.56% 是固定 workload/controller/profile 的经验回归基线，不是器件保证效率，也不能
从单个 nCCD 参数直接推导。

### 11.4 四标准与六 Stack 代表性运行

以下是 2026-08-15 使用各 configs/run 主配置、默认 10000 host requests 的本次
实测。它们用于证明配置可完成并展示统计口径，不是饱和带宽 benchmark；不同 workload、
读写比、映射、transaction 数和活跃 controller 数不同，不能直接横向评价器件优劣。

| 配置 | host/transaction | 完成读/写 | cycles | active ctrl | 理论峰值 GB/s | 实际 payload GB/s | 利用率 | 平均读延迟 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| HBM4 | 10000/20000 | 18010/1990 | 126854 | 10/32 | 2048.000 | 20.181 | 0.99% | 1001.97 |
| HBM3 | 10000/10000 | 7554/2446 | 12076 | 16/16 | 819.200 | 169.593 | 20.70% | 423.42 |
| LPDDR5 | 10000/10000 | 10000/0 | 40134 | 1/1 | 12.800 | 12.757 | 99.67% | 528.22 |
| LPDDR6 | 10000/20000 | 20000/0 | 144412 | 1/1 | 32.001 | 11.818 | 36.93% | 989.31 |
| HBM4 6-Stack | 10000/20000 | 18010/1990 | 23672 | 12/192 | 12288.000 | 108.145 | 0.88% | 1018.61 |

所有五次代表运行均满足：

~~~text
data_mismatches = 0
hit_cycle_limit = false
completed transactions = dram_transactions
~~~

HBM4 默认 stream/stride 只激活部分 channel，因此全 Stack 理论峰值很高而利用率低；
六 Stack 也只有 12/192 controller 实际收到了该工作负载。要测饱和聚合带宽，应设计
覆盖全部 channel/Stack 的映射和注入强度，并保存配置、活跃实例数和队列统计。

### 11.5 外部参考验证状态

项目提供 `tools/ramulator2_differential.py`，对 HBM3、HBM4、LPDDR5、LPDDR6 的
resolved Timing、基础访问、same/different BG、读写换向、tFAW、自动预充电、
refresh/RFM 及 HBM PC/SID 并行场景做受控差分。

2026-08-16 使用 Ramulator2 Git 版本 b30320bc9385，在独立
build-hbm-diff 目录完成默认 Release 构建，并执行：

~~~bash
python3 tools/ramulator2_differential.py \
  --binary ./build-delivery-validation/hbm_sim \
  --ramulator-root /home/guxing/learn/memorystack/ramulator2 \
  --json-out outputs/validation/ramulator2_diff_v2.json
~~~

本次结果：

| 检查 | 结果 |
| --- | --- |
| 24 项 resolved Timing | 完全一致，differences={} |
| closed_read | ACT、RD 一致；坐标一致；周期差 [0,-2] |
| row_hit | ACT、RD、RD 一致；坐标一致；周期差 [0,-2,-2] |
| row_conflict | ACT、RD、PREpb、ACT、RD 一致；坐标一致；周期差 [0,-2,-2,0,-2] |
| write_forwarded_read | ACT、WR 一致；坐标一致；周期差 [0,-2] |
| 汇总 | 当前四标准共同面 225/225 checks passed |

周期差绝对值均不超过脚本默认 2 tick 容差。结构化报告保存在
`outputs/validation/ramulator2_diff_v2.json`，字段 `passed=true`。报告保留 raw 与
canonical 两套事件，避免等价命令表示规范化后失去审计能力。

该结果只证明刻意对齐的共同命令面可比较，不覆盖真实 payload、DFI、文件后端、
ECC、floorplan、功耗、热、多 Stack 或厂商私有参数；Ramulator2.1 是非规范性
外部参考，不是本项目实现裁决标准。

### 11.6 验证结论

- 软件构建、执行和默认回归：通过。
- 真实数据、存储后端和错误路径：通过项目级验收。
- 控制器命令、状态、scope timing 和维护路径：通过项目级精确测试。
- 四标准入口和 6-Stack：通过可运行性及内部统计验收。
- 性能：通过固定 HBM4 单 channel 60% 回归门槛。
- 来源治理：通过，research defaults 未被隐藏。
- 外部模拟器：Ramulator2.1 四标准共同面 225/225；DRAMsim3 辅助 9/9。
- 厂商器件与真实硬件：未校准，不在本次完成声明内。

## 12. 已实现功能汇总

| 功能 | 状态 | 交付证据 |
| --- | --- | --- |
| C++20 仿真核心 | 已实现 | CMake 构建通过 |
| HBM3/HBM4/LPDDR5/LPDDR6 入口 | 已实现 | smoke + 28 项验收 |
| 29 类抽象命令 | 已实现 | common.hpp、sequence tests |
| FCFS/FR-FCFS | 已实现 | scheduler tests |
| open/closed/ClosedCAP | 已实现 | row policy tests |
| 4 地址映射、3 channel mapper | 已实现 | mapping tests |
| HBM 双总线/edge pairing | 已实现 | pairing matrix tests |
| LPDDR split activate/WCK/CAS | 已实现 | LPDDR sequence tests |
| Refresh、REFDB、RFM/PRAC | 已实现研究模型 | manager tests |
| 初始化、MR、DVFS、低功耗 | 已实现抽象路径 | control/low-power tests |
| 单 Stack 多 controller | 已实现 | parallel channel tests |
| 主动 6-Stack/192 controller | 已实现 | routing/QoS tests |
| streaming/trace/burst | 已实现 | smoke/frontend tests |
| 64 B→2×32 B transaction | 已实现 | HBM4/LPDDR6 split tests |
| 真实 payload/mask/expect | 已实现 | data correctness tests |
| row buffer/writeback/forward | 已实现 | storage sequence tests |
| sparse/mmap/chunk | 已实现 | persistence tests |
| checkpoint/CSV/golden/mismatch | 已实现 | smoke 正负例 |
| 物理逻辑坐标 | 已实现 | coordinate tests |
| configured/IDD 功耗 | 已实现研究入口 | power tests |
| sparse 3D RC/TSV thermal | 已实现研究入口 | thermal tests |
| SECDED shadow | 已实现研究入口 | ECC tests |
| command/DFI/timing 导出 | 已实现 | CSV smoke |
| command/DFI validator | 已实现 | online validators pass |
| 来源清单和 strict timing | 已实现 | manifest/source audit |
| Timing 自动边界 | 已执行，315/315 行 | C++ probe + CSV/JSON |
| Ramulator2 共同面差分 | 已执行，225/225 通过 | differential script + JSON 报告 |
| 性能曲线/敏感性/区间 | 已执行 | 84 点曲线、72 点 OAT、联合扰动 JSON |
| DRAMsim3 辅助验证 | 已执行，9/9 通过 | 公共 HBM 命令、IDD、热趋势报告 |
| 真实 vendor/硬件校准 | 未完成 | vendor gap manifest |

## 13. 完成指标总结

### 13.1 数量指标

| 指标 | 完成值 |
| --- | ---: |
| 支持标准 | 4 |
| 抽象命令 | 29 |
| 调度策略 | 2 |
| 行策略 | 3 |
| 地址映射模板 | 4 |
| channel mapper | 3 |
| Stack mapping | 2 |
| 存储后端 | 3 |
| 主运行配置 | 6 |
| profile index 项 | 9 |
| 项目身份能力项 | 10 |
| vendor 缺口分类 | 10 |
| sequence 实际调用 | 54 |
| smoke 命名场景 | 37 |
| model validation | 28/28 |
| CTest | 7/7 |
| Timing 边界行 | 315/315 |
| Ramulator2.1 共同面差分 | 225/225 |
| 性能曲线 | 84 点，32/32 checks |
| 敏感性/不确定性 | 72 点 OAT、每协议 24 样本，34/34 checks |
| DRAMsim3 辅助 | 9/9 |

### 13.2 功能完成指标

- 100% 默认 CTest 顶层用例通过。
- 100% 项目原生 28 项验收通过。
- 四标准入口均完成请求且不触发 cycle limit。
- sparse、mmap_sparse、chunk_file 均有自动测试。
- HBM4 单 channel 理论带宽公式误差为 0。
- ACT→RD、closed-row read、DFI read latency 均与精确期望相等。
- mismatch 负例按设计返回 3。
- timing source 46 项无非法来源，19 项 research default 明确可见。
- project identity 10 项均绑定验证证据，无缺失证据。
- Ramulator2.1 四标准 225 项检查全部通过；局部容差和抽象差异均在 JSON 中具名记录。

### 13.3 性能指标的正确解释

“完成指标”中的带宽分为三类：

1. 理论峰值：由速率和总线宽度公式计算。
2. 固定回归性能：项目原生单 channel workload 的 41.318 GB/s、64.56%。
3. 代表运行统计：用于展示具体配置的实际 workload 结果。

只有第 2 类用于自动回归门槛；第 1 类不是实测，第 3 类不是跨标准公平 benchmark。

## 14. 限制、风险与校准要求

### 14.1 参数风险

| 领域 | 当前来源 | 风险 | 交付后动作 |
| --- | --- | --- | --- |
| RL/WL、nRCD | research/selected branch | 目标 speed-bin 延迟不确定 | 导入 vendor latency/MR 表 |
| nRP/nRAS/nRC/nRRD/nFAW | 部分 research | 行时序数值不具器件权威性 | 按 density/temp 校准 |
| refresh | JEDEC/derived 为主 | 温度和 density 分支可能不同 | 核对 tREFI/tRFC |
| RFM/RAS | research policy | 阈值、恢复和覆盖面不完整 | 导入 RAA/PRAC/RFM 资料 |
| ECC/link | 接口/shadow 抽象 | 不能推断真实可靠性 | 导入 syndrome/retry/scrub |
| DFI/WCK/DVFS | project-defined | 不是 PHY 合规认证 | 用 PHY/vendor 时序校准 |
| power | research pJ/示例 IDD | 不能作为绝对功耗 | 用目标 VDD/IDD 或测量 |
| thermal | sparse RC | 不能预测封装绝对温度 | 对接 HotSpot/3D-ICE/实测 |
| geometry | 逻辑坐标 | 不等于真实版图 | 导入 floorplan/TSV/material |

### 14.2 工程风险

- CMake build directory 记录源码绝对路径，项目移动后必须重新 configure；不要复用
  指向旧目录的 CMakeCache。
- Makefile 使用 ?=，外部 CXX/CPPFLAGS/CXXFLAGS 可覆盖默认值。
- 自定义 Clang 工具链可能找不到自定义 GCC 的 libstdc++；需使用正确 sysroot 或
  --gcc-toolchain，或者直接用对应 g++。
- 完整 command/DFI trace 与命令数成比例占用内存，只应为有限调试窗口启用。
- mmap/chunk 文件格式是项目内部格式，不承诺跨版本兼容或异常断电事务一致性。
- topology_stats_scan_limit 跳过详细枚举时，容量和唯一写仍精确，但拓扑去重统计不完整。
- 过小 max_cycles 会得到未完成结果；必须检查 remaining 和 hit_cycle_limit。

### 14.3 声明边界模板

推荐报告中使用：

> 本实验采用 hbm_sim 的 JEDEC-oriented/研究基线配置。控制器状态、时序 gate、
> 数据和接口事件通过项目自动化回归；缺少目标厂商资料的 timing、RFM、RAS/ECC、
> PHY、功耗和物理参数保留为显式 research defaults。结果用于架构和相对趋势研究，
> 不代表某款器件的认证性能、功耗或温度。

## 15. 交付接收、复现与维护

### 15.1 最小接收流程

~~~bash
cmake -S . -B build-acceptance -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18
cmake --build build-acceptance --parallel
ctest --test-dir build-acceptance --output-on-failure

python3 tools/model_validation.py \
  --binary ./build-acceptance/hbm_sim \
  --json-out outputs/acceptance/model_validation.json
~~~

接收判据：

- build 返回 0。
- CTest 7/7。
- model validation 28/28。
- JSON passed=true。
- 运行标准配置时无 remaining、无 cycle limit。
- 正式实验的 timing.csv 来源与报告声明一致。

### 15.2 建议证据包

~~~text
outputs/<date>_<experiment>/
  config.cfg
  command.txt
  environment.txt
  git_revision.txt
  stats.txt
  timing.csv
  commands.csv
  dfi.csv
  dfi_signal.csv
  final_memory.csv
  mismatch.txt
  thermal_map.txt
  model_validation.json
  ramulator2_diff.json       # 若执行外部差分
  notes.md
~~~

notes.md 至少记录实验目标、标准/profile、source、workload、随机种子、映射、请求数、
停止条件、外部参考版本、可声明结论和不可声明结论。

### 15.3 扩展入口

| 修改任务 | 主要位置 | 必补验证 |
| --- | --- | --- |
| 新标准/别名 | standard_traits.cpp、profiles.cpp | factory/profile/smoke |
| 新 timing | spec.hpp、profiles、配置解析 | timing table + sequence |
| 新命令 | common.hpp、semantics、state、executor | state/timing/validator |
| 新调度 | scheduler.*、Controller 选择路径 | ready/ordering test |
| 新行策略 | row_policy.* | AP/PRE sequence |
| 新地址映射 | addr_map.* | decode/capacity test |
| 新后端 | memory_backend.* | reopen/persistence/golden |
| 新数据语义 | data.*、Controller completion | mask/forward/mismatch |
| 新 DFI 字段 | dfi.*、CLI dump | positive + injected negative |
| 新统计 | stats.*、CLI print | smoke 字段和公式 |
| 新 profile | configs/profiles | source audit、strict、small run |

### 15.4 文档索引

本交付文档给出验收总览；进一步源码级说明可查：

- README.md：项目入口。
- 文档/架构和构建流程.md：详细架构。
- 文档/内存模型和平台.md：四标准横向说明。
- 文档/仿真使用手册.md：操作和输出字段。
- 文档/多Stack及存储后端.md：多 Stack 与后端。
- 文档/审计.md：来源与厂商数据边界。
- VALIDATION_AND_CALIBRATION.md：外部模型和硬件校准路线。
- configs/README.md：完整配置字段字典。
- tests/README.md、tools/README.md：测试和工具。

## 附录 A：本次验收命令与原始结论

~~~bash
cmake -S . -B build-delivery-validation -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_CXX_COMPILER=/usr/bin/clang++-18 \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-delivery-validation --parallel
ctest --test-dir build-delivery-validation --output-on-failure
./build-delivery-validation/sequence_tests
python3 tools/model_validation.py \
  --binary ./build-delivery-validation/hbm_sim \
  --json-out /tmp/hbm_sim_delivery_model_validation.json
python3 tools/ramulator2_differential.py \
  --binary ./build-delivery-validation/hbm_sim \
  --ramulator-root /home/guxing/learn/memorystack/ramulator2 \
  --json-out outputs/validation/ramulator2_diff.json
~~~

原始汇总结论：

~~~text
100% tests passed, 0 tests failed out of 7
sequence tests passed
model validation: 28/28 passed
timing boundary validation: 315/315 rows passed
Ramulator2.1 four-standard overlap: 225/225 checks passed
performance curves: 32/32 checks passed; rows=84
sensitivity/uncertainty: 34/34 checks passed; oat_rows=72
DRAMsim3 auxiliary validation: 9/9 checks passed
~~~

## 附录 B：交付验收清单

- [x] 总体设计说明。
- [x] 模块详细设计说明。
- [x] 四标准模型说明。
- [x] 配置、构建、运行和输出手册。
- [x] 功能测试方法、用例、判据和结果。
- [x] 仿真验证公式、性能、数据、DFI 和来源结果。
- [x] 已实现功能和完成指标汇总。
- [x] 模型边界、风险和校准要求。
- [x] 根目录单一交付文档。
- [x] 本地全新构建与默认回归。
- [x] 四标准 Timing 边界矩阵：315/315 行通过。
- [x] Ramulator2.1 四标准共同面差分：225/225 通过。
- [x] 注入率曲线、敏感性和输入不确定性区间已生成。
- [x] DRAMsim3 公共 HBM/IDD/热辅助验证：9/9 通过。
- [ ] 目标厂商器件校准：需具体料号、datasheet/model、温度和 mode。
- [ ] 真实硬件校准：需测试平台、计数器/仪器和测量数据。

---

最终结论：本项目已达到“可构建、可运行、可配置、可审计、项目内部验证通过”的
研究型堆叠存储仿真模型交付状态；器件级数值精度和硬件一致性属于下一阶段校准工作，
不应由本次内部回归结果替代。
